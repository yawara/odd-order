---
id: 9126
slug: psu-borel-subgroup
title: "claim: PSU(3,q) standard Borel subgroup (lane b)"
created: 2026-07-18
---

# claim: PSU(3,q) standard Borel subgroup (lane b)

## 背景

Peterfalvi Part II, Chapter I §3, Lemma 1 uses the standard subgroup structure
of `PSU(3,q)`. Issue 9125 and commit `deafcc848` constructed the honest finite
permutation group generated by the Hermitian root translations, determinant-one
torus, and Weyl element. No open 9000-series claim owns the next upstream layer:
the faithful root--torus semidirect product and its exact order.

## やること

- [ ] Define `BorelModel n` using `RootGroup n`, `PSUTorusParameter n`, and the
  restricted action `psuTorusScaleHom n`.
- [ ] Construct the root-times-torus monoid homomorphism into
  `standardPermGroup n` and prove it injective by evaluating at the affine origin.
- [ ] Define `standardBorel` as the actual range; prove root/torus membership and
  the unique root-times-torus normal form.
- [ ] Prove `standardBorel ≤ stabilizer infinity` without asserting the reverse
  inclusion before the later Bruhat decomposition.
- [ ] Prove the exact PSU Borel order
  `2^(3*n) * ((2^(2*n)-1) / gcd(2^n+1,3))` for `0 < n`.
- [ ] Wire the leaf into the project and axiom audit; update the Peterfalvi
  frontier after strict, module, audit, and full builds pass.

## 完了条件

The standard Borel is an honestly constructed subgroup isomorphic to the
restricted PSU root--torus semidirect product, with faithful representation,
unique normal form, stabilizer inclusion, and exact order. No `sorry`, new
`axiom`, full-PGU torus substitution, or opaque group law is introduced.

## 参照

Upstream: issue 9125, commit `deafcc848`,
`ProjectiveUnitary/GeneratedAction.lean`. Architecture reference:
`SpecificGroups/Suzuki/Borel.lean`. Next consumer: the PSU Bruhat decomposition,
which will prove the reverse stabilizer inclusion and the full group order.
