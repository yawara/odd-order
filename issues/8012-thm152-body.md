---
id: 8012
slug: thm152-body
title: "BG Theorem 15.2 proof body (step 2-5, §14-gated conditional helpers)"
created: 2026-06-16
---

# BG Theorem 15.2 proof body (step 2-5, §14-gated conditional helpers)

## 背景

`mf_ne_msigma_typeP1_structure` (`S15_MF.lean`, Thm 15.2) は §15→§16 funnel の keystone
(Cor 15.3 の `Q`, Cor 15.6 の `K*⊆M_F` を供給) だが本体未証明 (sorry)。issue 8011 で唯一の §3 gate
**BG Theorem 3.8** (`S03h.thm38`, `⁅K,R⁆⊆F(K)` coprime action) を形式化・main 合流済み ⟹ proof の
step 2 が解禁。純 `_of_inputs` skeleton は効かない (構造出力 Q/D/Q0/Q̄/p/q に cite 元が無い —
この定理自身の新規内容; audit §14.2) ので、**§14-gated facts を明示仮説に取った sorry-free
conditional helper** で証明本体を積む方針 (monitor net 0、H が Lem 14.1/Thm 14.7(f) を landing
した時点で wrapper の sorry を置換 net −1)。正本 = `notes/bg/s15_16_audit.md` §14。

mmd 証明 = L4190-4202。step 構造:
- **step 1** (§14-gate): Lem 14.1 ⟹ (a) type-P1; Thm 14.7(f) ⟹ `q=|K*|` prime。
- **step 2** (Thm 3.8 で解禁): Prop 14.2(a) (prime action) + Lem 6.3(a) ⟹ `M_σ=⁅M_σ,K⁆`;
  `⊄F(M_σ)` (M_σ 非冪零) + **Thm 3.8 contrapositive** ⟹ `C_{F(M_σ)}(K)≠1` ⟹ `K*⊆Q=O_q(M)`;
  Prop 1.5(d) + Thm 3.7 ⟹ `M_σ/Q` 冪零。⟹ (c)(d)。
- **step 3** (Prop 1.5(a)): K-不変 complement `D` of `Q`; `Q0=C_Q(D)` proper; minimal-normal
  `Q1/Q0` chain ⟹ `Q1=Q`。⟹ (e) + (f) 前半。
- **step 4** (Prop 1.5(d) + Thm 3.10): `F(M)=QC_M(Q)=C_{M_σ}(Q̄)⊂M_σ`; Frobenius `KD` on `Q̄` ⟹
  `p=|K|` prime, `|Q̄|=q^p`, `D'⊆C_D(Q̄)` ⟹ (g)。
- **step 5** (Thm 5.5(a)): `q∉β(M)` ⟹ `D` centralizes `Q` 矛盾 ⟹ `q∈β(M)`。

依存 ledger (audit §14.3): Prop 14.2(a)✅ / Lem 6.3(a)✅ / Thm 3.7✅ / Thm 3.10✅ /
Prop 1.5(a)(d)✅ / Thm 5.5✅ / **Thm 3.8✅ (issue 8011)** / Lem 14.1・Thm 14.7(f) ⏳§14 (Lane H)。

## やること

- [x] **step 2 entry**: `msigma_eq_commutator_kappa_of_isComplement'` (`⁅M_σ,K⁆=M_σ` from type-P1
  complement + M_σ⊆M' via Lem 6.3(a))。sorry-free, landed。
- [x] **step 2 core (Thm 3.8 application)** ✅ landed
  (`centralizer_kappa_inf_fittingInAmbient_ne_bot_of_inputs`, commit `0047671d`): 仮説 = type-P1
  complement (hcompl) + prime-manner action `C_{M_σ}(x)=C_{M_σ}(K)` ∀x∈K^# (Prop 14.2(a)) +
  coprimality (hcop) + `M_F≠M_σ` (⟹ M_σ 非冪零)。結論 = `C_{F(M_σ)}(K)≠1`。証明 = `thm38`
  (G:=↥M, K↦M_σ.subgroupOf M, R↦K.subgroupOf M) の contrapositive。friction 解決: (i) `Odd ↥M` =
  `hG.odd.of_dvd_nat`; (iii) 冪零⟺`fittingInAmbient=self` 逆向き = 新 reusable lemma
  `isNilpotent_of_fittingInAmbient_eq_self` (forward の converse, §14-非依存); (iv) `R^#` 条件は
  thm38 形をそのまま hcond2 に。(ii) κ/σ coprimality は **hcop として仮説化** (wrapper が Hall 構造
  から供給; §14-非依存ゆえ将来 inline 導出も可)。`by_contra hbot` は二重否定を自動簡約 (not_not 不要)。
  import `S03h_Thm38` 追加。leaf build green 3115 jobs, sorry 0。
- [x] **step 2 tail — K*⊆Q** ✅ landed: `kstar_le_fittingInAmbient_of_inputs` (↥M, part1, K*⊆F(M_σ)
  via `le_of_inf_ne_bot_of_card_prime`) + `kstar_le_opiCore_of_le_fittingInAmbient` (part2, q-core 鎖
  `piGroup_le_opiCoreInG_of_nilpotent`→char 鎖正規→`le_opiCoreInG_of_normal_of_isPiSubgroup`) +
  `kstar_le_fittingInAmbient_G_of_inputs` (↥M↔G bridge: `centralizer_subgroupOf`/`fitting_subgroupOf_map_subtype_eq`、
  step2 chain assemble 検証) + `kstar_le_opiCore_of_inputs` (合成 K*⊆Q=O_q(M))。支持 reusable =
  `isNilpotent_of_fittingInAmbient_eq_self`。import S03h/S05 追加。
- [x] **step 2 tail — (d) D 冪零** ✅ landed: `complement_isNilpotent_of_inputs`。商 M_σ/Q を避け、
  同型 complement D≤M_σ (D∩Q=1) を Thm 3.7 (`isNilpotent_of_normalizing_primeOrder_fixedPointFree`)
  で直接 nilpotent 化。FPF = C_D(K₁)⊆C_{M_σ}(K₁)=K*⊆Q (hCentleQ=Prop14.2(a)+K*⊆Q) ∧ D∩Q=1。
  ⟹ Thm 15.2 (d) 完了。(c) の Q⊴M+Q=O_q(M) 材料も揃い、残 (c) = Q⊆M_F + Q Sylow。
- [ ] **step 2 (c) 残**: `Q ⊆ M_F` + Q が normal Sylow q-subgroup。
- [x] **step 3 setup** ✅ landed (3 reusable bricks): `inf_centralizer_ne_self_of_sup_not_nilpotent`
  (Q₀=C_Q(D)⊊Q ← M_σ 非冪零 + `isNilpotent_sup_of_commutator_eq_bot`) + `sup_le_normalizer_centralizer_inf`
  (Q₀ は K⊔D-不変 ← `normalizer_le_normalizer_centralizer`+`le_normalizer_inf`) +
  `lt_normalizer_of_lt_of_isNilpotent` (N_Q(Q₀)⊋Q₀ ← 冪零 normalizer condition
  `lt_normalizer_of_isNilpotent_of_lt_top`+`subgroupOf_normalizer_eq`)。配置 = isNilpotent_sup 直後
  (forward-ref 回避; split 0071 で wrapper 前へ整列)。
- [x] **step 3 前提: `exists_minimal_normal_le` de-private** ✅ (commit `ff50b0ee`):
  `Subgroup.SchurZassenhausConj.exists_minimal_normal_le` を public 化。L794 で既に quotient `G⧸N` に
  適用される pattern ⟹ N_M(Q₀)/Q₀ の minimal normal Q₁/Q₀ 取得に流用可。
- [ ] **step 3 hard core** (Q₁ 解析、heavy quotient machinery、multi-turn): mmd L4194。
  - (i) **Q₁ existence**: `G' := ↥(N_M(Q₀))⧸(Q₀.subgroupOf N_M(Q₀))` で、N_Q(Q₀)/Q₀ (非自明正規,
    N_Q(Q₀)⊋Q₀ ∧ N_Q(Q₀)=Q⊓N_M(Q₀)⊴N_M(Q₀)) に `exists_minimal_normal_le` → minimal normal
    L=Q₁/Q₀。pullback (`QuotientGroup.comap_mk'` 系) で Q₀⊆Q₁⊆N_Q(Q₀)⊆Q。
  - (ii) **regular-action 矛盾**: K*⊄Q₁ or K*⊆Q₀ なら K が DQ₁/Q₀ に regular 作用 (prime-manner
    C_{M_σ}(K)=K* の像が trivial) → Thm 3.7 → D が Q₁/Q₀ centralize → Prop 1.5 で Q₁=C_{Q₁}(D)Q₀=Q₀
    矛盾 ⟹ Q₁⊇K*, Q₀⊉K*。同論法 (Q₁↔Q₀) で Q₁⊄Q if Q₁⊊Q ⟹ **Q₁=Q**。⟹ (e)+(f)前半。
  - **(i) Q₁ existence ✅ landed** (`exists_minimal_normalOver`, commit `dcac68a6`): **QuotientGroup
    machinery 不要**と判明 — `Set.Finite.exists_minimal` を部分群 interval `{H | Q₀<H≤T, H⊴N}` に
    適用すれば minimal-normal-over-Q₀ が直接得られる (quotient-free)。N=N_M(Q₀), T=N_Q(Q₀)。
  - **(ii) regular-action 矛盾 = 真の hard core (heavy quotient/coprime machinery、未着手)**:
    調査結論 (2026-06-16) — 一般 coprime 分解 `Q₁=⁅Q₁,D⁆·C_{Q₁}(D)` は **未形式化**
    (`Isaacs.Ch04` Thm 4.34 = `Main:3386` は **abelian 版のみ** `Ḡ=C×⁅G,A⁆`)。⟹ 2 経路:
    - **経路 A (既存 machinery 活用)**: D の Q₁ への共役 MulAut 作用 (`φ:D→MulAut Q₁`) を setup →
      `fixedPointsOfMulAut_quotientMulAutHom_eq_map` (S03h:414, Prop 1.5(d) subgroup form, 既存) で
      `C_{Q̄₁}(D)=C_{Q₁}(D)Q₀/Q₀` → 商 Q̄₁=Q₁/Q₀ (minimal normal ⟹ elementary abelian) に Thm 4.34 →
      「D が Q̄₁ 中心化 ⟹ Q₁=C_{Q₁}(D)Q₀」。+ regular-action 部 (K が DQ₁/Q₀ に FPF → Thm 3.7
      `isNilpotent_of_normalizing_primeOrder_fixedPointFree` を商の subgroup に適用 → DQ̄₁ 冪零 →
      D が Q̄₁ 中心化)。**heavy = MulAut 作用構成 + 商上 Thm 3.7、multi-turn grind。**
    - **経路 B**: 一般 `G=⁅G,A⁆C_G(A)` を新規形式化 (induction on |G| + minimal-normal + abelian
      Thm 4.34)。foundational reusable だが substantial。
    - friction: いずれも商/MulAut machinery が本質 (part (i) のような quotient-free 回避は不可、
      conclusion が商 Q̄₁ の中心化作用そのものゆえ)。MATH は明確、純 formalization 労力。
      ChatGPT は repo 固有 API を知らず此処では限定的 ([[feedback-ask-chatgpt-for-elided-gaps]] は
      math gap 用、本件は formalization gap)。
  - **✅ 方針決定 (2026-06-16, ユーザー裁可)**: **経路 B = 一般 coprime 分解を先に形式化**。
    `eq_commutator_sup_centralizer_of_coprime`: D が N を正規化 + (|D|,|N|)=1 ⟹
    `N = ⁅N,D⁆ ⊔ (N ⊓ C_G(D))` (非可換一般形、Isaacs Thm 4.34 abelian 版の一般化、reusable)。
    **証明 route (確定)**: D は N/⁅N,D⁆ に自明作用 (commutator 殺す) → `coprime_fixedPoints_quotient`
    (Isaacs Cor 3.28, Prop 1.5(d)) で各 n∈N が D-fixed 代表を持つ ⟹ C_N(D) が N/⁅N,D⁆ に surject ⟹
    N=⁅N,D⁆·C_N(D)。**MulAut setup template = S03h_Thm38:159-199** (`ψ:=MulAut.conj.comp D.subtype` /
    `IsAInvariant ψ N` [N正規でなく D≤N_G(N) ゆえ normalizer 版が要] / `hXinv.restrict` /
    `coprime_fixedPoints_quotient_of_coprime_normal hCop hSolv hN_inv hgfix`)。⁅N,D⁆≤N は D≤N_G(N) から。
    ~50-70 行、multi-build-iteration。part (ii) で N=Q₁,Q₀ に適用: ⁅Q₁,D⁆⊆Q₀ ∧ C_{Q₁}(D)⊆Q₀ ⟹ Q₁⊆Q₀ 矛盾。
  - **🛑 一般分解 WIP は obstacle で頓挫・revert (2026-06-16)**: `le_commutator_sup_centralizer_of_coprime`
    を S03h template で書いたが、`coprime_fixedPoints_quotient_of_coprime_normal` が商の分母
    `⁅N,D⁆.subgroupOf N` の **↥N での Normal インスタンス**を要求 → **`⁅N,D⁆ ⊴ N`** が必要だが、これは
    D normalizes N からは自明でない (mathlib `commutator_normal` は両 normal 要求; `commutator_le_left`
    は ≤ のみ)。⟹ 一般分解は normality sub-lemma を別途要する。
  - **▶ refined path (採用)**: 一般分解でなく **specific lifting**「D normalizes Q₁ + Q₀⊴(ambient,
    正規!) + ⁅Q₁,D⁆⊆Q₀ + coprime ⟹ Q₁=C_{Q₁}(D)·Q₀」。**Q₀ は正規 (Q₀⊴N_M(Q₀)) ゆえ normality
    obstacle 回避**。既存 `fixedPointsOfMulAut_quotientMulAutHom_eq_map` (S03h:414, Prop 1.5(d)) が
    C_{Q₁/Q₀}(D)=C_{Q₁}(D)Q₀/Q₀ を与える ⟹ ⁅Q₁,D⁆⊆Q₀ で D が Q₁/Q₀ 自明作用 → Q₁=C_{Q₁}(D)Q₀。
    MulAut setup は依然必要だが [N,D]⊴N 不要。これが part (ii) の実用形。
  - **✅ refined-path lifting LANDED** (`le_sup_inf_centralizer_of_commutator_le`, S15_MF.lean,
    sorry-free, leaf build green 3115 jobs): 仮説 = `M₀≤N` + `D≤N_G(N)` + `N≤N_G(M₀)` +
    `D≤N_G(M₀)` + `⁅N,D⁆≤M₀` + coprime(|D|,|N|) + (D∨N solvable); 結論 = `N ≤ M₀ ⊔ (N ⊓ C_G(D))`
    (= `N = M₀·C_N(D)`)。証明 route = φ:=`(normalizerMonoidHom N).comp(inclusion hDN)` (D-on-↥N
    共役作用) → `IsAInvariant φ (M₀.subgroupOf N)` (D≤N_G(M₀) を `mem_normalizer_iff` で) →
    `actionCommutator φ ≤ M₀'` (S06 `actionCommutator_conj_map_subtype`) → quotient fixed pts =⊤
    (`actionCommutator_quotient_eq_map`+`acts_trivially`) → Prop 1.5(d) で
    `C_N(D)⊔M₀'=⊤` (`comap_map_eq`) → N.subtype で押し出し (S06 `fixedPointsOfMulAut_conj_map_subtype`
    + `subgroupOf_map_subtype`)。**reusable**: 任意の正規 D-不変 M₀⊇⁅N,D⁆ で `N=M₀·C_N(D)`。
  - **▶ 残 part (ii) (次)**: regular-action 部 — **`⁅Q₁,D⁆⊆Q₀` を確立**する論法 (K が Q₁/Q₀ または
    DQ₁/Q₀ に FPF 作用 → Thm 3.7 で nilpotent → D が Q₁/Q₀ 中心化)。これと上記 lifting +
    `C_{Q₁}(D)⊆Q₀` (Q₀=C_Q(D) ⟹ C_{Q₁}(D)=Q₁⊓C_Q(D)=Q₁⊓Q₀=Q₀) で `Q₁⊆Q₀` 矛盾 ⟹ K*⊆Q₁ 等
    → 同論法反復で **Q₁=Q**。mmd L4194 を精読して FPF/Thm 3.7 適用形を確定する必要あり。
- [ ] **step 4**: Frobenius `KD` on Q̄ + Thm 3.10 (hard core、step 3 gate)。⟹ (g)。
- [ ] **step 5**: `q∈β(M)` (Thm 5.5(a)、step 4 gate)。
- [ ] **wrapper 配線**: H が Lem 14.1/Thm 14.7(f) landing 後、`mf_ne_msigma_typeP1_structure`
  (S15_MF:888) の sorry を helper 群の assembly で置換 (net −1)。

## 完了条件

`mf_ne_msigma_typeP1_structure` が sorry-free + axiom-clean。それまでは各 conditional helper が
sorry-free で landing し、残りが正確に §14 obligation (Lem 14.1 / Thm 14.7(f)) を名指す状態を維持。

## 参照

- issue 8011 (closed): BG Theorem 3.8 (step 2 の §3 gate)。
- `notes/bg/s15_16_audit.md` §14 (証明プラン正本)。
- mmd `references/bg/local-analysis.mmd` L4180-4202 (Thm 15.2 statement + proof)。
- `S03h.thm38` (`OddOrder/BG/Ch1_Preliminary/S03h_Thm38.lean:1012`)。
- Lem 6.3(a) = `OddOrder.BG.Ch1.S06.commutator_eq_self_of_isComplement'_le_commutator`。
