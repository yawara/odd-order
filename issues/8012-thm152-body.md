---
id: 8012
slug: thm152-body
title: "BG Theorem 15.2 proof body (step 2-5, §14-gated conditional helpers)"
created: 2026-06-16
---

# BG Theorem 15.2 proof body (step 2-5, §14-gated conditional helpers)

## 背景

`mf_ne_msigma_typeP1_structure` (`S15_MF.lean`, Thm 15.2) は §15→§16 funnel の keystone
(Cor 15.3 の `Q`, Cor 15.6 の `K*⊆M_F` を供給) だが本体未証明 (sorry)。issue 8011 で唯一の §3 gate
**BG Theorem 3.8** (`S03h.thm38`, `⁅K,R⁆⊆F(K)` coprime action) を形式化・main 合流済み ⟹ proof の
step 2 が解禁。純 `_of_inputs` skeleton は効かない (構造出力 Q/D/Q0/Q̄/p/q に cite 元が無い —
この定理自身の新規内容; audit §14.2) ので、**§14-gated facts を明示仮説に取った sorry-free
conditional helper** で証明本体を積む方針 (monitor net 0、H が Lem 14.1/Thm 14.7(f) を landing
した時点で wrapper の sorry を置換 net −1)。正本 = `notes/bg/s15_16_audit.md` §14。

mmd 証明 = L4190-4202。step 構造:
- **step 1** (§14-gate): Lem 14.1 ⟹ (a) type-P1; Thm 14.7(f) ⟹ `q=|K*|` prime。
- **step 2** (Thm 3.8 で解禁): Prop 14.2(a) (prime action) + Lem 6.3(a) ⟹ `M_σ=⁅M_σ,K⁆`;
  `⊄F(M_σ)` (M_σ 非冪零) + **Thm 3.8 contrapositive** ⟹ `C_{F(M_σ)}(K)≠1` ⟹ `K*⊆Q=O_q(M)`;
  Prop 1.5(d) + Thm 3.7 ⟹ `M_σ/Q` 冪零。⟹ (c)(d)。
- **step 3** (Prop 1.5(a)): K-不変 complement `D` of `Q`; `Q0=C_Q(D)` proper; minimal-normal
  `Q1/Q0` chain ⟹ `Q1=Q`。⟹ (e) + (f) 前半。
- **step 4** (Prop 1.5(d) + Thm 3.10): `F(M)=QC_M(Q)=C_{M_σ}(Q̄)⊂M_σ`; Frobenius `KD` on `Q̄` ⟹
  `p=|K|` prime, `|Q̄|=q^p`, `D'⊆C_D(Q̄)` ⟹ (g)。
- **step 5** (Thm 5.5(a)): `q∉β(M)` ⟹ `D` centralizes `Q` 矛盾 ⟹ `q∈β(M)`。

依存 ledger (audit §14.3): Prop 14.2(a)✅ / Lem 6.3(a)✅ / Thm 3.7✅ / Thm 3.10✅ /
Prop 1.5(a)(d)✅ / Thm 5.5✅ / **Thm 3.8✅ (issue 8011)** / Lem 14.1・Thm 14.7(f) ⏳§14 (Lane H)。

## やること

- [x] **step 2 entry**: `msigma_eq_commutator_kappa_of_isComplement'` (`⁅M_σ,K⁆=M_σ` from type-P1
  complement + M_σ⊆M' via Lem 6.3(a))。sorry-free, landed。
- [x] **step 2 core (Thm 3.8 application)** ✅ landed
  (`centralizer_kappa_inf_fittingInAmbient_ne_bot_of_inputs`, commit `0047671d`): 仮説 = type-P1
  complement (hcompl) + prime-manner action `C_{M_σ}(x)=C_{M_σ}(K)` ∀x∈K^# (Prop 14.2(a)) +
  coprimality (hcop) + `M_F≠M_σ` (⟹ M_σ 非冪零)。結論 = `C_{F(M_σ)}(K)≠1`。証明 = `thm38`
  (G:=↥M, K↦M_σ.subgroupOf M, R↦K.subgroupOf M) の contrapositive。friction 解決: (i) `Odd ↥M` =
  `hG.odd.of_dvd_nat`; (iii) 冪零⟺`fittingInAmbient=self` 逆向き = 新 reusable lemma
  `isNilpotent_of_fittingInAmbient_eq_self` (forward の converse, §14-非依存); (iv) `R^#` 条件は
  thm38 形をそのまま hcond2 に。(ii) κ/σ coprimality は **hcop として仮説化** (wrapper が Hall 構造
  から供給; §14-非依存ゆえ将来 inline 導出も可)。`by_contra hbot` は二重否定を自動簡約 (not_not 不要)。
  import `S03h_Thm38` 追加。leaf build green 3115 jobs, sorry 0。
- [x] **step 2 tail — K*⊆Q** ✅ landed: `kstar_le_fittingInAmbient_of_inputs` (↥M, part1, K*⊆F(M_σ)
  via `le_of_inf_ne_bot_of_card_prime`) + `kstar_le_opiCore_of_le_fittingInAmbient` (part2, q-core 鎖
  `piGroup_le_opiCoreInG_of_nilpotent`→char 鎖正規→`le_opiCoreInG_of_normal_of_isPiSubgroup`) +
  `kstar_le_fittingInAmbient_G_of_inputs` (↥M↔G bridge: `centralizer_subgroupOf`/`fitting_subgroupOf_map_subtype_eq`、
  step2 chain assemble 検証) + `kstar_le_opiCore_of_inputs` (合成 K*⊆Q=O_q(M))。支持 reusable =
  `isNilpotent_of_fittingInAmbient_eq_self`。import S03h/S05 追加。
- [x] **step 2 tail — (d) D 冪零** ✅ landed: `complement_isNilpotent_of_inputs`。商 M_σ/Q を避け、
  同型 complement D≤M_σ (D∩Q=1) を Thm 3.7 (`isNilpotent_of_normalizing_primeOrder_fixedPointFree`)
  で直接 nilpotent 化。FPF = C_D(K₁)⊆C_{M_σ}(K₁)=K*⊆Q (hCentleQ=Prop14.2(a)+K*⊆Q) ∧ D∩Q=1。
  ⟹ Thm 15.2 (d) 完了。(c) の Q⊴M+Q=O_q(M) 材料も揃い、残 (c) = Q⊆M_F + Q Sylow。
- [ ] **step 2 (c) 残**: `Q ⊆ M_F` + Q が normal Sylow q-subgroup。
- [x] **step 3 setup** ✅ landed (3 reusable bricks): `inf_centralizer_ne_self_of_sup_not_nilpotent`
  (Q₀=C_Q(D)⊊Q ← M_σ 非冪零 + `isNilpotent_sup_of_commutator_eq_bot`) + `sup_le_normalizer_centralizer_inf`
  (Q₀ は K⊔D-不変 ← `normalizer_le_normalizer_centralizer`+`le_normalizer_inf`) +
  `lt_normalizer_of_lt_of_isNilpotent` (N_Q(Q₀)⊋Q₀ ← 冪零 normalizer condition
  `lt_normalizer_of_isNilpotent_of_lt_top`+`subgroupOf_normalizer_eq`)。配置 = isNilpotent_sup 直後
  (forward-ref 回避; split 0071 で wrapper 前へ整列)。
- [x] **step 3 前提: `exists_minimal_normal_le` de-private** ✅ (commit `ff50b0ee`):
  `Subgroup.SchurZassenhausConj.exists_minimal_normal_le` を public 化。L794 で既に quotient `G⧸N` に
  適用される pattern ⟹ N_M(Q₀)/Q₀ の minimal normal Q₁/Q₀ 取得に流用可。
- [ ] **step 3 hard core** (Q₁ 解析、heavy quotient machinery、multi-turn): mmd L4194。
  - (i) **Q₁ existence**: `G' := ↥(N_M(Q₀))⧸(Q₀.subgroupOf N_M(Q₀))` で、N_Q(Q₀)/Q₀ (非自明正規,
    N_Q(Q₀)⊋Q₀ ∧ N_Q(Q₀)=Q⊓N_M(Q₀)⊴N_M(Q₀)) に `exists_minimal_normal_le` → minimal normal
    L=Q₁/Q₀。pullback (`QuotientGroup.comap_mk'` 系) で Q₀⊆Q₁⊆N_Q(Q₀)⊆Q。
  - (ii) **regular-action 矛盾**: K*⊄Q₁ or K*⊆Q₀ なら K が DQ₁/Q₀ に regular 作用 (prime-manner
    C_{M_σ}(K)=K* の像が trivial) → Thm 3.7 → D が Q₁/Q₀ centralize → Prop 1.5 で Q₁=C_{Q₁}(D)Q₀=Q₀
    矛盾 ⟹ Q₁⊇K*, Q₀⊉K*。同論法 (Q₁↔Q₀) で Q₁⊄Q if Q₁⊊Q ⟹ **Q₁=Q**。⟹ (e)+(f)前半。
  - **(i) Q₁ existence ✅ landed** (`exists_minimal_normalOver`, commit `dcac68a6`): **QuotientGroup
    machinery 不要**と判明 — `Set.Finite.exists_minimal` を部分群 interval `{H | Q₀<H≤T, H⊴N}` に
    適用すれば minimal-normal-over-Q₀ が直接得られる (quotient-free)。N=N_M(Q₀), T=N_Q(Q₀)。
  - **(ii) regular-action 矛盾 = 真の hard core (heavy quotient/coprime machinery、未着手)**:
    調査結論 (2026-06-16) — 一般 coprime 分解 `Q₁=⁅Q₁,D⁆·C_{Q₁}(D)` は **未形式化**
    (`Isaacs.Ch04` Thm 4.34 = `Main:3386` は **abelian 版のみ** `Ḡ=C×⁅G,A⁆`)。⟹ 2 経路:
    - **経路 A (既存 machinery 活用)**: D の Q₁ への共役 MulAut 作用 (`φ:D→MulAut Q₁`) を setup →
      `fixedPointsOfMulAut_quotientMulAutHom_eq_map` (S03h:414, Prop 1.5(d) subgroup form, 既存) で
      `C_{Q̄₁}(D)=C_{Q₁}(D)Q₀/Q₀` → 商 Q̄₁=Q₁/Q₀ (minimal normal ⟹ elementary abelian) に Thm 4.34 →
      「D が Q̄₁ 中心化 ⟹ Q₁=C_{Q₁}(D)Q₀」。+ regular-action 部 (K が DQ₁/Q₀ に FPF → Thm 3.7
      `isNilpotent_of_normalizing_primeOrder_fixedPointFree` を商の subgroup に適用 → DQ̄₁ 冪零 →
      D が Q̄₁ 中心化)。**heavy = MulAut 作用構成 + 商上 Thm 3.7、multi-turn grind。**
    - **経路 B**: 一般 `G=⁅G,A⁆C_G(A)` を新規形式化 (induction on |G| + minimal-normal + abelian
      Thm 4.34)。foundational reusable だが substantial。
    - friction: いずれも商/MulAut machinery が本質 (part (i) のような quotient-free 回避は不可、
      conclusion が商 Q̄₁ の中心化作用そのものゆえ)。MATH は明確、純 formalization 労力。
      ChatGPT は repo 固有 API を知らず此処では限定的 ([[feedback-ask-chatgpt-for-elided-gaps]] は
      math gap 用、本件は formalization gap)。
  - **✅ 方針決定 (2026-06-16, ユーザー裁可)**: **経路 B = 一般 coprime 分解を先に形式化**。
    `eq_commutator_sup_centralizer_of_coprime`: D が N を正規化 + (|D|,|N|)=1 ⟹
    `N = ⁅N,D⁆ ⊔ (N ⊓ C_G(D))` (非可換一般形、Isaacs Thm 4.34 abelian 版の一般化、reusable)。
    **証明 route (確定)**: D は N/⁅N,D⁆ に自明作用 (commutator 殺す) → `coprime_fixedPoints_quotient`
    (Isaacs Cor 3.28, Prop 1.5(d)) で各 n∈N が D-fixed 代表を持つ ⟹ C_N(D) が N/⁅N,D⁆ に surject ⟹
    N=⁅N,D⁆·C_N(D)。**MulAut setup template = S03h_Thm38:159-199** (`ψ:=MulAut.conj.comp D.subtype` /
    `IsAInvariant ψ N` [N正規でなく D≤N_G(N) ゆえ normalizer 版が要] / `hXinv.restrict` /
    `coprime_fixedPoints_quotient_of_coprime_normal hCop hSolv hN_inv hgfix`)。⁅N,D⁆≤N は D≤N_G(N) から。
    ~50-70 行、multi-build-iteration。part (ii) で N=Q₁,Q₀ に適用: ⁅Q₁,D⁆⊆Q₀ ∧ C_{Q₁}(D)⊆Q₀ ⟹ Q₁⊆Q₀ 矛盾。
  - **🛑 一般分解 WIP は obstacle で頓挫・revert (2026-06-16)**: `le_commutator_sup_centralizer_of_coprime`
    を S03h template で書いたが、`coprime_fixedPoints_quotient_of_coprime_normal` が商の分母
    `⁅N,D⁆.subgroupOf N` の **↥N での Normal インスタンス**を要求 → **`⁅N,D⁆ ⊴ N`** が必要だが、これは
    D normalizes N からは自明でない (mathlib `commutator_normal` は両 normal 要求; `commutator_le_left`
    は ≤ のみ)。⟹ 一般分解は normality sub-lemma を別途要する。
  - **▶ refined path (採用)**: 一般分解でなく **specific lifting**「D normalizes Q₁ + Q₀⊴(ambient,
    正規!) + ⁅Q₁,D⁆⊆Q₀ + coprime ⟹ Q₁=C_{Q₁}(D)·Q₀」。**Q₀ は正規 (Q₀⊴N_M(Q₀)) ゆえ normality
    obstacle 回避**。既存 `fixedPointsOfMulAut_quotientMulAutHom_eq_map` (S03h:414, Prop 1.5(d)) が
    C_{Q₁/Q₀}(D)=C_{Q₁}(D)Q₀/Q₀ を与える ⟹ ⁅Q₁,D⁆⊆Q₀ で D が Q₁/Q₀ 自明作用 → Q₁=C_{Q₁}(D)Q₀。
    MulAut setup は依然必要だが [N,D]⊴N 不要。これが part (ii) の実用形。
  - **✅ refined-path lifting LANDED** (`le_sup_inf_centralizer_of_commutator_le`, S15_MF.lean,
    sorry-free, leaf build green 3115 jobs): 仮説 = `M₀≤N` + `D≤N_G(N)` + `N≤N_G(M₀)` +
    `D≤N_G(M₀)` + `⁅N,D⁆≤M₀` + coprime(|D|,|N|) + (D∨N solvable); 結論 = `N ≤ M₀ ⊔ (N ⊓ C_G(D))`
    (= `N = M₀·C_N(D)`)。証明 route = φ:=`(normalizerMonoidHom N).comp(inclusion hDN)` (D-on-↥N
    共役作用) → `IsAInvariant φ (M₀.subgroupOf N)` (D≤N_G(M₀) を `mem_normalizer_iff` で) →
    `actionCommutator φ ≤ M₀'` (S06 `actionCommutator_conj_map_subtype`) → quotient fixed pts =⊤
    (`actionCommutator_quotient_eq_map`+`acts_trivially`) → Prop 1.5(d) で
    `C_N(D)⊔M₀'=⊤` (`comap_map_eq`) → N.subtype で押し出し (S06 `fixedPointsOfMulAut_conj_map_subtype`
    + `subgroupOf_map_subtype`)。**reusable**: 任意の正規 D-不変 M₀⊇⁅N,D⁆ で `N=M₀·C_N(D)`。
  - **✅ collapse engine LANDED** (`le_of_commutator_le_centralizerCap`, S15_MF.lean, sorry-free,
    commit `bb91de8a`): `Q₀=C_Q(D) ≤ Q₁ ≤ Q` + normalizer 条件 + `⁅Q₁,D⁆≤Q₀` + coprime ⟹ `Q₁≤Q₀`。
    lifting + `C_{Q₁}(D)=Q₁⊓C_G(D) ≤ Q⊓C_G(D)=Q₀` (Q₁≤Q) の合成。⟹ `⁅Q₁,D⁆⊆Q₀` から直接矛盾。
  - **✅ nilpotent-commute kernel LANDED** (`commutator_eq_bot_of_isNilpotent_of_normal_isPGroup`,
    S15_MF.lean, sorry-free, reusable §14-非依存, commit `f0a05202`): nilpotent 𝓗 + 正規 q-部分群 A
    (q prime) + q′-部分群 B ⟹ `⁅A,B⁆=⊥`。B≤O_{q′}=`opiCoreInG {q}ᶜ ⊤`
    (`piGroup_le_opiCoreInG_of_nilpotent`) + `commutator_le_inf` + `inf_eq_bot_of_coprime`。
  - **▶ 残 part (ii) = quotient 構成 (brick B 本体) + brick A + brick D (heavy, multi-iteration)**:
    - **✅ K-不変性 gap 解決 (no gap)**: Q₁/Q₀ は **N_M(Q₀)/Q₀ 全体の正規部分群** (minimal normal)
      ゆえ K≤N_M(Q₀) が自動的に Q₁/Q₀ を正規化 ⟹ K normalizes Q₁。`exists_minimal_normalOver` が
      与える `Q₁⊴N_M(Q₀)` から従う。reconstruction gap でない。
    - **✅ brick B1 LANDED** (`commutator_le_of_quotient_isNilpotent`, S15_MF, sorry-free, reusable,
      commit `8391f984`): N/Q₀' nilpotent + A₀⊴N q-group + B₀ q′-group ⟹ `⁅A₀,B₀⁆≤Q₀'`。kernel +
      `map_commutator` bridge。⟹ DQ₁/Q₀ nilpotent さえ得れば `⁅Q₁,D⁆⊆Q₀` (N=↥(D⊔Q₁) で instantiate
      + P.subtype pull back)。
    - **✅ B2 transfer LANDED** (`isNilpotent_quotient_subgroupOf_of_isNilpotent_map`, S15_MF,
      sorry-free, commit `3c8b958d`): N⊴G, H≤G, IsNilpotent ↥(H.map mk' N) ⟹
      IsNilpotent(↥H/(N.subgroupOf H))。`quotientKerEquivRange` + `nilpotent_of_mulEquiv`。
    - **✅✅ B2 core COMPLETE** (`isNilpotent_DQ1_quotient_of_regular`, S15_MF, **sorry-free**,
      commit `19a28b84`): regular K₁-作用 (preimage FPF `hFPF`) ⟹ `↥(D⊔Q₁)/Q₀.subgroupOf` nilpotent。
      ambient `Γ=↥(D⊔Q₁⊔K₁)/Q₀`、`ψ,ρ=π∘inclusion` で押し出し、Thm 3.7 (N=ψ.range, R=ρ.range) →
      `quotientKerEquivRange ψ` (ker `hkerψ`) で転送。**8 sub-fact 全充足**: IsSolvable [商可解];
      ρ.range≤N(ψ.range)=⊤ [**ψ.range⊴Γ** ∵ P≤N_G(D⊔Q₁)]; Disjoint [(D⊔Q₁)⊓K₁=⊥ lift]; ψ.range≠⊥
      [hQ0lt]; ρ.range≠⊥+prime [K₁∩Q₀=1, ρ inj]; **FPF translation** [`hπeq` 商等式 helper で
      k fixes x mod Q₀ ⟹ x∈Q₀]。署名に `[(Q0.subgroupOf(D⊔Q1)).Normal]` instance + hFPF 形は
      `k*x⁻¹*k⁻¹*x∈Q0→x∈Q0` (hπeq 出力に整合)。**repo 初の quotient-Thm3.7 構成**。
    - **⟹ part (ii) 矛盾エンジン完成** (lifting+collapse+kernel+B1+transfer+B2core): regular ⟹
      ⁅Q₁,D⁆⊆Q₀ (B2core→transfer→B1) → Q₁≤Q₀ (collapse) → Q₀<Q₁ と矛盾。残 = **brick A** (FPF/regular
      を K*-条件から導出) + **brick D** (Q₁=Q 帰結) + wiring。
    - **✅✅ brick A COMPLETE** (A-core + A-lift, sorry-free): `K*⊆Q₀ ∨ K*⊄Q₁ ⟹ hFPF`。
      - **A-core** `centralizer_inf_DQ1_le_Q0` (commit `d1896672`): `C(k)⊓(D⊔Q₁)⊆Q₀` via Dedekind
        (`inf_sup_eq_of_le_normalizer_of_inf_eq_bot`) + prime-manner `C_{M_σ}(k)=K*` + |K*| prime 場合分け。
      - **A-lift** `fpf_of_centralizer_inf_le` (commit `d37ebacb`): `C(k)⊓(D⊔Q₁)⊆Q₀ ⟹ hFPF`。⟨k⟩ の
        D⊔Q₁ 共役 MulAut 作用 + Prop 1.5(d) で quotient fixed pts =⊥ (C_{D⊔Q₁}(⟨k⟩)⊆Q₀)、generator
        argument (`MulAction.stabilizer.zpow_mem`: k-fixed ⟹ ⟨k⟩-fixed) で x∈Q₀。lifting lemma の
        MulAut 機械を流用。hFPF 形 = `∀x∈D⊔Q₁, k*x⁻¹*k⁻¹*x∈Q₀→x∈Q₀`。
      - **⟹ part (ii) 完全鎖**: K*-condition →(A-core)→ C(k)⊓(D⊔Q₁)⊆Q₀ →(A-lift)→ hFPF
        →(B2-core)→ DQ₁/Q₀ nilpotent →(transfer→B1)→ ⁅Q₁,D⁆⊆Q₀ →(collapse)→ Q₁≤Q₀ ⊥ Q₀<Q₁。
        **全 brick sorry-free。残 = assembly (K*⊆Q₁∧K*⊄Q₀ 導出) + brick D**。
    - **✅✅ part (ii) assembly エンジン LANDED** (`false_of_kstar_condition_of_lt`, S15_MF,
      sorry-free + axiom-clean `[propext, Classical.choice, Quot.sound]`, leaf build 3115 jobs):
      上の完全鎖を 1 定理に合成 — 構造データ + K*-condition `Kstar≤Q0 ∨ ¬Kstar≤Q1` + `Q0<Q1` ⟹ `False`。
      内部 = `hFPF_of_kstar_condition` → `commutator_le_Q0_of_fpf (q:=q)` → collapse
      `le_of_commutator_le_of_inf_centralizer_le` → `Q₁≤Q₀ ⊥ Q₀<Q₁`。**2 用途を 1 engine で兼ねる**:
      (1) step-1 = `¬(hcond)` の contrapositive で `Kstar⊄Q₀ ∧ Kstar≤Q₁`; (2) brick D = `(Q₁,Q₂)` で
      `Kstar≤Q₁` (= 左 disjunct 真) を食わせて `False` ⟹ `Q₁=Q`。導出 helper:
      `hK1Q0=le_sup_right.trans hPQ0`, `Q0<D⊔Q1=hQ0lt.trans_le le_sup_right`,
      `Q1/D≤N(Q0)=le_sup_*.trans hDQ1Q0`, `IsSolvable ↥Q1` via inclusion。
    - **✅✅ step 1 LANDED** (`kstar_le_Q1_of_inputs`, S15_MF, sorry-free + axiom-clean): engine の
      contrapositive (`not_or`/`not_not`) で `¬ Kstar≤Q0 ∧ Kstar≤Q1`。brick D が食う `Kstar≤Q₁` を供給。
    - **✅✅ brick-D primitive LANDED** (`lt_inf_normalizer_of_lt_of_isNilpotent`, S15_MF, sorry-free):
      `Q0<Q` (Q 冪零) ⟹ `Q0 < Q ⊓ N_G(Q0)` (= N_Q(Q0))。既存 `lt_normalizer_of_lt_of_isNilpotent` は
      `≤Q` 包含を落とすので不足 → ↥Q 内 strict step を `map_lt_map_iff_of_injective` +
      `subgroupOf_map_subtype` で押し戻す。Q₂ 構成の chain top `Q₁<N_Q(Q₁)` 用。
    - **✅✅ brick-D construction LANDED** (`exists_chiefFactor_over_normalized`, S15_MF, sorry-free):
      `Q₁<Q` (冪零) + `Q⊴M` + `D,K₁≤M⊓N_G(Q₁)` ⟹ chief factor `Q₂/Q₁` (`Q₁<Q₂≤Q`, `D,K₁≤N_G(Q₂)`,
      `Q₂≤N_G(Q₁)`)。ambient `N=M⊓N_G(Q₁)` が `T=N_Q(Q₁)≤Q` を正規化 (`le_normalizer_inf`:
      `N≤N_G(Q)` via `M≤N_G(Q)`, `N≤N_G(N_G(Q₁))` via `le_normalizer`) → `exists_minimal_normalOver`。
    - **✅✅✅ brick D COMPLETE** (`Q1_eq_Q_of_inputs`, S15_MF, sorry-free + axiom-clean
      `[propext, Classical.choice, Quot.sound]`, leaf build 3115 jobs): `Kstar≤Q₁` 後、`Q₁⊊Q` 仮定 →
      construction で `Q₂` → engine を `(Q₁,Q₂)` に左 disjunct `Or.inl hKstarQ1` で適用 → `Q₂≤Q₁ ⊥
      Q₁<Q₂` ⟹ `Q₁=Q`。(Q₁,Q₂)-level engine 仮説は global data から機械的に降下
      (`Disjoint.mono_left/right`, `Coprime.coprime_dvd_right`, `IsPGroup.to_le∘comap_subtype`,
      `card_dvd_of_le`; 正規化は Q₂ 構成; cap `Q₂⊓C(D)≤Q₁` は `Q⊓C(D)≤Q₁` 経由)。
    - **⟹ ✅✅✅ Theorem 15.2 step 3 part (ii) 完全 assembled (sorry-free)**: engine + step-1 +
      primitive + construction + brick D の 5 helper で `Q₁=Q` (conjuncts (e)/(f) ⊆ chain) を導出
      (type-P/§14-gated 構造仮説をパラメータに取る conditional helper 群として)。
- [ ] **step 4** (hard core、part (ii) 完了で次の frontier)。⟹ (g)。RECON 2026-06-16:
  - **✅ (g) `⊆`-half LANDED** (`fittingInAmbient_le_opiCore_sup_centralizer_inf`, S15_MF, sorry-free
    + axiom-clean): 任意 π で `F(M) ≤ O_π(F(M)) ⊔ (C_G(O_π(F(M)))⊓M)`。π={q}, O_q(F(M))=Q で (g) の
    easy 包含。冪零 F の `opiCoreInG_sup_compl` 分解 + `opiCoreInG_commutator_compl_eq_bot`。
  - **✅✅ (g) `⊇`-half = (g) equality を単一 σ-gap `C_M(Q)≤M_σ` に還元・形式化** (ChatGPT 検証済ルート、
    sorry-free + axiom-clean、`notes/bg/s15_2_step4_chatgpt_prompt.md` の Answer 節が正本):
    - **crux** `isNilpotent_of_centralizes_normal_of_quotient_isNilpotent` — H≤K が正規 Q◁K を中心化 +
      K/Q 冪零 ⟹ H 冪零（central extension `isNilpotent_of_ker_le_center`、H∩Q⊆Z(H)）。汎用 reusable。
    - **capstone** `centralizer_inf_le_fittingInAmbient_of_le_Msigma` — `C_M(Q)≤M_σ` ⟹ `C_M(Q)⊆F(M)`
      （C_M(Q) 冪零 [crux] + ◁M [`M≤N(Q)≤N(C(Q))`] + `nilpotent_normal_le_fitting`）。
    - **wrapper-facing** `fittingInAmbient_eq_sup_centralizer_inf_of_le_Msigma` — `C_M(Q)≤M_σ` ⟹
      `F(M)=Q⊔(C(Q)⊓M)`（eq skeleton + capstone を連結）。
    - **ChatGPT 重要訂正**: `C_M(Q)=C_M(Q̄)` は一般に偽（正: `Q·C_M(Q)=C_M(Q̄)`）; section centralizer
      `C_M(Q̄)` を回避し `C_M(Q)≤M_σ` 直接ルートで形式化（mmd の chain より clean）。
    - **▶ 残 = `C_M(Q)≤M_σ`** = genuine BG σ-uniqueness fact（local structure からは出ない、ChatGPT 反例
      `M=(C₇⋊C₃)×(C₃₁⋊C₅)` で実証）。§10/§12 σ-structure から source か forward-input 化。
  - **🚨🚨 SCOPE 発見 (2026-06-16, Lane G): full BG Theorem 3.10 が未形式化 — step 4 (f)/(g) の真 gate。**
    mmd Thm 3.10 (L1267) = G=KR solvable Frobenius (kernel K, complement R) が冪零 M に作用 +
    (1)coprime (2)`C_M(K)=1` (3)prime-manner ⟹ **(a)** R cyclic prime p / **(b)** `|M|=|C_M(R)|^p` /
    **(c)** `C_M(R)` cyclic なら `K'⊆C_K(M)`。§15.2 step 4 は Frobenius **`KD`** (kernel **D**, complement
    **K**) を `Q̄` に作用させ instantiation `Thm-K↦D, Thm-R↦K, Thm-M↦Q̄`: (a)⟹`p=|K|` prime /
    (b)⟹`|Q̄|=|C_{Q̄}(K)|^p=q^p` / (c)⟹`D'⊆C_D(Q̄)`。
    - **repo の Thm 3.10 形は (a) のみ・abelian kernel 限定** (Thm310 三ファイル全定理を精査確認):
      `prime_card_complement_of_frobenius_conj` (共役形, `hUab`=U abelian, 結論 `|K| prime`) と
      `prime_card_of_abelian_frobenius_weight` (weight 形, abelian K, 結論 `|R| prime`)。**両者とも
      conclusion (b)`|M|=|C_M(R)|^p` も (c)`K'⊆C_K(M)` も与えない**。docstring 明記: 「U abelian で
      BG Case 2 の K₀-minimal-normal reduction を bypass — Prop 14.2(g) には不要」⟹ **Prop 14.2(g)
      (S14:1555 で消費) 専用の specialized 断片**。
    - **§15.2 では kernel D が非 abelian 可** (mmd 結論 (g) が `D'` を一般に保持; D abelian なら D'=1 と
      書くはず)。⟹ repo の abelian-kernel 形は **(a) すら直接適用不可** (一般 kernel は Case 2 の K₀
      reduction 要)。
    - ⟹ **audit §14.3 ledger 「Thm 3.10✅」は §15.2 用途には不正確**。正: ✅ = Prop 14.2(g) 用の
      abelian-kernel conclusion-(a) 断片のみ。**step 4 (f)`|Q̄|=q^p` + (c)`D'⊆C_D(Q̄)` は full Thm 3.10
      (一般 kernel + 結論 (b)(c)) の未形式化部分に gate** (step 2 が Thm 3.8 に gate されたのと同型の §3 gate)。
    - **見積**: 一般 kernel induction (Case1 M₀ 縮約 + Case2 K₀ 縮約) + 結論 (b) free-orbit 次元公式
      (既存 weight machinery 拡張) + (c)。**substantial §3 形式化, multi-session** (Thm 3.8 = issue 8011 と同規模以上)。
  - **✅✅ FPF brick `C_{Q̄}(D)=1` LANDED** (`mem_centralizer_of_centralizes_quotient`, S15_MF,
    sorry-free + axiom-clean `[propext, Classical.choice, Quot.sound]`, full build 3838 jobs/59s):
    `Q₀=C_Q(D)` + D coprime 作用 (`D≤N_G(Q)`, `Q,D≤N_G(Q₀)`) ⟹ D は `Q̄=Q/Q₀` に fixed-point-free
    作用 — `x∈Q` の class が D-中心化 (`∀d∈D, ⁅d,x⁆∈Q₀`) ⟹ `x∈Q₀`。これが `prime_card_complement_of_frobenius_conj`
    の `hCU` (`C_M(U)=1`) 入力。証明 = `fpf_of_centralizer_inf_le` (⟨k⟩版) のミラーだが **FPF source が
    定義的 `C_{↥Q}(D)=Q₀` ゆえ別 centralizer bound 不要 + D 群が直接作用ゆえ generator lift 不要**で単純化。
    Prop 1.5(d) `fixedPointsOfMulAut_quotientMulAutHom_eq_map` で quotient fixed pts = image of `C_Q(D)=Q₀`
    = ⊥。bracket 変換 `⁅a,x⁻¹⁆=x⁻¹⁅a,x⁆⁻¹x` は `x∈Q≤N_G(Q₀)`。`open scoped commutatorElement in` 要
    (element ⁅⁆ の Bracket G G、subgroup ⁅⁆ と別)。
  - **▶ 残 step-4 architecture (次セッション、multi-piece、Q₀⊴M = (e) gate)**: `prime_card_complement_of_frobenius_conj`
    を `G'=↥M⧸(Q₀.subgroupOf M)` (要 `Q₀⊴M` = (e)) で適用。要入力 = (1) ✅FPF→`hCU` の **centralizer-in-G'
    形への bridge** (my brick は element-wise; `Q̄⊓C_{G'}(D̄)=⊥` へ quotient-commutator 変換 ~20-30行) /
    (2) `IsNilpotent Q̄` (Q q-群⟹商冪零、small) / (3) `Q̄≠⊥` (Q₀⊊Q、small) / (4) `coprime |K̄D̄| |Q̄|` /
    (5) **`D̄ abelian`** (`hUab`、BG: U=E₂E₃ abelian、要 source/forward) / (6) **`IsFrobeniusGroup G' K̄D̄`-構造**
    (`hUK_frob`、substantial) / (7) **prime-manner `K̄` on `Q̄`** (Prop 14.2(a) を quotient へ降下)。
    `|Q̄|=q^p` は (a) 別途 weight 形 or (b) `|K|=p`+rank 論法。`O_q(F(M))=Q` は `opiCore_singleton_fittingInAmbient_eq` 済。
  - **✅✅✅ §3 gate 解消 (2026-06-17, lane-g)**: **BG Theorem 3.10 (a)(b)(c) 全形式化完了**
    (`S03g_Thm310General/ElemAbelian.lean`, sorry-free + axiom-clean, main 合流 `0f2b5b23`)。
    一般 kernel + 結論 (b)`|M|=|C_M(R)|^|R|` (`card_eq_pow_card_invariants_of_elemAbelian_general`) +
    (c)`K'⊆C_K(M)` (`commutator_acts_trivially_of_elemAbelian_general`) が揃い、step-4 (f)/(g) の
    SCOPE blocker が消えた。⟹ 上記「残 step-4 architecture」の旧プラン (`prime_card_complement_of_frobenius_conj`
    を G'=M/Q₀ で適用、(a)-only) は **(b)/(c) 一般形を直接適用する方式に更新**。
  - **✅✅ step-4 (f) engine COMPLETE (2026-06-17, lane-g, S15_MF, sorry-free + axiom-clean
    `[propext, Classical.choice, Quot.sound]`)**: `chiefFactor_index_eq_pow_of_inputs` —
    外部 Frobenius 群 `D⊔K` の **商 `↥Q⧸Q₀.subgroupOf Q` への共役 `MulDistribMulAction`** を
    `compHom`+`quotientMulAutHom` で inline 構成 (FPF brick / `fpf_lift_of_centralizer_bot` テンプレ)、
    `hEA.zmodModule` で `Module (ZMod q)` を据え、smul 特徴づけ `a•[x]=[x] ↔ ⁅a,x⁆∈Q₀` を補題化して
    hCK/hFrob/hcond3 を module 形へ翻訳、**一般形 (b) (`card_eq_pow_card_invariants_of_elemAbelian_general`)
    を適用**して `[Q:Q₀] = q^|K|` を得る。濃度 bridge `|inv|=q` は**新 reusable 補題
    `card_invariants_eq_card_of_fixedPoints`** (module を **instance 引数**化して `Module.End` 再合成の
    stuck を回避; invariants ≃ K-fixed 類 = `g.range`=image of C ≃ C/Q₀, 第一同型 + `Subgroup.index_eq_card`)
    に分離。C-subgroup interface (hCfix=K-fixed 類=image of C / hCcard=`[C:Q₀]=q`) で §14 を named-hyp 化 =
    gated-endpoint skeleton。**S15 に `S03g_Thm310ElemAbelian` import 追加**。⚠ 知見: 一般形の結論にある
    `Representation.invariants` 項を `letI` module 文脈で**再記述**すると `Module.End` monoid が
    stuck (metavar) → instance-arg 補題に切り出して **apply** で渡す (hmain と同じ unify 経路) のが回避策。
  - **✅✅ step-4 (g) BUNDLED into the engine (2026-06-17 /loop, commit `45aed597`)**: (f) engine を
    `chiefFactor_card_and_commutator_of_inputs` に改名・拡張し、同 action setup を 1 回で Thm 3.10(b)(c)
    両方に使う。新出力 = **`[Q:Q₀]=q^|K| ∧ ∀g∈⁅D,D⁆,∀x∈Q,⁅g,x⁆∈Q₀`**（= (f) |Q̄|=q^p ∧ (g) D'⊆C_D(Q̄)）。
    (c) は `commutator_acts_trivially_of_elemAbelian_general`、`hcyc`(C_Q̄(K) cyclic)は新補題
    **`finrank_le_one_of_card_eq`**（commit `222eb03d`）で `|inv|=q` から。⁅D,D⁆-結論は `g0∈⁅D,D⁆` を
    `⁅D̄,D̄⁆≤↥(D⊔K)` へ lift（`Subgroup.map_commutator`+`subgroupOf_map_subtype`）して読み出し。
  - **✅✅ conjunct 6 `¬IsCyclic M_F` COMPLETE (2026-06-17 /loop, commit `abb765ca`)**: 新補題
    `not_isCyclic_of_lt_card`（EA q-group で card>q ⟹ 非cyclic、`not_isCyclic_of_card_prime_sq` の q^n 一般化）
    + `not_isCyclic_MF_of_chiefFactor_inputs`（engine の `[Q:Q₀]=q^n`(n=|K|≥2) + `Q⊆M_F` → ¬IsCyclic M_F）。
    全 sorry-free + axiom-clean。
  - **✅✅ conjunct 3 `derivedInG(derivedInG M) ≤ fittingInAmbient M`（M''≤F(M)) LANDED**
    (2026-06-17 /loop, commit `d2961075`, `derivedDerived_le_fittingInAmbient_of_inputs`, S15_MF,
    sorry-free + axiom-clean `[propext, Classical.choice, Quot.sound]`, leaf build 3119 jobs):
    gated-endpoint skeleton で mmd L4198-4201 連鎖 `M_σ'⊆Q·D'⊆C_{M_σ}(Q̄)⊆F(M)` を named-hyp から組立。
    inputs = `h2`(conjunct 2 M_σ=M'、inner `derivedInG M`→`Msigma M` の `rw [← h2]`) +
    `hsigmaprime`(M_σ=QD 構造帰結 `M_σ'⊆Q⊔⁅D,D⁆`) + `hQab`(Q̄可換 `⁅Q,Q⁆⊆Q₀`) +
    `hDcomm`(engine 出力 `D'⊆C_D(Q̄)`) + `hsecFit`(**section-Fitting `C_{M_σ}(Q̄)⊆F(M)`**)。assembly =
    Q と D' がともに Q̄ を中心化 (hQab/hDcomm) ∧ ⊆M_σ ⟹ hsecFit で F(M) 入り。
    **🔑 設計判断: D' は Q でなく section Q̄ のみ中心化ゆえ、既存 full-centralizer helper
    `centralizer_inf_le_fittingInAmbient_of_le_Msigma`（C_M(Q)⊆M_σ→C_M(Q)⊆F(M)）では届かない** →
    section 形 `hsecFit` が honest gate（= mmd "Prop 1.5(d) yields F(M)=QC_M(Q)=C_{M_σ}(Q̄)"、
    σ-gap `C_M(Q)≤M_σ` + Prop 1.5(d) section 恒等式の束）。⚠ 既存 `derivedDerived_le_Msigma`(S15:578)
    は M''≤M_σ（弱い、別物）。`open scoped commutatorElement in` は docstring の**前**に置く（後ろは構文エラー）。
  - **▶ 残 conjunct (wrapper assembly 時に名指す forward inputs)**: conjunct 4/5 は既存 helper（σ-gap gated）、
    conjunct 2 `M_σ=M'` は §14/構造 gate の可能性大。**hsecFit の producer = §10/§12 σ-uniqueness（C_M(Q)≤M_σ）
    + Prop 1.5(d) section 恒等式** + hsigmaprime の producer = M_σ=QD semidirect 構造。assembly
    `mf_ne_msigma_typeP1_structure`(S15:1175 付近の sorry) は §14 `typeP_duality` gate ゆえ閉じない（触らない）。
- [x] **step 5 — `q∈β(M)` の Thm 5.5(a) gate ✅ LANDED** (sorry-free + axiom-clean
  `[propext, Classical.choice, Quot.sound]`, commit `d974cf0f`)。hard core `q∉β⟹D centralizes Q`
  を S15_MF に 3 lemma で形式化:
  - `commutator_eq_self_of_frobenius_DK`: `⁅D,K⁆=D` (Frobenius FPF ⟹ `C_D(K)=⊥`、
    `subgroup_coprime_decomposition` = Prop 1.6(d) で `D=C_D(K)⊔⁅D,K⁆` を collapse)。
  - `D_centralizes_Q_of_narrow`: 真の Thm 5.5(a) gate。`N=N_G(Q)` proper⟹solvable、
    `ψ=Q.normalizerMonoidHom`、`solvableAut_of_narrow` で `(N/ker)'` が q-群、`⁅D,K⁆=D⊆N'`
    の像が q-群 ∩ q'-群 = trivial ⟹ `D⊆C_G(Q)`。(Cor 10.9 `beta_complement_centralizes`
    の `P`→`D` 版 mirror。)
  - `D_centralizes_Q_of_not_mem_beta`: `mem_beta_of_inputs` 直挿し `hDcent` 形。narrowness を
    `q∉β` から `isNarrow_sylow_of_not_mem_beta` (de-private 化) 経由で derive (`Q=O_q(M)` は
    type-P1 で normal Sylow-q of M、witness `P:Sylow q ↥M` + `Q=P.map M.subtype` で供給)。
  **wrapper assembly が `Q=O_q(M)` の Sylow witness を供給すれば `q∈β` conjunct は unconditional。**
- [x] **step 1 derivation (a) — `isTypeP1_of_mf_ne_msigma`** ✅ LANDED (sorry-free + axiom-clean
  `[propext, Classical.choice, Quot.sound]`, commit `36d7a226`): `M_F ≠ M_σ` ⟹ `S14.IsTypeP1 M`.
  **Lem 14.1 / Thm 14.7 を §14 input でなく repo 既存 theorem に解決**: `¬IsTypeP2` =
  `msigma_isNilpotent_of_isTypeP2` (S14:7762) の contrapositive; `IsTypeP` = σ-complement `E≠⊥`
  (`SubgroupESetup.E_ne_bot` S12:1148) の prime `p∣|E|` を maximal-rank EA + Lemma 14.1
  (`msigma_structure_of_notMem_sigma_kappa`) で κ(M) に強制 (else M_σ nilpotent)。
  ⟹ wrapper conjunct 1 解禁、step-1(a) は **§14 gate でなく既証明**。
- [x] **step 1 derivation (b) — `kstar_card_prime_of_inputs`** ✅ LANDED (sorry-free + axiom-clean,
  commit `c24035cf`): type-P1 M ⟹ `(Nat.card ↥Kstar).Prime`。**Thm 14.7(f) を `typeP_duality` +
  `typeP_structure` (両 sorry-free) で解決**: duality (S14:8184) で partner M* 取得 →
  `IsTypeP2 M ∨ IsTypeP2 M*` で P1⟹¬P2 (`not_isTypeP1_and_isTypeP2`) ⟹ M* type-P2 →
  `typeP_structure` (S14:1665) の P2-conjunct `∃q, q.Prime ∧ Nat.card ↥Kstar = q`。
  ⟹ wrapper conjuncts 3/5 解禁。
- [ ] **wrapper 配線 (Stage 3) = BLOCKED on multiple genuine BG gates** (2026-06-19 RECON 完了,
  以下が正確な残ゲート; helper 群だけでは閉じない):
  - **GATE σ-1 `C_M(Q) ≤ M_σ`** (conjuncts 18 `F(M)≤M_σ` / 19 `F(M)=Q⊔C_M(Q)` を gate): **genuine
    σ-uniqueness fact, NOT locally derivable** — S15:4102-4109 docstring 明記 + ChatGPT-verified
    反例 `M=(C₇⋊C₃)×(C₃₁⋊C₅)`。BG §10 σ-uniqueness の未形式化拡張 (source or forward-input 要)。
  - **GATE σ-2 `hsecFit` = `C_{M_σ}(Q̄) ≤ F(M)`** (conjunct 17 `M''≤F(M)` を gate): section-Fitting,
    σ-uniqueness 系の未形式化 gate (S15:4254 `derivedDerived_le_fittingInAmbient_of_inputs` が
    named-hyp `hsecFit` で取る; producer 無し)。
  - **GATE frob `IsFrobeniusGroup ↥(D⊔K)`** (conjunct 15 `[Q:Q₀]=q^p` を
    `chiefFactor_card_and_commutator_of_inputs` S15:3814 経由で gate): Frobenius pair DK の構成は
    helper が input 仮定するのみ、producer 無し (§14 semidirect M_σ=Q⋊D + K coprime 作用から
    `IsFrobeniusGroup.of_centralizer_kernel_le` で構成可だが substantial assembly、未着手)。
  - **GATE β `q ∈ β(M)`** (conjunct 7、step 5): Thm 5.5(a) → `D centralizes Q` 矛盾の翻訳 hard core
    (`mem_beta_of_inputs` S15:4162 が `hDcent` named-hyp で取る; producer = step-5 未着手)。
  - **minor `p = |K|` prime** (conjunct 2/4): Thm 14.7(a)/Thm 3.10(a) — chief-factor engine の副産物
    or 別途。`q=|Kstar|` (Stage 2) と別 §14 fact、未配線。
  - **derivable だが未配線** (gate でない、Stage 3 で組める分): conjunct 16 `M_σ=M'`
    (`typeP_derivedInG_complement_of_eq_complement` S14:7980 + K=E cyclic via duality);
    `q∈σ(M)` (`Msigma_isPiGroup` S10:227); conjuncts 9-14 の Q/D/Q0 構成 (exists_aInvariant_hall
    S01:661 + step-2/3 chain helpers 全 sorry-free); conjunct 20
    (`not_isCyclic_MF_of_chiefFactor_inputs`, chief-factor data 経由)。
  - ⟹ **結論**: wrapper sorry は **σ-uniqueness (`C_M(Q)≤M_σ` + `hsecFit`) + Frobenius KD 構成 +
    step-5 (q∈β) + p=|K| prime** の 4 系統が landing するまで閉じない。これらは "helper 配線" の
    範囲外 (新規 BG 形式化)。Stage 1/2 (step-1 両 derivation) が helper-wiring で閉じる唯一の部分。
- [x] **GATE 再評価 (2026-06-19 final assembly セッション)** — 上記 4 系統のうち **3 つは解消**, 残 **2 つ**:
  - **✅ σ-1 `C_M(Q)≤M_σ` 解消**: `centralizer_le_Msigma_of_primeManner` (S15:4156, prime-manner
    から導出, sorry-free) が producer。conjunct 18/19 の入力 (`centralizer_inf_le_fittingInAmbient_of_le_Msigma`
    / `fittingInAmbient_eq_sup_centralizer_inf_of_le_Msigma`) を discharge。
  - **✅ frob `IsFrobeniusGroup ↥(D⊔K)` 解消**: `isFrobeniusGroup_DK_of_primeManner` (S15:4256,
    prime-manner から, sorry-free) が producer。chief-factor engine の `hfrob` 入力を discharge。
  - **✅ p=|K| prime 解消** (commit `360d6e79`): chief-factor engine
    `chiefFactor_card_and_commutator_of_inputs` を拡張し出力に `(Nat.card ↥K).Prime` を追加。
    同 Frobenius/module データから `prime_card_and_finrank_of_elemAbelian_general` (= Thm 3.10(a)) を
    呼び `|K̄|=|K|` prime を取得。sorry-free + axiom-clean `[propext, Classical.choice, Quot.sound]`。
  - **🛑 残 GATE σ-2 `hsecFit` = `C_{M_σ}(Q̄) ≤ F(M)`** (conjunct 17 `M''≤F(M)`): **真の未形式化 gate
    確定** (repo 全域 grep で producer 皆無)。section centralizer は full centralizer より弱く、
    `centralizer_le_Msigma_of_primeManner` (`C_M(Q)≤M_σ`) では届かない (検算: Q も D'=⁅D,D⁆ も section
    Q̄=Q/Q0 のみ中心化し Q 全体は中心化しないゆえ full-centralizer 補題が covers できない)。mmd の
    「Prop 1.5(d) yields F(M)=Q·C_M(Q)=C_{M_σ}(Q̄)」= 一般 coprime-action の section 恒等式
    `C_{M_σ}(Q̄)=Q·C_M(Q)` が必要だが repo 未形式化。**σ-uniqueness とは別物の §1 Prop 1.5(d) section 形**。
  - **🛑 残 GATE β `q ∈ β(M)`** (conjunct 7): `mem_beta_of_inputs` (S15:4380) が
    `hDcent : q∉β → D ≤ C(Q)` を named-hyp で取る。producer 候補 = `beta_complement_centralizes`
    (S10_BetaRadical:241, Cor 10.9, sorry-free) だが署名は **X が q-群** で `∃ Sylow p of M_σ, X ≤ C(S)`
    (p,q∈β', p≠q, X≤M' or p<q)。`hDcent` は **D が q'-群で Q=O_q を中心化**ゆえ役割が逆 + case 条件 +
    複数素数の β-排除が要 = **intricate, prompt が「force するな」と指定した hard core**。未着手。
  - **🚩 新発見: wrapper は UNDERSPECIFIED — `hKM : K ≤ M` を欠く**。`hK : IsHallSubgroup (kappa M)
    (K.subgroupOf M)` だけからは `K ≤ M` は導出不能 (`IsHallSubgroup π (K.subgroupOf M)` は `K⊓M` の
    述語で ambient 包含を含意しない; def = `Isaacs.Ch03.Main:580`)。`typeP_structure`/`typeP_duality`/
    `centralizer_le_Msigma_of_primeManner` 等 ほぼ全 helper が `K≤M` (or `IsTypeP`+`K≤M`) を要求。
    **両 caller は `K≤M` を持つ** (S15:4962 `K=K'.map M.subtype`≤M; S15:5111 `typeP_kstar_in_mf` は
    `hKM` 仮定) ゆえ wrapper への `hKM : K ≤ M` 追加は安全。**配線前にこの仮説追加が必須**。
  - ⟹ **wrapper を閉じるには**: (1) `hKM : K ≤ M` を signature に追加, (2) σ-2 `hsecFit` 形式化
    (§1 Prop 1.5(d) section 恒等式), (3) β `hDcent` 形式化 (Thm 5.5(a)/Cor 10.9 翻訳)。helper-wiring
    だけでは不可。`q∈β` は唯一 gate **でない** (`hsecFit` も並ぶ) ゆえ prompt の「q∈β sole なら sorry 据置」
    条件に該当せず — 但し 2 gate とも upstream BG 形式化で本セッション scope 外。**wrapper sorry 据置 +
    engine 拡張 (p=|K| prime) のみ landing**。

- [x] **2026-06-20 セッション: sub-tasks 1-3 + `hsecFit` 完遂、前 RECON の `hsecFit`-gate 判定を訂正、
  wrapper 残ゲート再精査**。すべて sorry-free + axiom-clean `[propext, Classical.choice, Quot.sound]`:
  - **✅ sub-task 1**: wrapper に `hKM : K ≤ M` 追加 (commit `2696378b`)。両 caller 修正
    (S15 caller=`Subgroup.map_subtype_le K'`; `typeP_kstar_in_mf` は既存 `hKM`)。
  - **✅ sub-task 3**: `le_maxNilpotentNormalHall` (commit `6d4e19d6`) — normal nilpotent Hall
    `N ≤ M` ⟹ `N ≤ M_F` (`le_sSup`)。conjunct 8 (`Q≤M_F`) の producer、conjunct 7 は `K*≤Q≤M_F`。
  - **✅✅ sub-task 2 (`hsecFit`) = 前 RECON の「真の未形式化 gate」判定を訂正・解消** (commit
    `60cbd938`)。`centralizer_msigma_quotient_le_fittingInAmbient` が `hsecFit` を discharge。
    **鍵**: `hsecFit` は `C_{M_σ}(Q̄)⊆F(M)` の **M_σ-section 版**で、σ-gate `C_M(Q)≤M_σ` は **不要**
    (それは *full* `C_M(Q̄)` 用)。ChatGPT-verified route (notes/bg/s15_2_step4_chatgpt_prompt.md step 2):
    `S=C_{M_σ}(Q̄)=Q⊔(D⊓S)` (complement 分解)、`D⊓S⊆C_G(Q)` (q′-元が Q̄ 中心化⟹Q 中心化、新補題
    `centralizes_Q_of_centralizes_quotient` = Prop 1.5(d)/1.6(d) via `subgroup_coprime_decomposition`)、
    ⟹ `⁅Q,D⊓S⁆=⊥` で `S` nilpotent + `S◁M` ⟹ `S⊆F(M)` (`nilpotent_normal_le_fitting`)。
    支持: generic `exists_mul_mem_of_isComplement_subgroupOf` (complement 積分解, `mem_sup` whnf-暴走
    回避に `IsComplement.mul_eq` 使用)。
  - **✅ wrapper front-matter 2 補題抽出** (commit `45299715`): `typeP1_msigma_eq_derivedInG`
    (conjunct 15 `M_σ=M'`, card 比較) + `actsPrimeManner_of_typeP` (prime-manner `C_G(x)⊓M_σ=K*`,
    `ActsPrimeOn` 展開)。assembly の front-matter は実証 (inline で sorry-free 確認後、revert して
    reusable 補題に抽出)。
  - **🛑 wrapper 本体 (`mf_ne_msigma_typeP1_structure`) は依然 sorry (134→134)**。残ゲートは
    `hsecFit`/`q∈β` だけでなく **chief-factor engine の C-subgroup interface が未 producer**:
    - **engine `chiefFactor_card_and_commutator_of_inputs` は `hCfix`/`hCcard` を named-hyp で取る**
      (`hCcard : (Q0.subgroupOf C).index = q` = `|C_{Q̄}(K)|=q`)。これは BG Thm 3.10(b) の出力
      `|Q̄|=q^p ∧ |C_{Q̄}(K)|=q` の specialization で、**producer が存在しない** (engine が内部で
      Thm 3.10(b) を呼ぶための *入力*)。`|C_{Q̄}(K)|=q` (K-fixed 類が位数 q ちょうど) の証明は
      非自明 (K* の Q̄ への像の構造論)。
    - **D-construction** (`exists_aInvariant_hall` で K-invariant q′-Hall complement を構成、`φ:↥K→MulAut ↥M_σ`
      setup 要) + **chief factor 構成** (`exists_chiefFactor_over_normalized`→`Q1_eq_Q_of_inputs`、
      ~20 hypothesis) + **G↔↥M 翻訳** (`kstar_le_opiCore_of_inputs` の `hcond2` は ↥M-level) の
      glue が大量。各 conjunct が substantial sub-proof。
    - **q∈β** (step 5): `D_centralizes_Q_of_not_mem_beta`+`mem_beta_of_inputs` は両 sorry-free 在
      (本 prompt の inventory 通り)。但し Sylow witness `P:Sylow q ↥M` (Q=O_q が normal Sylow-q) の
      供給 + `¬Q≤C(D)` (= `C_Q(D)=Q0⊊Q`) が要。
  - ⟹ **結論 (訂正)**: wrapper は「helper 配線 + 3 small sub-tasks」では閉じない。**全 math helper は
    landed (前 RECON が gate 認定した `hsecFit` も解消)** が、本体は **(a) engine の `hCcard`/`hCfix`
    producer (`|C_{Q̄}(K)|=q`, Thm 3.10(b) specialization, 未 land)、(b) D-construction + chief-factor +
    G↔↥M glue (~300-500 行)、(c) q∈β wiring** の full-proof assembly が残る。これは Thm 15.2 の
    proof body 全体の形式化に相当し、multi-session。本セッションは 4 helper + front-matter を landing。

- [x] **2026-06-20 セッション²: step (c)(d) + step (c) Sylow + step 3 D-construction の 3 大構造
  ブロックを landing** (全 sorry-free + axiom-clean `[propext, Classical.choice, Quot.sound]`;
  wrapper の前提 `Q=Sylow-q` / `D` complement が初めて構成可能に):
  - **✅ step (c)(d) `M_σ/Q` nilpotent** (commit `2708ea36`): `msigma_quotient_isNilpotent_of_inputs`
    — mmd L4192「K acts regularly on M_σ/Q → Thm 3.7 で M_σ/Q nilpotent」。支持 = 2 reusable 一般化:
    `isNilpotent_quotient_of_regular_general {N K1 Q0}` (= `isNilpotent_DQ1_quotient_of_regular` の
    N 任意化) + `fpf_of_centralizer_inf_le_general {A Q0 k}` (= `fpf_of_centralizer_inf_le` の A 任意化)。
    regular K-作用 = `C_{M_σ}(k)=K*⊆Q` (prime-manner + step-2)。
  - **✅ step (c) `Q=O_q(M)` is Sylow-q of M_σ** (commit `6a07c2e7`): `q_not_dvd_index_of_msigma_quotient_isNilpotent`
    — `¬ q ∣ [M_σ:Q]`。`M_σ/Q` nilpotent + Q=O_q maximal から、q-core `R̄=O_q(M_σ/Q)` の preimage R が
    q-群かつ `R.map M_σ.subtype ⊴ M` (q-core characteristic → M-conj-automorphism `QuotientGroup.congr`
    + `Subgroup.Characteristic.fixed` で保存) → R⊆O_q(M)=Q → R̄=⊥。支持 = `not_dvd_card_of_opCore_eq_bot`
    (nilpotent + trivial q-core ⟹ q'-群)。これが「D が complement」の根拠 (Q が full Sylow-q)。
  - **✅ step 3 D-construction** (commit `035199ff`): `exists_kInvariant_qComplement` — Prop 1.5(a)
    `exists_aInvariant_hall` で K-不変 {q}ᶜ-Hall D を構成、Q を complement
    (`IsComplement' (Q.subgroupOf M_σ) (D.subgroupOf M_σ)`, de-private 化した
    `OddOrder.BG.Ch1.S01.hall_compl_isComplement` 経由) + D nilpotent
    (`complement_isNilpotent_of_inputs`, prime-order K₁) + K≤N_G(D)
    (K-invariance `φ k • DM = DM` → `mem_normalizer_fintype`) + D≠⊥ (Q⊔D=M_σ≠Q)。
    支持 = `isHallSubgroup_subgroupOf_of_pgroup_of_not_dvd_index` ({q}-Hall fact)。
  - **wrapper setup も実証** (本セッションで inline 構築・verify したが sorry を含むため未コミット):
    `isTypeP1_of_mf_ne_msigma` / `kstar_card_prime_of_inputs` / `typeP1_msigma_eq_derivedInG` /
    `typeP_duality` から `M_σ=M'` + `K complements M_σ in M` (`Disjoint K M_σ` / `coprime |K| |M_σ|`) +
    `q∈σ(M)` + `K*⊆Q` (step-2、`hcond2` の ↥M prime-manner bridge を `centralizer_subgroupOf` で構築) +
    `Q≠M_σ` + `K≠⊥` (κ-prime 経由) まで sorry-free に組めることを確認。
  - **🛑 wrapper 残ゲート = conjunct 13 `M ≤ N_G(Q0)` (Q0=C_Q(D) が M-normal)** が真の deep step:
    **local data からは出ない** — M_σ=Q⋊D で `M_σ` は `C_Q(D)` を normalize しない (q'∈Q の共役は
    `C_{q'Dq'⁻¹}(...)` で D-共役、D は Q を normalize するが Q は D を normalize しない;
    `C_Q(D)⊴Q` も一般に偽)。mmd の「Q0=C_Q(D) ⊴ M」は **Q̄=Q/Q0 が elementary abelian chief factor に
    なった後の構造的帰結** (Q0 が M-作用の kernel 等と一致) で、chief-factor engine の出力に gate。
    ⟹ wrapper を閉じるには (a) conjunct 13 = Q0⊴M の deep 導出 (chief-factor 完了後) + (b) chief-factor
    engine の full wiring (Q1=Q + C-interface `hCcard`/`hCfix` の `card_centralizer_quotient_eq_of_kstar`
    instantiation + ~30 hypothesis discharge) + (c) q∈β。本セッションは「D まで」を landing、wrapper sorry 据置。

- [x] **2026-06-20 セッション³: chief-factor 構成 + elementary abelian の 2 大ピースを landing**
  (両 sorry-free + axiom-clean `[propext, Classical.choice, Quot.sound]`; 前 RECON の「conjunct 13 =
  Q0⊴M は chief-factor 出力に gate された deep step」判定を**訂正**):
  - **✅ `chiefFactor_Q0_normal_minimal_of_inputs`** (commit `38a14b51`, S15_MF): type-P1 data +
    `D` (complement) から `Q0 = C_Q(D)` の chief factor を構成。内部で `exists_minimal_normalOver` →
    `kstar_le_Q1_of_inputs` → `Q1_eq_Q_of_inputs` で `Q1 = Q` を得、出力 = **`M ≤ N_G(Q0)`** (conjunct
    13/14) + `¬ K* ≤ Q0` + `Q0 < Q` + **lattice-minimality** (`∀ H, Q0 < H → H ≤ Q →
    (H.subgroupOf M).Normal → Q ≤ H`)。
    **🔑 conjunct 13 (Q0⊴M) は deep gate でなく `Q1=Q` の直接帰結だった**: Q1 は `N_M(Q0)` の中で
    構成されるので `Q = Q1 ≤ N_G(Q0)`、+ `K,D ≤ N_G(Q0)` (Q0 の KD-invariance) + `M = M_σ ⊔ K`
    (`typeP_duality`, `M_σ=M'`) ⟹ `M ≤ N_G(Q0)`。前 RECON の「EA chief factor になった後」は不要。
  - **✅ `isElementaryAbelian_chiefFactor_of_minimalNormal`** (commit `a3043c54`, S15_MF):
    lattice-minimality + `Q ⊴ M`/`Q0 ⊴ M`/`Q0 < Q`/`Q` q-group ⟹
    **`IsElementaryAbelian q (↥Q ⧸ Q0.subgroupOf Q)` + `Nontrivial`** (= engine の `hEA`/`hNT`)。
    証明 = image `E = Q/Q0` が `M/Q0` (可解) の minimal normal (lattice-min を correspondence
    `comap (mk' Q0)` で transfer) → Isaacs Thm 3.11 (`solvable_minimal_normal_isElementaryAbelian`) で
    EA, prime は `quotientKerEquivRange` iso + q-群ゆえ `q`。
  - **⟹ chief-factor の全ピースが揃った**: 構成 (`chiefFactor_Q0`) + EA + engine
    (`chiefFactor_card_and_commutator_of_inputs`) + C-interface (`card_centralizer_quotient_eq_of_kstar`)
    + Frobenius (`isFrobeniusGroup_DK_of_primeManner`) + Fitting (`derivedDerived_le_…`,
    `fittingInAmbient_eq_…`) + `¬IsCyclic` (`not_isCyclic_MF_of_chiefFactor_inputs`) + q∈β
    (`mem_beta_of_inputs`) + step-2 (`kstar_le_opiCore_of_inputs`, `opiCoreInG_singleton_le_Msigma_of_mem_sigma`)
    がすべて landed。
  - **▶ 残 = wrapper `mf_ne_msigma_typeP1_structure` の最終 assembly のみ** (新規 math は 0、全部
    既 landed helper の threading): (i) Q=O_q(M)/D を構成 (`exists_kInvariant_qComplement`)、
    (ii) `chiefFactor_Q0` + EA + engine + C-interface + Frobenius + Fitting + q∈β を順に適用、
    (iii) 20 conjunct を組立。~200-250 行の threading。次セッションの focused 作業。

## 完了条件

`mf_ne_msigma_typeP1_structure` が sorry-free + axiom-clean。それまでは各 conditional helper が
sorry-free で landing し、残りが正確に §14 obligation (Lem 14.1 / Thm 14.7(f)) を名指す状態を維持。

## 参照

- issue 8011 (closed): BG Theorem 3.8 (step 2 の §3 gate)。
- `notes/bg/s15_16_audit.md` §14 (証明プラン正本)。
- mmd `references/bg/local-analysis.mmd` L4180-4202 (Thm 15.2 statement + proof)。
- `S03h.thm38` (`OddOrder/BG/Ch1_Preliminary/S03h_Thm38.lean:1012`)。
- Lem 6.3(a) = `OddOrder.BG.Ch1.S06.commutator_eq_self_of_isComplement'_le_commutator`。
