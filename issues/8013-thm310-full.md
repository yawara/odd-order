---
id: 8013
slug: thm310-full
title: "BG Theorem 3.10 full — conclusions (b)|M|=|C_M(R)|^p, (c), general kernel"
created: 2026-06-16
---

# BG Theorem 3.10 full — conclusions (b)|M|=|C_M(R)|^p, (c), general kernel

## 背景・動機

issue 8012 (Thm 15.2 body) step 4 の **(f) `|Q̄|=q^p`** + **(g) `D'⊆C_D(Q̄)`** が full BG
Theorem 3.10 に gate されると判明 (2026-06-16, Lane G; 正本 = `notes/bg/s15_16_audit.md` §14.3 訂正)。
**repo は Thm 3.10 の conclusion (a) `|R| prime` の abelian-kernel 形のみ**形式化済 (Prop 14.2(g)
=`S14:1555` 用)。ユーザー裁可 (2026-06-16): **G が full Thm 3.10 を形式化** (step 2 の Thm 3.8 =
issue 8011 と同型の §3 gate)。

## mmd Theorem 3.10 (L1267-1357)

`G=KR` solvable Frobenius (kernel `K`, complement `R`) が非自明冪零 `M` に作用、(1) `(|G|,|M|)=1`,
(2) `C_M(K)=1`, (3) prime-manner `C_M(x)=C_M(R)` ∀`x∈R^#`。**結論**:
- **(a)** `R` cyclic of prime order `p`;
- **(b)** `|M| = |C_M(R)|^p`;
- **(c)** `C_M(R)` cyclic ⟹ `K' ⊆ C_K(M)`。

§15.2 instantiation: Frobenius **`KD`** (kernel **`D`** [非 abelian 可], complement **`K`**) on
**`M=Q̄`** (elementary abelian, chief factor)。Thm-K↦D, Thm-R↦K, Thm-M↦Q̄ ⟹ (a)`p=|K|` prime /
(b)`|Q̄|=|C_{Q̄}(K)|^p=q^p` [C_{Q̄}(K)=K̄* 位数 q] / (c)`C_{Q̄}(K)` cyclic (位数 q prime ⟹ cyclic)
⟹ `D'⊆C_D(Q̄)`。

## 証明構造 (induction on |G|+|M|)

1. **R cyclic** (Prop 3.9, R が K に regular 作用)。✅ repo `isCyclic_of_isPGroup_of_isFrobeniusAction`。
2. **Case 1** (M に真の G-不変正規 M₀ あり): M₀ を maximal proper G-不変正規にとる。
   induction on (G,M₀) → (a)+(b)+(c) for M₀ (= 3.42)。KR が M/M₀ に作用、Prop 1.5(d) で C_{M/M₀}(K)=1
   + prime-manner → induction on (G,M/M₀) → (b)(c) for M/M₀。Prop 1.5(d) order 公式
   `|M/M₀|=|C_M(R)M₀/M₀|^p=|C_M(R)|^p/|C_{M₀}(R)|^p` + 3.42 → **(b)** `|M|=|C_M(R)|^p`。
   (c) = K'⊆C_K(M/M₀)∩C_K(M₀)⊆C_K(M) (Lemma 1.9)。
3. **Case 2** (M minimal normal = elementary abelian r-group, F_r 上 irreducible KR-module):
   (c) は (a)+**Thm 3.5** から。(a)(b) を示せば十分。
   - **K₀ 縮約**: K₀⊴KR minimal (1⊂K₀⊆K)。C_M(K₀) G-不変 ⟹ =1 (→ K₀R で induction) or =M (→ KR/K₀ で
     induction)。K₀≠K なら induction で (a)(b)。⟹ K minimal normal in KR ⟹ **K elementary abelian
     q-group (abelian!)**。
   - **abelian K**: KR faithful on V (Lemma 3.2)。Clifford/Wedderburn `V=⊕_{x∈R}(Wx)` (G Thm 3.4.3) →
     `dim C_V(R)=dim W=dim V/|R|` (3.43) → `|C_M(R)|^|R|=|M|` (3.44)。prime-manner で `|R|=p` (a)、
     `|C_M(R)|^p=|M|` (b)。

## repo の現状 (reuse map, 全精査済 2026-06-16)

**✅ 既存 (Thm310 三ファイル + FreeBlockPermutation)**:
- `isCyclic_of_isPGroup_of_isFrobeniusAction` (Prop 3.9, S03g_Thm310) — R cyclic。
- `prime_card_of_freeBlock_cond3` (S03g_Thm310Core) — free block + cond3 ⟹ `|R| prime`。**内部で
  `finrank_eq_card_mul_finrank_invariants_of_freeBlock` を `hR` として計算済 = (b) の次元版！**
- `Representation.finrank_eq_card_mul_finrank_invariants_of_freeBlock` (FreeBlockPermutation:350) —
  **`finrank V = |H| · finrank V^H`** (free block の orbit-counting; (b) の核心が既存)。
- `prime_card_of_abelian_frobenius_weight` (S03g_Thm310Module) — abelian K, weight, (a)。block 構成
  (ι=非自明 weight char, W=weight space, hfree=Frobenius, hperm) を内部に持つ。
- `prime_card_of_elemAbelian_mulDistrib` (S03g_Thm310Module) — elementary-abelian M, abelian K, (a);
  base-change to AlgebraicClosure(ZMod p)。
- `prime_card_complement_of_frobenius_conj` (S03g_Thm310) — Subgroup 共役形, abelian kernel U, (a);
  M を 1 つの minimal-normal M₀ に縮約 (Case 1 の片側)。S14:1555 が消費。
- **Thm 3.5** = `S03e.thm35` (main 在) — (c) 用。Thm 3.4 も。
- Prop 1.5(d) = `fixedPointsOfMulAut_quotientMulAutHom_eq_map` (S03h) / `coprime_fixedPoints_quotient`。
- `Module.card_eq_pow_finrank` (mathlib) — card↔finrank 変換。

**❌ 未形式化 (本 issue の対象)**:
- **(b) `|M|=|C_M(R)|^p`** — 全層 (weight / elemAbelian / 共役 / 一般 M)。core は finrank 公式 (既存) +
  card 変換だが ladder を通す要あり。
- **(c) `K'⊆C_K(M)`** — Thm 3.5 経由 (Case 2) + Case 1 の Lemma 1.9 合成。
- **一般 (非 abelian) kernel K** — Case 2 の K₀ 縮約 induction。
- **完全 Case 1 induction** (M/M₀ も含む; 既存 `prime_card_complement_of_frobenius_conj` は M₀ 片側のみ)。

## piece 分解 (実行順)

- [x] **piece 1: (b) weight-level finrank 公式** ✅ LANDED (S03g_Thm310Module, leaf build clean,
  full build 3838 jobs green): 既存 `prime_card_of_abelian_frobenius_weight` を richer 版
  **`prime_card_and_finrank_of_abelian_frobenius_weight`** にリファクタ — conclusion に (b)
  `finrank V = |R| · finrank V^R` を追加 (block 構造から `finrank_eq_card_mul_finrank_invariants_of_freeBlock`
  を取り出すだけ、(b) の orbit-counting は既存)。旧 `prime_card_of_abelian_frobenius_weight` は richer 版の
  **薄い projection** ((a) のみ) として温存 → 既存 elemAbelian/共役 ladder + S14:1555 (H 所有) 無変更
  (full propagation は S14 を壊すゆえ projection 方式が必須)。**(b) ladder の第一段確立。**
  - card 変換 `Nat.card M = (Nat.card C_M(R))^|R|` は piece 2 (elemAbelian 層、有限体 F_q 上) で。
    weight 層は alg-closed F̄ ゆえ finrank 公式止まり (card 無し)。
- [x] **piece 2a: (b) finrank 形を elemAbelian 層へ** ✅ LANDED (`prime_card_and_finrank_of_elemAbelian`,
  public, S03g_Thm310Module, sorry-free + axiom-clean, full build 3838 jobs green): richer aux を
  base-change で `finrank_{ZMod p}(Additive M) = |R| · finrank_{ZMod p}(invariants (ofDistribMulAction).comp R.subtype)`
  に。`refine`→`obtain` 化 (inline `(by ...)` で hCVK'/hcond3' 移植、型明示回避) + 移送
  (`Module.finrank_baseChange` LHS / `finrank_invariants_baseChangeRepresentation` RHS)。`(a)`-only
  projection `prime_card_of_elemAbelian_aux` で `mulDistrib` (S14 path) 無傷。
- [ ] **piece 2b: card 形** `Nat.card M = (Nat.card ↥C_M(R))^|R|`。`Module.card_eq_pow_finrank` (×2) で
  finrank→card。**要 C_M(R) ↔ invariants 同定**: 抽象 mulDistrib では module instance が `hM.zmodModule`
  (binder 非) ゆえ conclusion に `finrank (ZMod p)(Additive M)` を書けない (instance-in-conclusion 問題)。
  ⟹ card 形は **R-fixed subgroup `{m | ∀r∈R, r•m=m}` (module 非依存) の `Nat.card`** で述べ、
  `Nat.card (fixed subgroup) = Nat.card (invariants submodule)` (Additive bijection) を橋渡し。
  conjugation 層 (M=Subgroup, C_M(R)=M⊓C_G(R)) で具体化するのが最も自然。
- 旧設計メモ (piece 2 全体, 参考):
  - 移送補題既存: `Module.finrank_baseChange` (total V), `finrank_invariants_baseChangeRepresentation`
    (BaseChange:165, invariants; aux:256 で既使用), `Module.card_eq_pow_finrank` (card↔finrank, F=ZMod p)。
  - 手順: richer weight `prime_card_and_finrank_of_abelian_frobenius_weight ρ' ...` で F̄ 上
    `finrank V' = |R|·finrank C_{V'}(R)` → `Module.finrank_baseChange` で `=finrank_{ZMod p}(Additive M)`
    + `finrank_invariants_baseChange...` で invariants も ZMod p へ → `finrank_{ZMod p}(Additive M) =
    |R|·finrank_{ZMod p}(inv)` (= piece 2a, card 無し) → `card_eq_pow_finrank` で
    `Nat.card M = (Nat.card ↥inv)^|R|` (piece 2b)。
  - **refactor 注意**: `prime_card_of_elemAbelian_aux` (Module:155) は `refine
    prime_card_of_abelian_frobenius_weight ρ' ... ?_ hFrob ?_` で hCVK'/hcond3' を後置 bullet 証明
    (224-282, ~60行) している。richer 版にするには `refine`→`obtain` 化が要り、hole 証明を `have` へ
    移す (型明示が error-prone)。**piece 1 と同じ「richer + 薄い projection」パターン**で `mulDistrib`
    の (a)-only API を温存 (S14 path 無傷)。
  - **C_M(R) ↔ invariants**: 抽象 elemAbelian では C_M(R) が無名 → conclusion を `Nat.card ↥(invariants
    submodule of Additive M)` で述べるか、conjugation 層 (M=Subgroup, C_M(R)=M⊓C_G(R)) で具体化。
  - ⚠ **§15.2 には piece 2 単独で不足**: M=Q̄ は KD-module として可約な場合あり (Q̄ は M/Q₀ で
    minimal-normal だが KD では reducible 可) → piece 5 (Case-1 induction) で可約 M へ拡張要。
- [ ] **piece 3**: 一般 kernel K₀ 縮約 (Case 2 induction) — (a)(b) を非 abelian K へ。hard structural。
- [ ] **piece 4**: (c) base case (Thm 3.5 + (a))。
- [ ] **piece 5**: 完全 Case 1 induction (M/M₀ + Prop 1.5d order 公式) で (b)(c) を一般 M へ。
- [ ] **piece 6**: 最終 assembly `bgTheorem310` (general kernel, M elementary-abelian/nilpotent,
  (a)(b)(c))。§15.2 が要するのは M=Q̄ elementary-abelian, kernel D 一般。
- [ ] **wiring**: issue 8012 step 4 (f)/(g) を `bgTheorem310` instantiation で埋める。

## 完了条件

`bgTheorem310` (または §15.2 が要する elementary-abelian-M general-kernel 形) が sorry-free +
axiom-clean で landing し、issue 8012 step 4 の (f)`|Q̄|=q^p` + (g)`D'⊆C_D(Q̄)` を unblock。

## 参照

- mmd `references/bg/local-analysis.mmd` L1267-1357 (Thm 3.10 statement + proof)。
- issue 8012 (Thm 15.2 body) step-4 SCOPE 発見節。
- `notes/bg/s15_16_audit.md` §14.3 ledger 訂正。
- 既存 = `OddOrder/BG/Ch1_Preliminary/S03g_Thm310{,Module,Core}.lean` +
  `OddOrder/GroupTheory/RepresentationTheory/FreeBlockPermutation.lean`。
- issue 8011 (closed): Thm 3.8 (step 2 の §3 gate, 同型 precedent)。
