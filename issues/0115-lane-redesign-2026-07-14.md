---
id: 115
slug: lane-redesign-2026-07-14
title: "HUB: レーン再設計 2026-07-14 — c 再起動 (gate 再監査 4/5 workable) + OrderDetermination b→a 移管 + 4th lane 見送り"
created: 2026-07-14
---

# HUB: レーン再設計 2026-07-14 — c 再起動 (gate 再監査 4/5 workable) + OrderDetermination b→a 移管 + 4th lane 見送り

## 背景

<!-- なぜこの issue を立てたか. ROADMAP / notes / コミット等への参照. -->

## やること

- [ ]

## 完了条件

<!-- 何をもって closed とするか. 例: 該当 sorry が消える / lake build が通る / ノート x.md を書く -->

## 参照

<!-- 関連 issue / PR / ファイル / コミット. -->

## 背景

ユーザー発議 (2026-07-14 昼): 「c がずっと止まったまま。レーンを増やすか、設計を見直すか」。
hub 3 並列監査 (wf_525303b8: c-gate 再監査 / a 再配分候補 / Appendices on-path) に基づく裁定。

## 裁定 1: c 再起動 = GO (5 endpoint 中 4 が workable)

07-05 の「Verified c-unreachable」コメント群は **STALE** (07-12 の typeI_caseC_dichotomy /
typeI_caseC_dual_dichotomy landing + 本日の T-side landing で状況一変)。再起動 frontier は
独立 2 campaign:

- **Campaign A (ComparingLM 3 field)**: `m_row_odd` / `m_col_odd` を
  `S15.typeI_caseC_dichotomy` (S15_SAndT.lean:691) / `typeI_caseC_dual_dichotomy` (:747) の
  sorried-cite で配線 ((c1) 枝の refute 用 Coq (14.11.2) ub_u/ub_v 仮説は c-owned producer に追加)
  → `grid_mem` (Y=0) は leif assembly (Bessel `betaL_grid_coeff_bessel` :170 と ±1 rigidity
  engine は証明済・in place)。
- **Campaign B (SubgroupM:247 T-side field model)**: 本日 axiom-clean で開いた
  `isMulCommutative_V_unconditional` (TSideDegrees:910) + `card_Q_eq_qp` (:238) を使い、
  証明済 S-side Singer engine `exists_pu_field_repr` (SubgroupL.lean:628) の T-dual を構築 →
  `t_side_caseB_fieldModel` discharge。v-value は `lambda_forces_T_caseB_core`
  (S15_CharacterDegreeSupply:1086) の sorried-cite。
- ⚠ **statement 修正 (先にやる)**: SubgroupM:187 `hu_full` は無条件に u = (p^q−1)/(p−1) を
  述べるが、(13.15) は p ≡ 1 (mod q) 枝で (p^q−1)/(q(p−1)) — 枝仮説の追加 or 入力弱化が必要
  (監査で発見した statement-level mismatch)。
- operator: issue 0105 KEEP 裁定どおり codex 可 (現 codex は lane a 稼働中のため 2 セッション目
  or Claude)。**ユーザーの session 起動待ち**。

## 裁定 2: `S15_SAndT_Setup/OrderDetermination.lean` の所有を b→a に移管 (4 sorry)

- (13.11) `numeric_bounds` :358 = **即着手可** (analytic_inequality (13.10) 組立済、純算術)
- (13.12) `pc_le_maxNilpotentNormalHall` :501 = σ-gate が **9000 close (本日) で stale 化** — un-gated
- (13.13) `caseA_parameters` :610 / (13.15) `caseB_order_u` :774 = opaque-Prop statement の
  **de-opacify 要** (real certificate (CliffordCaseAData / (9.7.b) 系) へ restate + c の
  True-instantiate consumer 用 legacy wrapper 維持)。(13.15) は S16_CaseBOrder.lean の純算術
  engine `caseB_order_x_absurd_of_ge` の upstream 移設 or 再導出を伴う (a 裁量)
- 根拠: 4 本とも a が本日 close した 9000 typeP_Galois/(9.7) の材料を消費する = a が自然な owner。
  b の本日 active file 集合と非交差 (conflict low)。**b は 2035 T-side campaign に専念継続**。
- (13.15) landing は裁定 1 の c endpoint `hu_full` (SubgroupM:187) を un-gate する = a→c pipeline。

## 裁定 3: 4 レーン目増設は見送り (Appendices は off-path 確定)

Pf Appendices 15 sorry (FeitSibley/Huppert/NearFields/Suzuki/Suzuki2Groups) は全て
**Peterfalvi Part II (Suzuki 定理) の off-path scaffold**: (i) Part I (§1-14) 原文は Part II
付録を cite しない (mmd 全文 grep、cite は BG App.C のみ = 形式化済)、(ii) Coq odd-order は
PFsection1-14 のみで FT 完結、(iii) repo 内 code-level consumer 0、(iv) FT が必要とする唯一の
appendix 部品 `exists_field_semilinear` (App.I Prop 2) は**既に sorry-free で抽出済**。
15 中 14 は opaque-Prop scaffold で honest 化は Part II ~54 頁の研究級形式化 = 「3 冊網羅」の
別フェーズ。FT 経路限定政策の下で 4 レーン目の対象にならない。⟹ **c 再起動 + a 移管で
3 レーン飽和が正解**。将来 b の overload が続く場合の増設候補は「b クラスタの分割」のみ。

## 残 gate の恒久解 (別 issue)

NormEstimates 2 + CountingLayer 2 + SAndTBasic 1 の sorry は「honest producer が import DAG
下流」の layer-inversion 問題 (2035 #29) — レーンに配らず hub architecture task として
issue 0116 で追跡。

## 参照
- 監査 = workflow wf_525303b8 (3 agent、2026-07-14)。merge_monitor tick 41。
- issues/0105 (c operator KEEP)、2035 (b campaign)、9086/9087/9096 (本日の裁定群)、closed/9000

## ✅ lane-c 実施報告 (2026-07-14、Claude 再開 session): ⚠statement 修正 + Campaign A 完了

- **⚠ statement 修正 (commit 3450746f)**: `s_side_frobenius_kernel` の `hu_full` 無条件 u-value
  overclaim を除去。engine を `frobenius_PU_of_field_repr` (package 入力) に改修、named gate
  `s_side_field_repr` ((9.7.b) package、分岐非依存、Coq `typeP_Galois_P` 忠実) に移設。
  net sorry ±0。discharge = lane-a 9000/9097 pipeline。
- **Campaign A stage 2 (commit e357717d)**: `lSideGridCoeffData.m_row_odd`/`m_col_odd` を実証明
  (sorry −2)。`typeI_caseC_dichotomy`/`dual_dichotomy` を ζ₀ で適用、(c1) 枝は (14.11.2) 型
  strict gap `hub_u`/`hub_v` (producer 新規仮説、consumer の `hhv`/`hvu` から bridge 2 本で導出)
  で refute。dual 側は landed `T_isTypeP2` + `reconciled_typePData_T` 消費。
- **Campaign A stage 3 (commit 3fe31b7f)**: `grid_mem` field は **over-strong と判定し除去**
  (Coq `FTtype2_support_coherence` は `chi = τ₁φ ∨ chi = −τ₁φ*` の二択で終わり第一枝を pin
  しない — 3004 MHypothesis と同じ hoisted-conclusion 類; grid_mem は sorried だったので
  除去 = 純前進)。honest な (14.11.2) L-side 結論 `lSide_expansion_classification` を
  **実証明** — M-side と共有の generic engine (`etaGrid_projection_rigidity` = Parseval-defect
  Y=0 + ±1 rigidity / `_sub_beta_norm_one` / `_sub_beta_classification`) の L-instantiation。
  `lSide_signed_eta_expansion` は両 χ 枝 (distinguished ε=1 / `exists_conjIndex` 経由 conj
  ε=−1) で証明完了。**ComparingLM の local sorry は 0** (net −1)。
- 残 Campaign A 依存の上流 named obligations (別 file): (13.19.c) bound_c1/c2 系 (b 所有 S15)、
  `T_isTypeP2` の推移 gate (11.9/nzT1_Ga 系)。Campaign B (SubgroupM:247 T-side field model) が
  次 frontier — lane-a 9097 `exists_normOne_galoisField_conjugation_repr` adapter を消費予定。
