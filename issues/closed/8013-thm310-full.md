---
id: 8013
slug: thm310-full
title: "BG Theorem 3.10 full — conclusions (b)|M|=|C_M(R)|^p, (c), general kernel"
created: 2026-06-16
---

# BG Theorem 3.10 full — conclusions (b)|M|=|C_M(R)|^p, (c), general kernel

## 背景・動機

issue 8012 (Thm 15.2 body) step 4 の **(f) `|Q̄|=q^p`** + **(g) `D'⊆C_D(Q̄)`** が full BG
Theorem 3.10 に gate されると判明 (2026-06-16, Lane G; 正本 = `notes/bg/s15_16_audit.md` §14.3 訂正)。
**repo は Thm 3.10 の conclusion (a) `|R| prime` の abelian-kernel 形のみ**形式化済 (Prop 14.2(g)
=`S14:1555` 用)。ユーザー裁可 (2026-06-16): **G が full Thm 3.10 を形式化** (step 2 の Thm 3.8 =
issue 8011 と同型の §3 gate)。

## mmd Theorem 3.10 (L1267-1357)

`G=KR` solvable Frobenius (kernel `K`, complement `R`) が非自明冪零 `M` に作用、(1) `(|G|,|M|)=1`,
(2) `C_M(K)=1`, (3) prime-manner `C_M(x)=C_M(R)` ∀`x∈R^#`。**結論**:
- **(a)** `R` cyclic of prime order `p`;
- **(b)** `|M| = |C_M(R)|^p`;
- **(c)** `C_M(R)` cyclic ⟹ `K' ⊆ C_K(M)`。

§15.2 instantiation: Frobenius **`KD`** (kernel **`D`** [非 abelian 可], complement **`K`**) on
**`M=Q̄`** (elementary abelian, chief factor)。Thm-K↦D, Thm-R↦K, Thm-M↦Q̄ ⟹ (a)`p=|K|` prime /
(b)`|Q̄|=|C_{Q̄}(K)|^p=q^p` [C_{Q̄}(K)=K̄* 位数 q] / (c)`C_{Q̄}(K)` cyclic (位数 q prime ⟹ cyclic)
⟹ `D'⊆C_D(Q̄)`。

## 証明構造 (induction on |G|+|M|)

1. **R cyclic** (Prop 3.9, R が K に regular 作用)。✅ repo `isCyclic_of_isPGroup_of_isFrobeniusAction`。
2. **Case 1** (M に真の G-不変正規 M₀ あり): M₀ を maximal proper G-不変正規にとる。
   induction on (G,M₀) → (a)+(b)+(c) for M₀ (= 3.42)。KR が M/M₀ に作用、Prop 1.5(d) で C_{M/M₀}(K)=1
   + prime-manner → induction on (G,M/M₀) → (b)(c) for M/M₀。Prop 1.5(d) order 公式
   `|M/M₀|=|C_M(R)M₀/M₀|^p=|C_M(R)|^p/|C_{M₀}(R)|^p` + 3.42 → **(b)** `|M|=|C_M(R)|^p`。
   (c) = K'⊆C_K(M/M₀)∩C_K(M₀)⊆C_K(M) (Lemma 1.9)。
3. **Case 2** (M minimal normal = elementary abelian r-group, F_r 上 irreducible KR-module):
   (c) は (a)+**Thm 3.5** から。(a)(b) を示せば十分。
   - **K₀ 縮約**: K₀⊴KR minimal (1⊂K₀⊆K)。C_M(K₀) G-不変 ⟹ =1 (→ K₀R で induction) or =M (→ KR/K₀ で
     induction)。K₀≠K なら induction で (a)(b)。⟹ K minimal normal in KR ⟹ **K elementary abelian
     q-group (abelian!)**。
   - **abelian K**: KR faithful on V (Lemma 3.2)。Clifford/Wedderburn `V=⊕_{x∈R}(Wx)` (G Thm 3.4.3) →
     `dim C_V(R)=dim W=dim V/|R|` (3.43) → `|C_M(R)|^|R|=|M|` (3.44)。prime-manner で `|R|=p` (a)、
     `|C_M(R)|^p=|M|` (b)。

## repo の現状 (reuse map, 全精査済 2026-06-16)

**✅ 既存 (Thm310 三ファイル + FreeBlockPermutation)**:
- `isCyclic_of_isPGroup_of_isFrobeniusAction` (Prop 3.9, S03g_Thm310) — R cyclic。
- `prime_card_of_freeBlock_cond3` (S03g_Thm310Core) — free block + cond3 ⟹ `|R| prime`。**内部で
  `finrank_eq_card_mul_finrank_invariants_of_freeBlock` を `hR` として計算済 = (b) の次元版！**
- `Representation.finrank_eq_card_mul_finrank_invariants_of_freeBlock` (FreeBlockPermutation:350) —
  **`finrank V = |H| · finrank V^H`** (free block の orbit-counting; (b) の核心が既存)。
- `prime_card_of_abelian_frobenius_weight` (S03g_Thm310Module) — abelian K, weight, (a)。block 構成
  (ι=非自明 weight char, W=weight space, hfree=Frobenius, hperm) を内部に持つ。
- `prime_card_of_elemAbelian_mulDistrib` (S03g_Thm310Module) — elementary-abelian M, abelian K, (a);
  base-change to AlgebraicClosure(ZMod p)。
- `prime_card_complement_of_frobenius_conj` (S03g_Thm310) — Subgroup 共役形, abelian kernel U, (a);
  M を 1 つの minimal-normal M₀ に縮約 (Case 1 の片側)。S14:1555 が消費。
- **Thm 3.5** = `S03e.thm35` (main 在) — (c) 用。Thm 3.4 も。
- Prop 1.5(d) = `fixedPointsOfMulAut_quotientMulAutHom_eq_map` (S03h) / `coprime_fixedPoints_quotient`。
- `Module.card_eq_pow_finrank` (mathlib) — card↔finrank 変換。

**❌ 未形式化 (本 issue の対象)**:
- **(b) `|M|=|C_M(R)|^p`** — 全層 (weight / elemAbelian / 共役 / 一般 M)。core は finrank 公式 (既存) +
  card 変換だが ladder を通す要あり。
- **(c) `K'⊆C_K(M)`** — Thm 3.5 経由 (Case 2) + Case 1 の Lemma 1.9 合成。
- **一般 (非 abelian) kernel K** — Case 2 の K₀ 縮約 induction。
- **完全 Case 1 induction** (M/M₀ も含む; 既存 `prime_card_complement_of_frobenius_conj` は M₀ 片側のみ)。

## piece 分解 (実行順)

- [x] **piece 1: (b) weight-level finrank 公式** ✅ LANDED (S03g_Thm310Module, leaf build clean,
  full build 3838 jobs green): 既存 `prime_card_of_abelian_frobenius_weight` を richer 版
  **`prime_card_and_finrank_of_abelian_frobenius_weight`** にリファクタ — conclusion に (b)
  `finrank V = |R| · finrank V^R` を追加 (block 構造から `finrank_eq_card_mul_finrank_invariants_of_freeBlock`
  を取り出すだけ、(b) の orbit-counting は既存)。旧 `prime_card_of_abelian_frobenius_weight` は richer 版の
  **薄い projection** ((a) のみ) として温存 → 既存 elemAbelian/共役 ladder + S14:1555 (H 所有) 無変更
  (full propagation は S14 を壊すゆえ projection 方式が必須)。**(b) ladder の第一段確立。**
  - card 変換 `Nat.card M = (Nat.card C_M(R))^|R|` は piece 2 (elemAbelian 層、有限体 F_q 上) で。
    weight 層は alg-closed F̄ ゆえ finrank 公式止まり (card 無し)。
- [x] **piece 2a: (b) finrank 形を elemAbelian 層へ** ✅ LANDED (`prime_card_and_finrank_of_elemAbelian`,
  public, S03g_Thm310Module, sorry-free + axiom-clean, full build 3838 jobs green): richer aux を
  base-change で `finrank_{ZMod p}(Additive M) = |R| · finrank_{ZMod p}(invariants (ofDistribMulAction).comp R.subtype)`
  に。`refine`→`obtain` 化 (inline `(by ...)` で hCVK'/hcond3' 移植、型明示回避) + 移送
  (`Module.finrank_baseChange` LHS / `finrank_invariants_baseChangeRepresentation` RHS)。`(a)`-only
  projection `prime_card_of_elemAbelian_aux` で `mulDistrib` (S14 path) 無傷。
- [ ] **piece 2b: card 形** `Nat.card M = (Nat.card ↥C_M(R))^|R|`。`Module.card_eq_pow_finrank` (×2) で
  finrank→card。**要 C_M(R) ↔ invariants 同定**: 抽象 mulDistrib では module instance が `hM.zmodModule`
  (binder 非) ゆえ conclusion に `finrank (ZMod p)(Additive M)` を書けない (instance-in-conclusion 問題)。
  ⟹ card 形は **R-fixed subgroup `{m | ∀r∈R, r•m=m}` (module 非依存) の `Nat.card`** で述べ、
  `Nat.card (fixed subgroup) = Nat.card (invariants submodule)` (Additive bijection) を橋渡し。
  conjugation 層 (M=Subgroup, C_M(R)=M⊓C_G(R)) で具体化するのが最も自然。
- 旧設計メモ (piece 2 全体, 参考):
  - 移送補題既存: `Module.finrank_baseChange` (total V), `finrank_invariants_baseChangeRepresentation`
    (BaseChange:165, invariants; aux:256 で既使用), `Module.card_eq_pow_finrank` (card↔finrank, F=ZMod p)。
  - 手順: richer weight `prime_card_and_finrank_of_abelian_frobenius_weight ρ' ...` で F̄ 上
    `finrank V' = |R|·finrank C_{V'}(R)` → `Module.finrank_baseChange` で `=finrank_{ZMod p}(Additive M)`
    + `finrank_invariants_baseChange...` で invariants も ZMod p へ → `finrank_{ZMod p}(Additive M) =
    |R|·finrank_{ZMod p}(inv)` (= piece 2a, card 無し) → `card_eq_pow_finrank` で
    `Nat.card M = (Nat.card ↥inv)^|R|` (piece 2b)。
  - **refactor 注意**: `prime_card_of_elemAbelian_aux` (Module:155) は `refine
    prime_card_of_abelian_frobenius_weight ρ' ... ?_ hFrob ?_` で hCVK'/hcond3' を後置 bullet 証明
    (224-282, ~60行) している。richer 版にするには `refine`→`obtain` 化が要り、hole 証明を `have` へ
    移す (型明示が error-prone)。**piece 1 と同じ「richer + 薄い projection」パターン**で `mulDistrib`
    の (a)-only API を温存 (S14 path 無傷)。
  - **C_M(R) ↔ invariants**: 抽象 elemAbelian では C_M(R) が無名 → conclusion を `Nat.card ↥(invariants
    submodule of Additive M)` で述べるか、conjugation 層 (M=Subgroup, C_M(R)=M⊓C_G(R)) で具体化。
  - ⚠ **§15.2 には piece 2 単独で不足**: M=Q̄ は KD-module として可約な場合あり (Q̄ は M/Q₀ で
    minimal-normal だが KD では reducible 可) → piece 5 (Case-1 induction) で可約 M へ拡張要。
- [x] **piece 3: 一般 kernel K₀ 縮約 (Case 2 induction) — (a)(b) を非 abelian K へ。** ✅✅ **COMPLETE**
  (2026-06-16, Lane G `6cc5cebf`; sorry-free + axiom-clean, full build 3839 jobs green)。capstone =
  `prime_card_and_finrank_of_frobenius_general` (BG 3.10 (a)+(b), irreducible-module case, 一般
  kernel)、core = `frobenius_general_aux` (型多相 `Nat.strong_induction_on`)。新 leaf
  `S03g_Thm310General.lean` (root+AxiomsCheck wire 済)。**bottleneck 解除。**
  - **定式化 (確定)**: **fixed-G**（G=KR 全体, V irreducible G-module, kernel K ⊴ G general, R ≤ G,
    `[IsSolvable G]`, hFrob/hCVK/hcond3/hKcard）。`Nat.card K` 強帰納法 (型多相: Case B が群を G/K₀ に変える
    ⟹ motive は `∀ {G:Type u}[Group G]..., Nat.card K = n → concl` で全群量化、`Nat.strong_induction_on n`)。
  - **構造**: `obtain ⟨K₀, hK₀min, hK₀leK⟩ := exists_isMinimalNormal_le_of_normal K hKne` →
    `by_cases hK₀K : K₀ = K`:
    - **✅ base (K₀=K, K minimal normal) LANDED** (`prime_card_and_finrank_of_minimalNormal_kernel`,
      sorry-free + axiom-clean): `solvable_minimal_normal_isElementaryAbelian` ⟹ K elementary abelian
      ⟹ hKab (`hKea.comm` + `← Subgroup.coe_mul`) ⟹ `prime_card_and_finrank_of_abelian_frobenius_weight`
      で (a)+(b)。`[IsSolvable G]` 要。
    - **step (K₀⊊K)**: dichotomy `C_V(K₀)∈{⊥,⊤}`:
      - **✅ dichotomy LANDED** (`invariants_normal_eq_bot_or_top_of_isIrreducible`, sorry-free +
        axiom-clean): irreducible ρ + K₀⊴G ⟹ `invariants(ρ.comp K₀.subtype) ∈ {⊥,⊤}`。C_V(K₀) を
        G-invariant `Subrepresentation` に package (K₀ 正規 ⟹ ρ g が K₀-fixed を K₀-fixed に、共役戻し)
        → `IsSimpleOrder.eq_bot_or_eq_top`。tip: `Module.End.mul_apply` (not `LinearMap.mul_apply`),
        invariance は `ρ k₀(ρ g v)=ρ(k₀ g)v=ρ(g·g⁻¹k₀g)v=ρ g v` + `group`。
      - **Case A (C_V(K₀)=⊥)**: **同 G で kernel を K₀ に縮めて IH** (|K₀|<|K|)。clean — K₀⊴G, R FPF on
        K₀ (hFrob 制限), C_V(K₀)=⊥, prime-manner 不変, V irreducible 不変。**subgroup K₀R に降りる必要なし**
        (mmd は G→K₀R だが fixed-G で kernel 縮小のみで足りる、∵ 定理は G=kernel⊔R を要求しない)。
      - **Case B (C_V(K₀)=⊤)**: K₀ trivial 作用 ⟹ `ρ̄ = QuotientGroup.lift K₀ ρ`、IH を ρ̄ (kernel
        K/K₀) に適用 → 戻す。**Case B transfer brick 群すべて landing 済 (loop 2026-06-16)**:
        - **✅ B1 `invariants_lift_map_eq_of_trivial`** (`c74adbfb`): ρ̄ の `S·K₀/K₀`-invariants =
          ρ の S-invariants (ρ̄⟦g⟧=ρ g rfl + mk' 全射)。hCVK' (S=K) と結論移送 (S=R) 両用。
        - **✅ B2 `card_map_mk'_eq_of_disjoint`** + **`caseB_transfer`** (`e16b6536`): R∩K₀=⊥ で
          |R·K₀/K₀|=|R| → 商の (a)(b) を ρ へ戻す (card + invariants finrank 不変)。
        - **✅ B3 `isIrreducible_lift_of_trivial`** (`fef6425c`): ρ̄ 既約 (Subrep 同一 + IsSimpleOrder)。
          IH の `[IsIrreducible ρ̄]` を供給。
        - **✅ B4 `fpf_lift_of_centralizer_bot`** (`6d3cf41a`): C_K(r)=1 + coprime ⟹ r が K/K₀ に FPF
          (Prop 1.5d、S03h import)。hFrob' を供給。
  - **✅ B5 = 型多相 strong induction 本体 LANDED** (`6cc5cebf`, `frobenius_general_aux`): `∀ (n:ℕ)
    {G:Type*}[Group G]..., Nat.card K = n → concl` を `Nat.strong_induction_on n` (precedent =
    `frobeniusKernelIsNilpotent_aux` S03c_Thm37:595)。**設計上の改良**: Case A は IH 不要 — K₀ が
    minimal normal ゆえ `prime_card_and_finrank_of_minimalNormal_kernel` を K₀ で**直接適用**して底打ち
    (結論は R/V のみで kernel 非依存ゆえ妥当)。**recursion は Case B のみ**。Case B = hker (C_V(K₀)=⊤⟹
    ρ x=1) → lift 既約 (B3) + (K.map mk').Normal (`Normal.map`+`mk'_surjective`) + Finite quotient →
    lifted 仮説 (hRne'/hKmapne[K=K₀⟹⊥=⊤矛盾]/hKcard'[`card_map_dvd`]/hcop'[B2+dvd]/hCVK'[B1]/hFrob'[B4]/
    hcond3'[B1+`map_zpowers`]) → card 減少 (`quotientKerEquivRange` 第一同型 + ker `K₀≠⊥`⟹
    `card K = card range · card ker`, ker>1) → IH → `caseB_transfer`。`R⊓K₀=⊥` は hFrob から
    (`disjoint_def`+`group`)。一般-kernel 定理に `hcop : Coprime |R| |K|` 追加済 (B4 が使用)。
    💡 build 知見: `Representation` は abbrev ⟹ `.IsIrreducible` dot-notation は `MonoidHom` に解決 →
    `Representation.IsIrreducible (...)` と明記要; `mem_invariants` は ρ explicit; `one_lt_card_iff_ne_bot`
    は H 明示; `caseB_transfer` の未使用 implicit `{K}` は `(K := K)` で供給。
- [x] **piece 4**: (c) base case (Thm 3.5 + (a))。✅ `commutator_eq_one_of_frobenius_elemAbelian` に統合。
- [x] **piece 5**: 可約 M induction。✅ **COMPLETE** — Maschke subrep 帰納で (a)+(b)+(c) を可約
  elementary-abelian M へ (rep 形 `prime_card_and_finrank_of_frobenius_elemAbelian` +
  `commutator_eq_one_of_frobenius_elemAbelian`)。
- [x] **piece 6 (+2b)**: ✅ **COMPLETE** — elementary-abelian GROUP form (general kernel):
  `prime_card_and_finrank_of_elemAbelian_general` (a)+(b) finrank /
  `card_eq_pow_card_invariants_of_elemAbelian_general` (b) card `|M|=|C_M(R)|^|R|` (=§15.2 (f)) /
  `commutator_acts_trivially_of_elemAbelian_general` (c) `K'⊆C_K(M)` (=§15.2 (g))。
  base-change F_q→F̄_q (`invariants_baseChangeRepresentation_comp_eq` で subspace-hcond3 transfer)。
- [ ] **wiring**: issue 8012 step 4 (f)/(g) を上記 group form の instantiation で埋める (kernel=D,
  complement=K, M=Q̄)。⚠ 要入力に Prop 14.2(a) prime-manner 等 §14-gate が残る可能性 — wiring 着手時に
  assess。

**✅✅✅ 2026-06-17: BG Theorem 3.10 (a)+(b)+(c) [group + card + rep 形] ALL COMPLETE** (elementary-abelian
general-kernel, 全 sorry-free + axiom-clean, commits 6cc5cebf〜0f3e8a9d, S03g_Thm310{General,ElemAbelian})。
§3 gate 解消。残 = §15.2 wiring (S15_MF) のみ。

## 次の一手 (2026-06-17, piece 3 完了後 — 精査済み設計、execute-ready)

bottleneck だった piece 3 (一般 kernel (a)+(b), finrank 形, irreducible-module) は解除済。残 = §15.2
(f)/(g) への funnel。**mmd L1279-1357 全読 + Thm 3.5 repo 署名 + issue 8012 interface 精査済**。確定事項:

### 最終形 (issue 8012 step 4 = `IsFrobeniusGroup KD` on Q̄, line 237)
§15.2 は **`IsFrobeniusGroup G K R`** を供給 (abstract FPF でなく)。⟹ 最終定理 `bgTheorem310_elemAbelian`
は **IsFrobeniusGroup 形**で述べる (Thm 3.5 `thm35` も同形を要求)。piece 3 の abstract `hFrob`/`hcop` は
IsFrobeniusGroup から導出 (Frobenius ⟹ FPF on K + `Coprime |R| |K|`)。**hypothesis (3) は subspace 形**
`∀ x∈R^#, C_V(x)=C_V(R)` (submodule 等式) で持つ — piece 3 の finrank 形はここから `congrArg finrank`
で導出 (subspace 形は subrep/直和に制限できるが finrank 形は不可、これが piece 5 の要)。

### piece 5 = (a)+(b)+(c) を subrep 上の帰納で (CLEANEST ROUTE, 精査確定)
**型多相不要**: 固定 ρ の `Subrepresentation` W 上で `Nat.strong_induction_on (finrank ↥W.toSubmodule)`
帰納 (V, G, K, R 固定; W のみ動く)。infra = mathlib `Subrepresentation.toRepresentation`
(`RepresentationTheory/Subrepresentation.lean:54`)、`IsSemisimpleRepresentation` (Maschke,
`Semisimple.lean`)。
- **W irreducible**: piece 3 `prime_card_and_finrank_of_frobenius_general` を `W.toRepresentation` に適用
  ((a)+(b) finrank)。(c) = `thm35` (R prime [from (a)] + `C_W(R)` cyclic ⟹ 1-dim) ⟹ `K'⊆C_K(W)`。
- **W reducible**: char∤|G| ⟹ Maschke で `W = W₀ ⊕ W₁` (G-invariant subrep, proper nonzero)。IH を
  W₀/W₁ に。**combine**: `finrank W = finrank W₀ + finrank W₁`、`C_W(R)=C_{W₀}(R)⊕C_{W₁}(R)` ⟹
  `finrank C_W(R)=finrank C_{W₀}+finrank C_{W₁}` (直和、exactness 不要！) ⟹ (b)。(c): `C_W(R)` cyclic
  ⟹ `C_{W_i}(R)≤C_W(R)` も cyclic ⟹ IH で `K'⊆C_K(W_i)` ⟹ `K'⊆C_K(W₀)∩C_K(W₁)=C_K(W)` (Lemma 1.9 /
  直和)。**maximality 不要** (任意 proper nonzero G-invariant subrep で可)。
- 仮説制限: `C_{W_i}(K)⊆C_W(K)=⊥`; subspace-hcond3 は `C_{W_i}(x)=W_i∩C_W(x)=W_i∩C_W(R)=C_{W_i}(R)`;
  hcop/hKcard 不変。**全 subrep が固定 ρ の sub ゆえ型変化なし** (piece 3 と違い non-type-poly)。
- 要 bridging lemma: `W.toRepresentation` の invariants ↔ `W.toSubmodule ∩ C_V(R)`、直和の finrank/invariants。

### piece 2b/6/wiring
piece 5 が (a)+(b) finrank+(c) を与えた後: **2b** = elementary abelian で `|M|=r^{finrank}` ⟹
finrank 形 (b) から card 形 `|M|=|C_M(R)|^p` (`Module.card_eq_pow_finrank`)。**6** = IsFrobeniusGroup
wrapper `bgTheorem310_elemAbelian` 組立。**wiring** = issue 8012 step 4 (f)/(g)。
⚠ piece 5 は piece 3 級の労力 (Maschke + subrep 帰納 + (c) threading + invariants bridging); fresh
session 推奨。**(c) は piece 5 に統合**して帰納で運ぶ (旧 piece 4 単独でなく)。

## 完了条件

`bgTheorem310` (または §15.2 が要する elementary-abelian-M general-kernel 形) が sorry-free +
axiom-clean で landing し、issue 8012 step 4 の (f)`|Q̄|=q^p` + (g)`D'⊆C_D(Q̄)` を unblock。

## 参照

- mmd `references/bg/local-analysis.mmd` L1267-1357 (Thm 3.10 statement + proof)。
- issue 8012 (Thm 15.2 body) step-4 SCOPE 発見節。
- `notes/bg/s15_16_audit.md` §14.3 ledger 訂正。
- 既存 = `OddOrder/BG/Ch1_Preliminary/S03g_Thm310{,Module,Core}.lean` +
  `OddOrder/GroupTheory/RepresentationTheory/FreeBlockPermutation.lean`。
- issue 8011 (closed): Thm 3.8 (step 2 の §3 gate, 同型 precedent)。

## ✅ CLOSE (2026-07-02 hub 全体レビュー)

Thm 3.10 完成 + 15.2 配線済: S15_MF.lean の (f) 適用行 :5662 / (g) 適用行 :5666 で (b)/(c) を消費
(検証 2026-07-02)。
