---
id: 9038
slug: pow-closure-characteristic
title: "Public power-closure characteristic API"
created: 2026-07-06
---

# Public power-closure characteristic API

## 背景

BG §1 `S01_Solvable.lean` has a private `pow_closure_characteristic` helper:
the subgroup generated by all `n`-th powers is characteristic. Existing
`powMonoidHom_range_characteristic` covers only the commutative `powMonoidHom`
range case, so the arbitrary-group closure version should be public too.

## やること

- [x] Add public `Subgroup.pow_closure_characteristic`.
- [x] Build `OddOrder.Mathlib.Subgroup`.

## 完了条件

The lemma is sorry-free, the target leaf build passes, and this issue is moved to
`issues/closed/`.

## 参照

- `OddOrder/BG/Ch1_Preliminary/S01_Solvable.lean`
- `OddOrder/Mathlib/Subgroup.lean`


## 完了メモ

2026-07-06 lane d: added public arbitrary-group
`Subgroup.pow_closure_characteristic`. Verified by
`lake build OddOrder.Mathlib.Subgroup`.
