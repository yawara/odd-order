# BG §11 Exceptional Maximal — Lane bg-s11 handoff (2026-06-07)

Worktree `/home/ywr/odd-order-bg-s11`, branch `bg-s11`, `ODD_ISSUE_BASE=6000`.
Target: `OddOrder/BG/Ch3_MaximalSubgroups/S11_ExceptionalMaximal.lean`.

## Status of the 4 §11 sorries

| Result | Lean name | Status |
|---|---|---|
| Cor 11.4 | `eq_of_Msigma_meet_Hsigma` | **DONE** (commit `9879dd6`) |
| Thm 11.5 | `sylow_p_isCommutative` | blocked — Lemma 10.13(c) |
| Cor 11.6 | `omega1_eq_and_centralizer_trivial` | blocked — Thm 11.5 |
| Thm 11.7 | `MsigmaA_normal` | blocked — Cor 11.6 (+ Thm 4.20 WIP) |

Leaf `lake build OddOrder.BG.Ch3_MaximalSubgroups.S11_ExceptionalMaximal` is green
(3 remaining sorries at lines 816/824/830). Cor 11.4 introduces **no new sorry** at its
node; it still inherits the upstream §7/§10 gates transitively (so `#print axioms` shows
`sorryAx` until Thompson transitivity / Lemma 10.13 land).

## The single gate for the whole remaining cascade

**Lemma 10.13(c)** = third conjunct of
`OddOrder.BG.Ch3.S10.nonabelian_pSubgroup_rankTwo_elemAbelian_structure`
(`S10_LocalLemmas.lean:1063`, currently `sorry`):

```
∀ X Y, elemAbelianOfRankIn p 1 A X → X ≠ omega1CenterInG P p →
       elemAbelianOfRankIn p 1 A Y → Y ≠ omega1CenterInG P p →
         ∃ n ∈ Subgroup.normalizer (A : Set G) ⊓ P, MulAut.conj n • X = Y
```

(`N_P(A)` acts transitively by conjugation on `ℰ¹(A) − {Ω₁(Z(P))}`.)
This is §10 lane territory — **do not prove it here**; ff-sync `main` and start 11.5 once it lands.

## Cor 11.4 — proof shipped (reference)

`Msigma_isNilpotent` (Thm 11.3) ⟹ `M_σ` nilpotent; with `M_σ ⊓ H_σ ≠ ⊥`, contrapositive of
`S10.disjoint_of_not_conj` (Lem 10.12(b)) gives `∃ g, M^g = H`; `A ≤ H = M^g` and
`M_σ ⊓ M^g ⊇ M_σ ⊓ H_σ ≠ ⊥` feed the contrapositive of `Msigma_meet_conjugate` (Cor 11.2(a))
to force `g ∈ M`, whence `M^g = M = H` via `conj_smul_eq_self_of_mem_normalizer`.

## Thm 11.5 `sylow_p_isCommutative` — proof skeleton (BG p.78)

Goal: `(S : Sylow p ↥M) → IsMulCommutative (S : Subgroup ↥M)`.

BG argument (by contradiction, P not abelian):
1. `g ∈ N_G(P) − N_M(P)`, `q ∈ σ(M)`; `Q₁` = `P`-invariant Sylow `q` of `M_σ`, `Q₂ = Q₁^g`.
2. **Prop 1.16** ⟹ `Q₁ = ⟨ C_{Q₁}(X) | X ∈ ℰ¹(A) ⟩`, same for `Q₂`. So ∃ `X₁,X₂ ∈ ℰ¹(A)`
   with `C_{Q₁}(X₁) ≠ 1`, `C_{Q₂}(X₂) ≠ 1`.
   - Lean: `cocyclicFixedByClosure_eq_top_of_not_isCyclic` (`S01b_Prop116.lean`).
3. **Lemma 11.1(b)** (already proven, `invariant_sylow_disjoint` 2nd conjunct, this file):
   each `X ∈ ℰ¹(A)` has `C_{Q₁}(X)=1 ∨ C_{Q₂}(X)=1`. ⟹ `C_{Q₁}(X₂)=1`, so `X₂` not `P`-conjugate `X₁`.
4. **Lemma 10.13(c)** (THE GATE): all of `ℰ¹(A) − {X}` (`X=Ω₁(Z(P))`) are `P`-conjugate.
   ⟹ `X=X₁` or `X=X₂`. But `X=X^g` ⟹ `C_{Q₂}(X)=C_{Q₁}(X)^g`, so `C_{Q₁}(X)=C_{Q₂}(X)=1`,
   contradicting step 2.

**Known subtleties (resolve when implementing):**
- `Hypothesis111` stores only `IsPGroup p ↥P` + `P ≤ M` + `¬ N_G(P) ≤ M`, **not** literally
  `P ∈ Syl_p(M)`. The statement quantifies over an arbitrary `S : Sylow p ↥M`. Plan: prove `P`
  abelian, then transfer to `S` by Sylow conjugacy (`MulAut.conj`-invariance of abelianness),
  OR first establish `P` is a Sylow of `M` from the Lem-10.5 derivation data. Confirm which
  invariants are available before committing the statement shape.
- Need a constructor for "`P`-invariant Sylow `q` of `M_σ`" — `exists_isAInvSylowIn` (this file)
  gives `A`-invariant; for `P`-invariant use `P ≤ N_G(M_σ)` (P ≤ M ≤ N_G(M_σ)) and the same
  Cor-3.25 engine, or adapt `IsAInvSylowIn` to `P` in place of `A`.

### Deep-dive (2026-06-07 session 2): full 11.5 proof design + resolved subtleties

Worked out end-to-end; the **only** genuine gate is Lemma 10.13(c). Recorded so the
post-10.13 fill-in is mechanical.

**(i) P-as-Sylow-of-M — RESOLVED (but cross-lane).** `Hypothesis111` has **zero construction
sites** in the repo (grep `Hypothesis111\b` outside S11 = empty; §12 imports the spine but does
not build the structure). So a `P_isSylow : <P is a Sylow p of M>` field can be added to
`Hypothesis111` **build-safely right now**. BG's Lemma-10.5 setup ("`A ⊆ P ∈ Syl_p(M)`", already
in the docstring) guarantees it, so the field is faithful — it fixes an omission, not a
theorem-hiding shortcut. CAVEAT: the proof obligation then falls on the eventual §10/§12
constructor (another lane). Do **not** add it unilaterally from bg-s11 — flag it for the main
hub / §10 owner so the constructor side is wired at the same time. With the field, the arbitrary
`S : Sylow p ↥M` reduces to `P` by Sylow conjugacy in `↥M` (`Sylow.exists_smul_eq` / conj
transports `IsMulCommutative`).

**(ii) Prop 1.16 → ℰ¹(A) extraction (step 2) — mechanism.** `cocyclicFixedByClosure φ` (def in
`S01b_Prop116.lean`) `= closure {g | ∃ Y, (∃ a, Y ⊔ zpowers a = ⊤) ∧ ∀ y∈Y, φ y g = g}`. Apply
`cocyclicFixedByClosure_eq_top_of_not_isCyclic` to `φ = conjugation ↥A →* MulAut ↥Q₁`
(coprime: `|A|=p²` vs `q`-group `Q₁`, `q≠p`; `¬IsCyclic ↥A` since `A≅(ℤ/p)²`). Gives
`closure(gens) = ⊤`. Contrapositive: if `∀ X∈ℰ¹(A), C_{Q₁}(X)=1`, then for **every** cyclic-quotient
`Y` the fixed set is trivial — because for `A≅(ℤ/p)²` the subgroups `Y` with `A/Y` cyclic are
exactly the `p+1` order-`p` subgroups (`∈ ℰ¹(A)`) **and** `Y=A` (giving `C_{Q₁}(A) ≤ C_{Q₁}(X)=1`);
`Y=1` is excluded (`A/1=A` noncyclic). So `gens ⊆ {1}`, `closure = ⊥ = ⊤`, forcing `Q₁=1` —
contradicting `Q₁≠1` (Sylow `q` of `M_σ`, `q∈σ(M)` so `q ∣ |M_σ|`, `isAInvSylowIn_card_dvd`).
Hence `∃ X₁∈ℰ¹(A), C_{Q₁}(X₁)≠1`. Plumbing cost: bridge subtype `actionFixedPoints φ Y` (⊆`↥Q₁`)
↔ `Subgroup.centralizer (X:Set G) ⊓ Q₁` (⊆`G`); and the rank-2 `Y`-classification.

**(iii) Centralizer–conjugation transfer (steps 5,7) — needed helper.** Universal lemma:
`MulAut.conj n • Subgroup.centralizer (X:Set G) = Subgroup.centralizer ((conj n • X):Set G)`
(proof: `mulAut_smul_eq_map` both sides, then `ext`/`mem_centralizer_iff`; fwd by `map_mul` on
`conj n`, bwd witness `(conj n)⁻¹ g`). Not in Mathlib (no `smul_centralizer`). With `n ∈ N_G(Q)`
(`conj n • Q = Q`) it gives `C_Q(X)^n = C_Q(X^n)`. Used for: step 5 `X₂` not `P`-conjugate `X₁`
(if `X₂=X₁^n`, `n∈P` normalizes the `P`-invariant `Q₁`, so `C_{Q₁}(X₂)=C_{Q₁}(X₁)^n≠1`,
contra `=1`); step 7 `X₀=Ω₁(Z(P))` is `g`-fixed (`g∈N_G(P)` fixes `Z(P)`,`Ω₁`) so
`C_{Q₂}(X₀)=C_{Q₁}(X₀)^g`. (Do NOT commit this helper standalone — unused ⇒ lint orphan until
the 11.5 body exists.)

**(iv) Assembly order when 10.13(c) lands:** (a) add `P_isSylow` field + reduce `S`→`P`
[coordinate cross-lane]; (b) prove `P`-abelian core: build `Q₁` (`exists_isAInvSylowIn` with `P`),
`Q₂=Q₁^g`; extraction (ii) for `X₁,X₂`; `invariant_sylow_disjoint.2` for the dichotomy; (iii) for
the conjugacy transfers; `nonabelian_pSubgroup_rankTwo_elemAbelian_structure.2.2` (10.13(c)) for
`ℰ¹(A)−{X₀}` transitivity; close by contradiction. Q₁≠1 via `isAInvSylowIn_card_dvd`.

## Cor 11.6 `omega1_eq_and_centralizer_trivial` — skeleton (BG p.78)

Lean goal: `A = (Omega ↥P p 1).map P.subtype ∧ C_G(A) ⊓ M_σ = ⊥` (parts (a),(b); (c) deferred).
- (a) `A=Ω₁(P)`: `A ∈ ℰ²(P)` and **P abelian of rank two** (Thm 11.5) ⟹ `A=Ω₁(P) ⊴ N_G(P)`.
  This is why 11.6 waits on 11.5.
- (c)/(b): `N_G(P) ⊄ M` + odd order ⟹ `|N_G(P):N_M(P)| ≥ 3`; pick `g₁,g₂ ∈ N_G(P)−N_M(P)`,
  `g₁g₂⁻¹ ∉ M` ⟹ `∉ N_G(A₀)` ⟹ `A₀^{g₁} ≠ A₀^{g₂}`. `A₀ ⊆ A ⊴ N_G(P)` ⟹ `A₁,A₂ ≤ A`.
  **Cor 11.2(b)** (`Msigma_meet_conjugate` 2nd conjunct): `C_{M_σ}(A₁)=C_{M_σ}(A₂)=1`.
  (b) then `C_{M_σ}(A) ≤ C_{M_σ}(A₁) = 1`.
  - For each `gᵢ`: `gᵢ ∈ N_G(P)` ⟹ `gᵢ ∉ N_M(P) ⟺ gᵢ ∉ M`; and `A ≤ P = P^{gᵢ} ≤ M^{gᵢ}`.
- NB: to expose `A₁,A₂` (needed by 11.7 Cor 11.6(c)) the Lean statement may need extending
  beyond (a)+(b) — 11.7 consumes `A = A₁ × A₂`.

## Thm 11.7 `MsigmaA_normal` — skeleton (BG p.78–79), heavy/last

Goal: `M ≤ N_G(M_σ ⊔ A)`. By contradiction. Complement `E` to `M_σ` in `M` with `A ≤ E`,
`r(E)=2`; `τ={q∈π(E)|q>p}`, `K=O_τ(E)`.
- **Thm 4.20** (`S04g_Thm418.lean`, WIP): `K` Hall-`τ` of `E`, `KP` normal Hall-`(τ∪{p})`.
- Cor 11.6(a) `A=Ω₁(P)`. If `[A,K]=1`: `KA=K×A`, `A=Ω₁(O_p(KP))⊴E`, `M_σA⊴M`, contra.
- Else: `q ∣ |K:C_K(A)|`, `Q` = `A`-inv Sylow `q` of `K`, `[A,Q]≠1`.
- `C_Q(A)=1`: else `r(Q)=2`, `B∈ℰ²(Q)`; **Lemma 10.4(c)** (`S10.alpha_criterion`) ⟹
  `A∈ℰ_p*(G)`, `B∈ℰ_q*(G)`; `q∈π(C_G(A))`, `Q⊆Q*∈ℋ_G*(A;q)`; **Prop 10.10(c)**
  (`S10.normalizer_factorization`) ⟹ `[A,Q*]=1`, contra `[A,Q]≠1`.
- `Q₀=Z(Q)`; **Prop 1.6(d)** (`S01_Solvable.lean`) ⟹ `Q₀=[A,Q₀]`. With `A=A₁×A₂` (Cor 11.6(c)):
  `Q₀=[A₁,Q₀][A₂,Q₀]`; **Prop 10.11(d)** (`S10.sigma_complement_commutator_cyclic_normal`) ⟹
  both `⊴ M`. So `Q₀⊴M`, `N_G(Q)⊆N_G(Q₀)=M`; but `Q` Sylow-`q` of `M`, `q∉σ(M)` — contra.

All §10/§1/§4 deps above exist as **statements** (several still `sorry`): existence of `E`
(complement, Schur–Zassenhaus-style) is the one piece with no explicit citation — check
`OddOrder/GroupTheory` for a complement constructor before starting.

## Lane discipline reminders
- Touch only `S11_ExceptionalMaximal.lean` (+ import §10/§1/§4 results). Do **not** edit
  Peterfalvi/S08/S15, S10_BetaRadical/HallStructure, GroupTheory/RepresentationTheory.
- Per result: leaf build green → commit (`Pf (BG 11.x): …` + Co-Authored-By trailer).
- `git merge --ff-only main` when clean to pick up §10 (esp. Lemma 10.13) landings.
