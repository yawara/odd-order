# 引き継ぎ — BG §9 + L2 (Thm 4.20c) — 2026-06-05

## TL;DR

このセッションで **BG §9 Theorem 9.1 を closon**、**S09 を mathlib 基準で4分割**、**L2 (Thm 4.20(c) 存在) に着手**した。
§9 の残り実 sorry は **1 個 = Lemma 9.5**（`S09_Lemma95.lean:2407`）で、その gate が **L2**（`S04g_Thm418.lean:1915`）。
L2 は **statement anchor + 正しい construction 設計 + 強帰納 skeleton まで build-green**、残りは**帰納 step の実装**のみ。

- tree clean、origin から **58 commits ahead（未 push、local-first）**、full build green (3580 jobs)。
- critical path: §9 → S16.Hypothesis → BG.AppC → final_contradiction（正本 = memory `ft-master-roadmap`）。

---

## このセッションの commits（13、新しい順）

| commit | 内容 |
|---|---|
| `9b7de18` | **L2** docstring を正しい construction (Thm 4.18b route) に訂正 + 強帰納 skeleton |
| `35096a7` | **L2** statement anchor `exists_characteristicSylowSeriesPackage_of_rank_fitting_le_two`（初版 docstring は pRank 誤り） |
| `0c750b8` | CLAUDE.md: minimal-import の速度主張を訂正（shared-closure caveat + shake 不信） |
| `f434885` | CLAUDE.md: mathlib ファイル粒度基準を採用（1 file=1 topic） |
| `cc1864c` | **S09 を mathlib 4分割**（Theorem91/Corollaries/Lemma95/hub） |
| `94ee09c` | **BG §9 Thm 9.1 closon**（本体アセンブリ、型エラー2修正） |
| `209ee21`〜`9b99ccb` (5) | §9 Thm 9.1 の STEP0–STEP7 helper（背景 batch workflow の checkpoint、後に foreground で仕上げ） |
| `f851e05` | **L1 一般 Hall-Higman**（Isaacs 3.21）`centralizer_oPiPrimePiCore_le` |
| `975e4da` | §9 frontier audit（L1/L2 上流 blocker 記録 + issue 0059） |

---

## 🔨 最優先の残作業: L2 = `exists_characteristicSylowSeriesPackage_of_rank_fitting_le_two`

**場所**: `OddOrder/BG/Ch1_Preliminary/S04g_Thm418.lean`（`end Thm418` の直前、現在 sorry @ 1915）
**namespace**: `OddOrder.BG.Ch1.S04`（opens: GroupTheory, Isaacs, scoped commutatorElement）

```lean
theorem exists_characteristicSylowSeriesPackage_of_rank_fitting_le_two
    [IsSolvable G] [Nontrivial G] (hodd : Odd (Nat.card G))
    (hrank : rank ↥(Ch01.fitting G) ≤ 2) :
    Nonempty (CharacteristicSylowSeriesPackage G)
```

**現状**: `suffices H : ∀ n, ∀ (G : Type _) [Group G] [Finite G] [IsSolvable G] [Nontrivial G],
Nat.card G = n → Odd … → rank … ≤ 2 → Nonempty …` + `Nat.strong_induction_on` の枠まで build-green。
帰納 step（`intro G _ _ _ _ hcard hodd' hrank'` の後）が `sorry`。

### 正しい construction（BG 実証明 mmd L1786、docstring に記録済）

> ⚠️ **初版の pRank/構造補題 plan は誤り**（`pRank G p ≤ r(F(G))` は一般に偽 — Fitting 外に大きな
> elementary abelian があり得る）。`solvable_structure_of_pRank_le_two` は **使わない**。

|G| 強帰納で:
1. `F := F(G)`。**Thm 4.20(a)** `derived_le_fitting_of_centralizer_rank_le_two`（`S05_NarrowPGroups.lean:3412`）で `G' ≤ F` ⟹ `G/F` abelian。
2. **最小素数** `p₁`（`(Nat.card G).primeFactors` の min）。`H` を `F ≤ H` かつ `H/F = O_{p₁'}(G/F)` で取る（comap of `Ch03.oPiCore {r≠p₁} (G/F)` along `QuotientGroup.mk' F`）。
3. `F` は `H` の Sylow-p₁ を含む（H/F は p₁'）。`G/H`（abelian `G/F` の商）は **p₁-群**。
4. **Thm 4.18(b)**（`S04g_Thm418.lean:866` `BG Theorem 4.18`、完成済）で `H` は正規 p₁-complement を持つ。
   `G/H` が p₁-群なので `hasNormalPComplement_of_…_quotient_isPGroup`（`S04g:1165` 系、要 signature 確認）で
   **`Ch05.HasNormalPComplement p₁ G`**。complement は `K = O_{p₁'}(G)`（`normalPComplement_eq_oPiCore_compl` `S04g:988`）。
5. **`F(K) ⊆ F(G)`**（`fitting_map_subtype_le_fitting` `Isaacs/Ch01_Sylow/Main.lean:1119`、`[K.Normal]`）⟹ `r(F(K)) ≤ r(F(G)) ≤ 2`。
   `Nat.card K < Nat.card G`（G/K ≅ Sylow-p₁ ≠ 1）で **IH を K に適用**。
6. `IH` の series を **`lift_oPiCore_series_of_hasNormalPComplement_ne`**（`S04g:1837`）でリフト（hpq: 各 step.q ≠ p₁ は
   K=O_{p₁'} ゆえ自明）。`length_pos`（length+1>0）と `terminal_mem`（p₁ ∈ π(G)）を package に。
   - **base case**: `O_{p₁'}(G) = ⊥`（G が p₁-群）→ IH 不可。`consTop` + trivial top layer
     （`CharacteristicSylowLayer.top_of_hasNormalPComplement`）で length-1 series を直接構成。

**pRank 障害なし** — rank 仮説は `G'≤F` と `F(K)⊆F(G)` の直接包含のみで流れる。全 hook は repo に存在確認済み。

### 実装の罠（予測）

- 強induction の universe: `∀ (G : Type _)` で OK（実証済み、一発で通った）。
- `H` 構成は `Subgroup.comap (QuotientGroup.mk' F) (Ch03.oPiCore {r≠p₁} (G⧸F))` 系の quotient/comap 操作で intricate。
- Thm 4.18(b) の正確な signature と仮説（`r_p(G)≤2` を要求するか？要確認。要求するなら 4.18 適用に別 bridge が要る — ただし
  docstring `S04g:985` が「4.20(c) で Thm4.18(b) を使う」と明言しているので適用可能なはず。**Thm 4.18 の statement を必ず精読**）。
- `CharacteristicSylowSeriesPackage` の `terminal_mem` field（terminal step の q ∈ π(G)）の bookkeeping。

---

## L2 完成後: Lemma 9.5 への配線

**消費者**（`S09_Lemma95.lean`、`SP : OddOrder.BG.Ch1.S04.CharacteristicSylowSeriesPackage ↥M`/`↥L` を仮説に取る 6 箇所）:
`exists_pSubgroup_normalizer_package_of_not_scn3_of_sylowSeriesPackage` (833) ほか L508/570/632/838/1235/1301。

放電: M（および L）は **minimal simple odd G の maximal subgroup ゆえ solvable**（`hG.solvable_of_mem_maximalSubgroups`）、
かつ §9.5 の場合分けで **r(F(M)) ≤ 2**（Thm 8.1 contrapositive 経由）。`Nontrivial ↥M` は proper maximal ゆえ自明。
これで `Nonempty (CharacteristicSylowSeriesPackage ↥M)` を得て `.some` で SP を供給。
→ 最後に `scn3_isUniquelyMaximal`（`S09_Lemma95.lean:2407`）の本体アセンブリ（~40 helper は既証明、Thm 9.1 は済）。

---

## S09 の新構造（mathlib 4分割、commit cc1864c）

import 鎖 `Theorem91 ← Corollaries ← Lemma95 ← Uniqueness(hub)`、下流は `import …S09_Uniqueness` で不変。

| ファイル | 行 | 内容 |
|---|---|---|
| `S09_Theorem91.lean` | 966 | Thm 9.1 `noncyclic_isUniquelyMaximal_of_centralizer_le` + STEP/contrapositive/共通 helper |
| `S09_Corollaries.lean` | 887 | Cor 9.2/9.3 + Lem 9.4 + scn3Global 族 |
| **`S09_Lemma95.lean`** | 2544 | **Lemma 9.5（active、sorry @ 2407）** + helper chain |
| `S09_Uniqueness.lean` | 139 | Thm 9.6 `uniquenessTheorem` capstone + 3つを束ねる hub |

cross-file helper（scn3Global 族・centralizer_singleton_*・rank bridge・contrapositive）は un-private 化済。

---

## 運用上の学び（次セッション向け）

- **背景 workflow は user 割り込みで止まる**（subagent に `[Request interrupted]` 伝播）。→ **commit checkpoint 方式**で進捗保全、`resumeFromRunId` で復旧。重い proof は workflow が不利、**ほぼ完成の仕上げは foreground 逐次が速い**（Thm 9.1 は WIP の型エラー2個修正で closon）。memory `workflow-stops-on-user-interrupt`。
- **ビルド速度**: 律速は import closure 固定費(~5s)、行数差は小。**開発は leaf build で回し full は commit 直前のみ**。S09 分割の leaf 高速化は 5.5→4.7s と控えめ。
- **minimal-import は本 repo の spine では速度価値ゼロ**（深い共有 closure ゆえ推移閉包不変）。`lake exe shake` は**誤判定多発**（実使用 import を removable と誤報）→ 鵜呑み禁止、build/grep で検証。memory + CLAUDE.md 0c750b8。
- **construction は推測せず教科書の実証明を読む**（L2 で pRank 版を誤って commit 後 mmd L1786 で訂正）。

---

## 正本ポインタ

- 全体現在地・critical path = memory `ft-master-roadmap`（2026-06-05 エントリに L1/Thm9.1/分割/L2 を記録）
- §9 全体 = `notes/bg/s09_uniqueness.md`（「🔴 2026-06-05 BLOCKER UPDATE」）
- issue 0059（L1）= closed。L2 は issue 未発行（必要なら `bin/new-issue`）。
