# BG Theorem 3.4 — scaffold 設計と進捗 (Lane A `a-keystone`)

**スコープ**: BG, Chapter I §3, mmd `references/bg/local-analysis.mmd` **L863-901**.
正本の上流計画は `s03_keystone_plan.md` 末尾「Thm 3.4 セッション」(GAP 1/2 決定 + ingredient inventory);
本ファイルは **scaffold 確定後の作業ノート** (statement・IH 配線・残 step)。

## ステートメント (確定, 型検証済 — `OddOrder/BG/Ch1_Preliminary/S03d_Thm34.lean`)

> ⚠ **S03d_Thm34.lean は現状 sorry 入り scaffold で uncommitted** (worktree に untracked で存在,
> OddOrder root に未配線ゆえ full build は green 不変)。sorry-free 達成時に commit。

```lean
private theorem thm34_aux : ∀ (n : ℕ)
    {F : Type*} [Field F] [IsAlgClosed F]
    {G : Type*} [Group G] [Finite G] [IsSolvable G]
    {V : Type*} [AddCommGroup V] [Module F V] [FiniteDimensional F V]
    (ρ : Representation F G V) (K R : Subgroup G) [K.Normal],
    Subgroup.IsComplement' K R →
    Nat.Coprime (Nat.card ↥K) (Nat.card ↥R) →           -- Hall (R 素数位数ゆえ |R|∤|K|)
    (∃ p : ℕ, p.Prime ∧ Nat.card ↥R = p) →
    Odd (Nat.card G) →
    (Nat.card G : F) ≠ 0 →                               -- char ∤ |G|
    (∀ v : V, (∀ r : ↥R, ρ (r : G) v = v) → v = 0) →    -- C_V(R) = 0 (S03b idiom)
    Nat.card G = n →
    ∀ g ∈ ⁅R, K⁆, ρ g = 1                               -- [R,K] ⊆ C_K(V)
-- main: `thm34_algClosed` は同型を hyp 引数で取り `thm34_aux (Nat.card G) ρ K R … rfl` を呼ぶ
```

設計判断:
- **alg-closed core** (Option A, GAP 2 決定): core を `[IsAlgClosed F]` で証明 (Thm 2.5/Gor 3.2.2 が必須)。
  一般 `F` は base change (BG (2.9) = `BaseChange.lean`) の別 leaf `thm34` で導く (downstream Thm 3.6 が消費)。
  induction は alg-closed F を保つ (subgroup HR も quotient G/N も同じ F)。
- **measure = `Nat.card G`** (= |G|), `Nat.strong_induction_on` (template = `frobeniusKernelIsNilpotent_aux`,
  `S03c_Thm37.lean:595`)。
- **Hall = `Coprime |K| |R|`** で表現 (Hall 構造体を持たず coprime で十分; 証明で使うのは coprime action)。
- **C_V(R)=0** / **C_K(V)**: S03b の idiom (`∀ v, (∀ r, ρ r v = v) → v=0` / `∀ g∈⁅R,K⁆, ρ g=1`)。
- minimal counterexample = `by_contra` で `[R,K]⊄C` (= `∃ g∈⁅R,K⁆, ρ g≠1`) を取り、IH で各 step → False。

## IH 配線 (rep-carrying induction, repo に既存パターン無し — 自作)

**✅ subgroup 枝 (step 1) — 型検証済 (2026-06-08)**: proper `R`-invariant `H < K` に対し
`HR = H ⊔ R` へ `ρ.comp HR.subtype : Representation F ↥HR V` を制限し、
`K' = H.subgroupOf HR`, `R' = R.subgroupOf HR` で IH を呼ぶ。**型整合 + instance 自動解決を確認**:
```lean
haveI : Finite ↥HR := inferInstance       -- subgroup of finite ✅
haveI : IsSolvable ↥HR := inferInstance    -- subgroup of solvable ✅
haveI : (H.subgroupOf HR).Normal := …      -- R が H を正規化 ⟹ H ◁ HR (要証)
have hIH := IH (Nat.card ↥HR) (…|HR|<n) (ρ.comp HR.subtype)
    (H.subgroupOf HR) (R.subgroupOf HR) (hcompl') (hHall') (hRp') (hodd') (hchar') (hCV') rfl
-- hIH : ∀ g' ∈ ⁅R.subgroupOf HR, H.subgroupOf HR⁆, (ρ.comp HR.subtype) g' = 1
```
残 = setup transport (各 `sorry`): `H◁HR` / `|HR|<n` (H<K, |HR|=|H||R|<|K||R|=|G|) /
`IsComplement' K' R'` / `Coprime |K'| |R'|` / `R'` prime / `Odd |HR|` / `(|HR|:F)≠0` (|HR|∣|G|) /
`C_V(R')=0` (元の hCV を subgroupOfEquivOfLe で transport) + 戻し (`⁅R,H⁆→⁅R',K'⁆` commutator transport)。
S03c は `Subgroup.subgroupOfEquivOfLe.toEquiv`/`Nat.card_congr`/`nilpotent_of_surjective` を使用; 同手法。

**✅ quotient 枝 (step 2-3) — 型検証済 (2026-06-08)**:
- `W : Subrepresentation ρ` (irreducible G-invariant submodule; Maschke `IsSemisimpleModule` で存在)。
  `W.toRepresentation : Representation F G ↥W.toSubmodule` (`Subrepresentation.lean:54`)。
- `N = C_G(W)` = `W.toRepresentation` の kernel (`MonoidHom.ker`, Subgroup G, Normal)。
- `N ≤ ker` ⟹ `[Representation.IsTrivial (W.toRepresentation.comp N.subtype)]` ⟹
  `Representation.ofQuotient W.toRepresentation N : Representation F (G⧸N) ↥W.toSubmodule`
  (mathlib `Basic.lean:352`)。
- **IH 呼び出しを型検証** (`error` 無し):
  ```lean
  IH (Nat.card (G ⧸ N)) _ (Representation.ofQuotient W.toRepresentation N)
     (K.map (QuotientGroup.mk' N)) (R.map (QuotientGroup.mk' N)) … rfl
  ```
  instance `[Finite (G⧸N)]`/`[IsSolvable (G⧸N)]`/`[FiniteDimensional F ↥W.toSubmodule]`/
  `[(K.map mk').Normal]` は **全自動解決**。
- 残 setup: `K.map mk'` normal Hall in `G⧸N`, `R.map mk'` complement prime, `C_W(R̄)=0`,
  `|G/N|<|G|` (N≠1), 戻し (`[K̄,R̄]=1 ⟹ [K,R]⊆N ⟹ M 選択 ([K,R] 非自明) に矛盾 ⟹ N=1`)。**重い**。

## 証明 8 step と ingredient (mmd L867-901; inventory 正本 = `s03_keystone_plan.md`「7/8 done」)

1. **(3.2)** proper R-inv `H<K` ⟹ `[H,R]⊆C` (subgroup 枝 IH) ⟹ `[R,K]⊄C` より K は proper R-inv で
   生成されない。論理: `K=⨆{proper R-inv H}` なら `[R,K]=⨆[R,H]⊆C` で by_contra に矛盾。
2. Maschke (mathlib `IsSemisimpleModule k[G] V`) ⟹ `[K,R]` 非自明な irred G-submodule `M`。`N=C_G(M)`,
   `p=|R|`。`C_M(R)⊆C_V(R)=0` ⟹ `N∩R=1` ⟹ `|G|=p|K|`, `N◁G` p'-subgroup `⊂K`。
3. `G/N` で IH ⟹ `N=1` ⟹ G faithful irred on M (∴ V). **Gor 3.2.2** = `isCyclic_center_of_faithful_irreducible`
   (`AbsolutelyIrreducible.lean:187`) ⟹ `Z(G)` cyclic。
4. `|K|` ≥2 primes ⟹ **Prop 1.5(a)** (`exists_aInvariant_sylow`, `Ch04 ForwardFromCh03:440` /
   `exists_aInvariant_hall`, `S01:661`) で R-inv Sylow ⟹ K が proper R-inv で生成 ⟹ (3.2) 矛盾 ⟹ `|K|=qⁿ`。
   (= **Leaf 2**, `s03_keystone_plan.md`: factorization で join=⊤, ~60-80 行, scaffold の (3.2) 形に coupling)
5. **Gor 5.3.7** = `isSpecial_of_pprimeAction_trivialOnProper` (`S04e:206`): K elem.ab. or nonabelian special
   + R 既約 on K/K' (= 第3連言) + R が K' 中心化 (= commutator≤center)。**φ:R→*MulAut↥K は
   `MulAut.conjNormal ∘ R.subtype`** (既存パターン `S7B1_NormalJ.lean:703/710`; 新規 def 不要)。
6. K elem.ab. ⟹ `K'=1`, `[R,K]≠1` ⟹ `C_K(R)⊂K`, (3.3) で `C_K(R)=1` ⟹ **Lemma 3.1**
   (`IsFrobeniusGroup` + `of_centralizer_kernel_le`, `FrobeniusGroup:240/324`) で Frobenius ⟹
   **Lemma 3.3** (`centralizer_ne_bot_of_nontrivial_kernel`, `S03b:114`) で `C_V(R)≠0` 矛盾 ⟹ K nonabelian special。
7. K nonabelian special ⟹ `Z(K)=K'`, (3.3) で R が `Z(K)` 中心化 ⟹ `Z(K)⊆Z(G)` ⟹ Z(K) cyclic
   (subgroup of cyclic) ⟹ **K extraspecial** = **Leaf 1** `IsExtraspecial.of_isSpecial_of_isCyclic_center`
   (`IsExtraspecial.lean:128`, IsSpecial + ¬elem.ab. + IsCyclic(center) ⟹ IsExtraspecial)。
8. (3.3) で R 既約 on `K/Z(K)`, Z(K) maximal R-inv ⟹ `Z(K)=C_K(R)=C_K(x) ∀x∈R^#`。**Thm 2.5**
   (`finrank_eq_sub_one_of_extraspecial`, `ExtraspecialThm25Final:89`) で最終矛盾 = **Leaf 4**:
   `dim M=p-1` 偶 (p 奇) vs `(dim M)²=|K/Z(K)|` 奇 (`sq_finrank_eq_card_quotient_center`,
   `ExtraspecialFaithful:238`; |K|∣|G| 奇) ⟹ dim M 奇 ⟹ False。`dim M=qᵐ` 陽出し不要。

## 進捗サマリ (2026-06-08, `a-keystone`)

- ✅ **statement 確定・型検証** (aux + main, `thm34_algClosed`)。
- ✅ **両枝 IH 配線 型検証** — subgroup (`ρ.comp HR.subtype`+`subgroupOf`) + quotient
  (`ofQuotient W.toRepresentation N`+`K.map (mk' N)`); instance 全自動解決。
  **⟹ rep-carrying induction 骨格 (scaffold 最大の不確実性) が実装可能と確定。**
- ▶ 残: 両枝 setup transport 充足 → Leaf 2 (step 4) → Leaf 4 (step 8) → step 2-3,5-7 を既
  ingredient で充足 → base-change wrapper (`thm34`)。
- **コミット状態**: S03d_Thm34.lean は uncommitted (sorry scaffold)。本ノートのみ commit。
- **次の一手**: 両枝の setup transport (subgroup: `H◁HR`/`|HR|<n`/`Complement'`/`Coprime`/prime/odd/
  char/`C_V(R')`/commutator 戻し; quotient: `N=ker`・`N=1` 還元・`K̄` Hall・`R̄` prime・`C_W(R̄)`) を
  bottom-up 充足。Leaf 2 (|K|=prime power) / Leaf 4 (parity) は他 step と独立度が高く先行可。
