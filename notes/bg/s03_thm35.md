# BG Theorem 3.5 — 形式化プラン (Lane A `a-keystone`, 2026-06-09 着手)

**スコープ**: Bender–Glauberman, Chapter I §3, mmd `references/bg/local-analysis.mmd` **L903-951**.
Thm 3.4 (`S03d_Thm34.lean`, ✅ 完成) の姉妹定理。上流は Thm 3.6 (mmd L955) → §10.6 (keystone 解除)。

## ステートメント

> **BG Theorem 3.5**: `G = KR` を Frobenius 群 (可解 Frobenius kernel `K`, 巡回 Frobenius complement
> `R` 素数位数 `p`)。`G` が体 `F` 上のベクトル空間 `V` に作用 (`char F ∤ |G|`)。**`C_V(R)` が
> 1 次元**なら `K' ⊆ C_K(V)`。

```lean
-- alg-closed core (induction の本体; WLOG F alg-closed は証明 step 1)
theorem thm35_algClosed {F} [Field F] [IsAlgClosed F] {G} [Group G] [Finite G]
    {V} [AddCommGroup V] [Module F V] [FiniteDimensional F V]
    (ρ : Representation F G V) (K R : Subgroup G)
    (hFrob : IsFrobeniusGroup G K R) (hKsolv : IsSolvable ↥K) (hRcyc : IsCyclic ↥R)
    (hRp : ∃ p, p.Prime ∧ Nat.card ↥R = p) (hchar : (Nat.card G : F) ≠ 0)
    (hCV1 : Module.finrank F (Representation.invariants (ρ.comp R.subtype)) = 1) :
    ∀ g ∈ ⁅(⊤ : Subgroup ↥K), ⊤⁆.map K.subtype, ρ g = 1  -- K' ⊆ C_K(V)
-- general F は base change で `thm35` として導く (Thm 3.4 の `thm34` と同型)
```

⚠ **K' の表現**: `commutator ↥K` を `K.subtype` で G に map した `⁅⊤,⊤⁆.map K.subtype`、または
別途 `K'_subgroup : Subgroup G`。Thm 3.4 は `⁅R,K⁆` を直接使ったが、3.5 は `K'` が必要 — 表現を要検討
(Thm 3.6 が `[H,R]'` 等でどう消費するかに合わせる)。

## 証明 9 step (mmd L907-951, |G| 強帰納法)

1. **WLOG F alg-closed** (L907): F→F̄, V→V⊗F̄。`C_V(R)` 1-dim は base change で保存 (要新補題、下記)。
   ⟹ `thm35` (一般 F) = `thm35_algClosed` への wrapper。
2. **非 faithful 枝** (L909-913): `C = C_G(V) ≠ 1`。`K ⊆ C` なら `K'⊆C` 即終。`K ⊄ C` なら **Lemma 3.2**
   (`frobenius_quotient_of_normal_lt_kernel`, S03c:233; ただし `C ⊆ K` の導出 = Frobenius の normal は
   kernel に含まれる or 含む、別途要) で `C ⊂ K` + `G/C` Frobenius。`C_V(RC/C)=C_V(R)` ⟹ IH ⟹ `(K/C)'⊆C/C`
   ⟹ `K'⊆C`。
3. **faithful 仮定** (L915, 残り全体)。⟹ `C_G(V)=1`、つまり任意の `N≤G` で `C_N(V)=1`。
4. **(3.4)** (L917-927): proper R-inv subgroup `N<K` ⟹ `NR` が仮説満たす (`|NR|<|G|`) ⟹ IH で `N'⊆C_N(V)`。
   faithful ゆえ `C_N(V)⊆C_G(V)=1` ⟹ `N'=1` ⟹ **N abelian**。よって全 proper R-inv subgroup は abelian。
5. **K' abelian** (L929): K 可解 ⟹ `K'` proper characteristic ⟹ proper R-inv ⟹ (3.4) で abelian。
6. **(3.5) 二分律** (L931-939) [**tractable leaf, IH 不要**]: R-inv `N⊆K`, NR-submodule `U⊕W⊆V`
   (U,W NR-inv)。N が U に非自明作用 ⟹ Lem 3.3 で `C_U(R)≠0` ⟹ (1-dim) `C_V(R)=C_U(R)⊆U` ⟹
   `C_W(R)=C_V(R)∩W⊆U∩W=0` ⟹ Lem 3.3 対偶で N が W に自明。∴ **`U⊆C_V(N)` ∨ `W⊆C_V(N)`**。
   ⚠ plumbing: NR を Frobenius 部分群として組む (`N.subgroupOf NR`/`R.subgroupOf NR` + `centralizer_ne_bot`)。
7. **U 選択 + Maschke** (L941): faithful ⟹ K が非自明作用する irreducible G-submodule `U` が存在。Maschke で
   G-inv complement `W` (`V=U⊕W`)。(3.5) [N=K] で `W⊆C_V(K)⊆C_V(K')`。
8. **`C_U(K')≠0` ⟹ done** (L943): `C_U(K')≠0` なら `K'◁G` ゆえ submodule、U irreducible ⟹ `U=C_U(K')⊆C_V(K')`
   ⟹ `V=U⊕W⊆C_V(K')` ⟹ faithful で `K'=1`。∴ **`C_U(K')≠0` を示せば十分**。
9. **`C_U(K')≠0` の証明** (L945-951) [**hard core = 一般体 Clifford/Wedderburn**]:
   - U を K-module として: irreducible K-submodule M, R=⟨x⟩。**Clifford (G 3.4.1)**: `U=⊕共役M`,
     R が Wedderburn 成分を transitive に置換 ⟹ 成分数 = 1 or p。
     - **p 成分**: `U=M⊕Mˣ⊕…⊕Mˣ^{p-1}`, π₁ 射影。`Σ mxⁱ` は R-fixed ⟹ π₁(C_U(R))=M, C_U(R) 1-dim
       ⟹ M 1-dim K-module ⟹ `M⊆C_U(K')` ⟹ done。
     - **1 成分** (共役全 iso): **Prop 2.2** ⟹ `U_K≅M` (U irreducible K-module)。
   - U を K'R-module として: reducible なら (3.5) で `C_U(K')≠0`。⟹ U irreducible K'R-module と仮定。
   - U を K'-module として: Clifford ⟹ Wedderburn 成分。R が transitive (数∣p), K が transitive (数∣|K|),
     `gcd(p,|K|)=1` ⟹ 1 成分。**Prop 2.2** ⟹ U irreducible K'-module。
   - F alg-closed + K' abelian ⟹ **U 1-dim** ⟹ `U⊆C_U(K')` ⟹ `C_U(K')≠0`。done。

## 依存 ingredient 状態

| 依存 | 状態 | 場所 / 備考 |
|---|---|---|
| **Lemma 3.3** (Frobenius kernel nontrivial ⟹ C_V(R)≠0) | ✅ | `S03b.centralizer_ne_bot_of_nontrivial_kernel` / `kernel_acts_trivially_of_centralizer_eq_bot` (全群 G=KR 用; (3.5) では NR 部分群に適用 = 要 plumbing) |
| **Lemma 3.2** (Frobenius quotient) | ◯ 部分 | `S03c.frobenius_quotient_of_normal_lt_kernel` (N≤K 前提で G/N Frobenius)。**残: `C⊆K` (Frobenius の normal subgroup は kernel に含まれる) の導出** が別途要 |
| **Lemma 3.1** (Frobenius criterion) | ✅ | S03 + `IsFrobeniusGroup.of_centralizer_kernel_le` (FrobeniusGroup) |
| **base change** (WLOG alg-closed) | ◯ 部分 | `BaseChange.lean` (`baseChangeRepresentation`/`_comp`/`invariants_baseChangeRepresentation_eq_bot`)。**残: `finrank (invariants (baseChange ρ)) = finrank (invariants ρ)`** (1-dim 保存) = 新補題。`invariants_baseChangeRepresentation_eq_bot` は =⊥ のみ |
| Maschke / 完全可約 / irreducible submodule 存在 | ✅ | mathlib (`IsSemisimpleModule`); Thm 3.4 で使用パターンあり (`exists_irreducible_subrep_apply_ne`) |
| **Clifford (G 3.4.1)** = U=⊕共役, 群が Wedderburn 成分を transitive 置換 | ❌ **一般体未** | `Clifford.lean` は **ℂ 限定** (65 decls)。alg-closed 一般体版は `CliffordAlgClosed`/`CliffordConjugateChar` + mathlib isotypic API (`RingTheory/SimpleModule/Isotypic.lean`) を土台に**新規構築** (= 本定理の hard core) |
| **Prop 2.2** (共役全 iso ⟹ U_H≅M irreducible; 単一 Wedderburn 成分 ⟹ irreducible) | ❌ **一般体未** | 同上。hVP 用 `restriction_isIrreducible_of_*` (CliffordConjugateChar) は extraspecial 特化 — 一般 Prop 2.2 は別 |

## hard core = 一般体 Clifford/Wedderburn (Thm 2.5 級インフラ)

step 9 の Clifford 分解 (Wedderburn 成分 + 群の transitive 置換 + p-vs-1 二分律 + 射影論法 + Prop 2.2) が
本定理の本丸で、現状 ℂ 限定。**一般 alg-closed 体上の Clifford 理論**を新規に積む必要がある (Thm 3.4 における
Thm 2.5 に相当する位置)。土台候補:
- mathlib isotypic API (`IsIsotypicOfType`/`linearEquiv_fun`/`endAlgEquiv`, Maschke instance)。
- repo `CliffordAlgClosed.lean` (`conjNormalMulAut`/`conjSemilinearEnd`/`isSimpleModule_map_conjSemilinearEnd`/
  `eq_top_of_forall_map_conjSemilinearEnd_le`) + `CliffordConjugateChar.lean` (`isIsotypicOfType_of_conjugates`)。
- 不足 = 「巡回群 R が Wedderburn 成分を transitive 置換、成分数 ∣ |R|」「Prop 2.2 一般版」。

## tractable leaves (Clifford 非依存, 先行着地可)

1. **(3.5) 二分律** (step 6) — Lem 3.3 + NR-Frobenius plumbing。IH 不要、standalone reusable。
2. **base-change dim 保存補題** — `finrank (invariants (baseChange ρ)) = finrank (invariants ρ)`
   (BaseChange.lean 拡張、obstruction-map + flat base change of ker)。WLOG step 1 に必要。
3. **Lemma 3.2 の `C⊆K` 部分** — Frobenius の proper normal subgroup ⊆ kernel (純群論)。

## 進捗 (2026-06-09, S03e_Thm35.lean = untracked scaffold)

- ✅ **statement `thm35_algClosed` 確定・型検証** (Frobenius/solvable K/prime R/`char∤|G|`/
  `finrank (invariants (ρ.comp R.subtype)) = 1` / 結論 `∀ g ∈ ⁅K,K⁆, ρ g = 1`)。
- ✅ **sub-Frobenius constructor `isFrobeniusGroup_subgroupOf_sup` DONE** (sorry-free):
  `IsFrobeniusGroup G K R` + `N≤K` + `R≤N_G(N)` + `N≠⊥` ⟹ `IsFrobeniusGroup ↥(N⊔R) N.subgroupOf
  R.subgroupOf`。Thm 3.4 の HR setup を Frobenius 版に。**(3.4) と (3.5) 両方の基盤**。
  GOTCHA: `subgroupOf_eq_bot ↔ Disjoint`; `where` 各 field は独立ゆえ isComplement 内で normal を
  `haveI` 再注入; conj_frobenius は `N≤K` で K の条件に帰着。

### 非 faithful 枝の精緻化 (実装容易と判明)

IH を G/C (C=ker ρ) に適用すると `∀ ḡ∈⁅K̄,K̄⁆, ρ̄ ḡ=1`、ρ̄(mk g)=ρ g (C=ker ρ で factor) ⟹
`∀ g∈⁅K,K⁆, ρ g=1` 直接。**`C⊂K` strict は不要、`C≤K` (frobenius_quotient 用) のみ**。
よって残 = **`C≤K` (Frobenius の normal subgroup ⊆ kernel, ¬K≤C 時)** = 純群論 leaf。
`K≤C` 枝は `⁅K,K⁆≤K≤C=ker ρ` で即終。

## 次の一手 (induction scaffold + leaves)

1. **induction backbone** `thm35_aux` (`Nat.card G=n` + `Nat.strong_induction_on`, Thm 3.4 thm34_aux 型)
   + `by_cases ρ faithful`。非faithful 枝 = `C≤K`(sorry) + `frobenius_quotient_of_normal_lt_kernel`
   + IH on G/C + commutator map。faithful 枝 = (3.4)/(K' abelian)/(step7-8)/Clifford(sorry)。
2. **(3.4)** = `isFrobeniusGroup_subgroupOf_sup` で NR 構成 → IH on NR (`|NR|<|G|`, dim C_V(R) 転送) →
   `N'⊆C_N(V)` → faithful で `C_N(V)=1` → `N'=1` → N abelian。
3. **(3.5)** = NR-submodule U⊕W に Lem 3.3 ×2 (NR=sub-Frobenius, ρ を U に制限)。
4. **`C≤K` leaf** (Frobenius normal ⊆ kernel) = 純群論。
5. **step 9 = hard core** (一般体 Clifford) は別インフラ build。
**Thm 3.4 (`thm34`/`thm34_algClosed`) 完成済で step 7-9 中で消費可能性あり** (faithful irreducible 構造)。
