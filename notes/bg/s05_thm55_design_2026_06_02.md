# BG Thm 5.5 (solvableAut_of_narrow) — 設計 + 現在地 (2026-06-02)

> **scope**: §5 後半 Thm 5.5 / 5.6 / 5.7 の攻略計画。
> Thm 5.3 / 5.3(d) / Cor 5.4 は **2026-06-02 に closed** (本 note の前提)。
> 出典: mmd L1881-1967。前半の設計は `s05_design_2026_05_30.md`。

## ✅✅ 2026-06-02 (同日セッション続行): **Thm 5.5 COMPLETE**

`solvableAut_of_narrow` は **sorry-free・axiom-clean・AxiomsCheck 登録済** (commit 90fac6f)。
本 note §1–§3 の計画はそのまま実装された (歴史的記録として保持):

- Brick A = `not_narrow_witness_le_critical`, Brick B = `card_inf_critical_centralizer_eq_prime`
  (+ 一般 squeeze `isCyclic_of_le_centralizer_of_inf_eq_bot_of_pRank_le_two` へ 5.3(d) を refactor),
  commit d5545b3。
- Brick C = `card_le_card_mul_of_commutator_mem_of_card_centralizer_le` (coset counting) +
  `isPGroup_commutator_and_orderOf_dvd_of_narrow_witness` (降鎖+stability, ~280行), commit 7b87424。
- (c) assembly = `thm55c_of_pRank_le_two` (Lem 4.14 + Thm 4.16 中心積 → `Ω₁(R)=R₁` →
  `IsCyclic.card_mulAut`+`Nat.totient_prime_pow` で **G** Thm 5.4.1 を代替), commit 90fac6f。

S05 の残 sorry = **2 件**: `narrow_sylow_solvable_structure` (5.6)
/ `derived_le_fitting_of_centralizer_pRank_le_two` (5.7)。

## ✅✅ Thm 4.18 COMPLETE (同日続行, commit bc3f2f1)

`S04.solvable_structure_of_pRank_le_two` (新 leaf `S04g_Thm418.lean`, §4H,
sorry-free・axiom-clean・AxiomsCheck 登録済)。結論 5 連 (a)-(e) は **Thm 5.6 の scaffold
statement と同語彙** (oPiCore {r≠p} / HasNormalPComplement / IsPiSubgroup /
oPiPrimePiCore / hasPLengthOne)。

- core (O_{p'}=1) = Hall–Higman 1.2.3 (`Ch03.hall_higman_1_2_3` = **G** Thm 6.3.2 —
  Fitting/Prop 1.3 経路は不要だった) + Lem 4.17 + Lem 4.13。
- 一般化 = 商 Ḡ = G/O_{p'} へ core を適用し third isomorphism で引き戻し。
  **新 reusable**: `pRank_quotient_le_of_coprime` (p'-核の商で p-rank 不増;
  逆像の Sylow lift)・`conjNormal_ker`・`{p}`-IsPiGroup glue。
- ⚠ Ch03 の `Subgroup.IsPiGroup` 実名は namespace 入れ子で
  `OddOrder.Isaacs.Ch03.Subgroup.IsPiGroup` (`Ch03.Subgroup.IsPiGroup` で参照)。
  `oPiPrimePiCore` の内部 π'-集合は `{q | q ∉ ({p} : Set ℕ)}` — `{r | r ≠ p}` と
  集合としては等しいが **rw は mk'-instance 絡みで motive 死する** → N をこの形で
  定義し、境界 (card/(a)) でのみ plain-oPiCore の set-rw で変換するのが正解。

**次 = Thm 5.6**: r(S)≤2 分岐は 4.18 直結 (r_p(G) = r(S) の Sylow-rank 橋が必要:
pRank G p ≤ pRank ↥S p — 全 elem-ab p-部分群が Sylow 共役内 — 要実装 ~50行)、
r(S)≥3 分岐は hasPLengthOne 仮定 + Thm 5.5 + 「Thm 4.18 の方法」(mmd L1953)。
**Thm 5.7** = Prop 1.2 chief-factor 還元 + Cor 4.19 形の中心化 + Thm 5.5。

**✅ 完成済みゲート (今セッション)**:

1. **BG Lem 4.17** = `S04.isPGroup_commutator_of_mulAut_odd_of_pRank_le_two`
   (S04 §4G, axiom-clean, AxiomsCheck 登録済): `p` odd, `r(R)≤2`, `φ : A →* MulAut R`
   faithful, `|A|` odd ⇒ `IsPGroup p (commutator A)`。solvable 仮定は不要だった
   (§2 エンジンが odd のみで成立)。
   - 内部 private: `mulAut_eq_one_of_coprime_orderOf_of_frattini` (elementwise Thm 1.8
     Burnside: p'-order automorphism が Φ(H) mod 自明 ⇒ 自明) と
     `isPGroup_commutator_or_comm_of_homGLTwo` (GL(2,p) 受けの Thm 2.6 エンジン)。
     後者は **5.6/5.7 でも再利用可能な形** (B ↪ GL(Fin 2)(ZMod p) → A' p-群 or abelian)。
2. **BG Lem 1.9 多段一般形** = `S01.coprime_stabilizes_chain_trivial` (AppA private から
   昇格)。π'-元 `a` には `⟨a⟩` (cyclic, coprime) を渡して elementwise に使う。
3. **Thm 5.5(a) generic core** = `S05.quotient_opCore_comm_and_not_dvd_of_isPGroup_commutator`
   (private): `IsPGroup p (commutator A)` ⇒ `A/O_p(A)` abelian ∧ `¬p ∣ |A/O_p(A)|`。
   **両 rank 分岐がここに合流する**。

## 1. Thm 5.5 の Lean 組み立て (確定設計)

```
by_cases hrank : pRank R p ≤ 2
├─ r ≤ 2:
│   hA' := S04.isPGroup_commutator_of_mulAut_odd_of_pRank_le_two   ✅
│   (a)  := quotient_opCore_comm_and_not_dvd_of_isPGroup_commutator hA'   ✅
│   (b)  := fun h3 => absurd (h3, hrank) (omega)
│   (c)  := §3 の assembly (下記)
└─ r ≥ 3 (¬ ≤ 2):
    hA' := 「A' p-群」 r≥3 版 (§2 の chain 機構 — 未実装の本体)
    (a)  := 同 generic core hA'   ✅
    (b)  := chain 機構の α^{p-1} 帰結 (§2)
    (c)  := (b) で矛盾: |A|=q prime ∤ p(p-1) なら q ∤ p → A の生成元は p'-元
            → (b) で q ∣ p-1 → q ∣ p(p-1) 矛盾 → 前提が偽
```

## 2. r ≥ 3 chain 機構 (残る本体, 推定 ~500 行)

mmd L1893-1917。未実装 brick 3 つ:

### Brick A: `R₀ ⊄ H` (~150 行)

narrow witness `R₀` (位数 p, `C_R(R₀) = R₀ ⊔ R₁`, `R₁` cyclic;
`exists_narrow_witness_of_three_le_pRank` ✅) と Thm 1.13 の critical `H`
(`thompson_critical_omega` ✅; `hHcommtop : ⁅H,⊤⁆ ≤ Z(H).map subtype`) に対し:

- `U := R₀ ⊔ (center ↥H).map H.subtype` とおく。`R₀ ≤ H` を仮定して矛盾:
- `U` elem-ab (`R₀` 位数 p + `Z(H)` exp p (H exp p) + 中心化:
  `Subgroup.IsElementaryAbelian.sup_of_le_centralizer` ✅)。
- **`U ⊴ R`**: `[⊤,U] ≤ [⊤,H] = ⁅H,⊤⁆ ≤ Z(H)map ≤ U` (BG は「U⊴H」と書くが (5.4) から
  実際は R-normal が出る。ここが Lem 5.1(b) 適用の鍵)。
- `U ≤ C_R(R₀)` → `m(U) ≤ r(C_R(R₀)) ≤ 2` (`pRank_centralizer_le_two_of_narrow_witness` ✅
  + subgroupOf + `le_pRank`)。`|U| = p^{m(U)}`, m(U) ∈ {1,2}:
  - **m(U)=1**: `U = R₀` (card で) → `Z(H) ≤ R₀`。`H ⊴ R` 非自明 → `H ∩ Z(R) ≠ 1`
    (`exists_mem_center_of_normal_ne_bot` ✅ @CriticalSubgroup:118) → その元は `Z(H) ≤ R₀`
    → `R₀ ≤ Z(R)` (位数 p) → `C_R(R₀) = ⊤` → `pRank R ≤ 2` 矛盾
    (パターン: S05 `inf_omega1Center_eq_bot_...` L1339-1357)。
  - **m(U)=2**: `U ⊴ R` elem-ab 位数 p² → **Lem 5.1(b)**
    `mem_scn3_of_normal_isElementaryAbelian_card_prime_sq` ✅ → `∃ A∈SCN₃, U ≤ A`
    → `A ≤ C_R(U) ≤ C_R(R₀)` (A abelian) → `3 ≤ m(A) ≤ r(C_R(R₀)) ≤ 2` 矛盾。

### Brick B: `|C_H(R₀)| = p` (~100 行)

- `C_H(R₀) := H ⊓ Subgroup.centralizer R₀`。
- `≤ p`: `C_H(R₀)` は elem-ab (exp p ⊂ H + abelian: `C_R(R₀)` が abelian —
  `R₀ ≤ Z(C_R(R₀))` + `C/R₀ ≅ R₁` cyclic → mathlib
  `commutative_of_cyclic_center_quotient`; あるいは rank-squeeze だけで足りる)。
  `C_H(R₀) ⊓ R₀ = ⊥` (Brick A: `R₀ ⊄ H` + 位数 p → `H ⊓ R₀ = ⊥`,
  `le_of_inf_ne_bot_of_card_prime` ✅)。`R₀ ⊔ C_H(R₀)` elem-ab 位数 `p^{1+t}` ≤
  `C_R(R₀)` rank ≤ 2 → `t ≤ 1` (**5.3(d) cyclicity 補題と同じ squeeze**;
  `isCyclic_inf_centralizer_omega1UpperCentralTwo_of_centralizer_pRank_le_two` の
  card 版を一般化して共用するのが吉)。
- `≥ p`: `H ∩ Z(R) ≠ 1` の元 (位数 p) が `C_H(R₀)` に入る。

### Brick C: 降鎖 + stability (~250 行)

- `s : ℕ → Subgroup R`, `s 0 = H`, `s (i+1) = ⁅s i, ⊤⁆`
  (= `[R, H_i]`; mathlib `Subgroup.commutator_characteristic` instance で各 char)。
- **下降**: `s (i+1) ≤ s i` (`Subgroup.commutator_le_right`-ish, normal ✅)。
- **⊥ 到達**: `s i ≤ lowerCentralSeries R i` (帰納; `H ≤ ⊤` + 単調) +
  `IsPGroup.isNilpotent` → `∃ n, LCS n = ⊥`。
- **factor ≤ p**: `v ∈ R₀`, `v ≠ 1`。conjugation orbit `{x⁻¹ v x | x ∈ H_i}` ⊆ `v·H_{i+1}`
  (`v⁻¹x⁻¹vx = ⁅v⁻¹?,..⁆ ∈ ⁅⊤, s i⁆`) + orbit-stabilizer
  (`MulAction.card_orbit_mul_card_stabilizer_eq_card_group` for ConjAct or 手組み:
  fibers of `x ↦ x⁻¹vx` = `C_{H_i}(v)`-cosets) + `|C_{H_i}(v)| ≤ |C_H(R₀)| = p` (Brick B;
  `⟨v⟩ = R₀` zpowers) → `|H_i : H_{i+1}| ≤ p`。
- **stabilizer subgroup**: `StabA := {a : A | ∀ i, ∀ x ∈ s i, x⁻¹ * φ a x ∈ s (i+1)}` は
  A の部分群 (factor 上自明な元の集合; 積閉は factor ごとの計算)。
- **A' ≤ StabA, α^{p-1} ∈ StabA**: factor `s i ⧸ s (i+1)` は位数 ≤ p → Aut は位数 ∣ p-1 の
  abelian (`IsCyclic.card_mulAut` + `Nat.totient_prime` or 直接 `ZMod`); A の factor-作用
  hom で commutator ↦ 1, α^{p-1} ↦ (image α)^{p-1} = 1。
- **StabA の p'-元は C_A(H) に落ちる**: `a ∈ StabA` p'-order →
  `S01.coprime_stabilizes_chain_trivial` ✅ を `⟨a⟩`, `s · |_H` (subgroupOf H 化) に適用 →
  `a` は H 上自明 → `a ∈ ker(restrictAction) ≤ comap φ (autCentralizer H)` p-群
  (Lem 4.17 の `hkerH_le`/`hkerH_pg` パターン ✅) → p'-order → `a = 1`。
- **帰結**: `A' は p-群` (a ∈ A' の p'-part ∈ StabA → =1; Lem 4.17 の
  `exists_eq_pow_mul_and_not_dvd` パターン ✅) → (a)。
  `α` p'-元 → `α^{p-1} ∈ StabA` も p'-元 → `α^{p-1} = 1` → `orderOf α ∣ p-1` → (b)。

## 3. (c) assembly (~200 行, r≤2 分岐)

前提: `|A| = q` prime, `q ∤ p(p-1)`。`SCN₃(R) = ∅` (A∈SCN₃ → `3 ≤ pRank ↥A ≤ pRank R ≤ 2`)。

1. **q ∣ (p+1)/2**: `q ∣ |MulAut R|` (φ injective → `|A| = |φ.range| ∣ |MulAut R|`),
   `q ≠ p` → **Lem 4.14** `dvd_half_prime_add_or_sub_of_prime_dvd_aut_of_scn3_empty` ✅
   → `q ∣ (p+1)/2 ∨ q ∣ (p-1)/2`; 後者 → `q ∣ p-1` (odd p: `p-1 = 2·(p-1)/2`) → 矛盾。
2. **|R| = p³** (R=[R,A] 非可換時): **Thm 4.16** `blackburnRankTwoClassification` ✅
   (hcop: q≠p prime → coprime q p^n) → `BlackburnCentralProductCase`: `R = R₁∘R₂`,
   `R₁` nonabelian p³ exp p, `R₂` cyclic, `Ω₁(R₂) = R₁'`。
   - **`Ω₁(R) = R₁` (集合同定)**: `x = u·v` (中心積分解), `x^p = v^p`;
     `v^p=1 ⟺ v ∈ Ω₁(R₂) = R₁' ≤ R₁` → 位数≤p 元の集合 = `R₁` → `Omega R p 1 = R₁`。
   - `R/Ω₁(R)` cyclic: `R₂ ↠ R/R₁` (sup = ⊤)。
   - **A は R/Ω₁ を中心化**: `N := Omega R p 1` characteristic → φ-invariant →
     `quotientMulAutHom` (注意: 実名は
     `OddOrder.Isaacs.Ch04.OddOrder.Isaacs.Ch03.IsAInvariant.quotientMulAutHom` —
     namespace 入れ子事故, S04e/S04G の open 行参照) で `A →* MulAut (R⧸N)`;
     `|MulAut (R⧸N)| = totient |R⧸N| = p^{t-1}(p-1)` (`IsCyclic.card_mulAut` +
     `Nat.totient_prime_pow`), `q` prime ∤ p(p-1) → image 自明。
   - **[R,A] ≤ N**: `Ch04.actionCommutator_quotient_eq_map` ✅ (Ch04:2259 付近) で
     quotient の actionCommutator = map; 自明作用 → map = ⊥ → `actionCommutator φ ≤ N`。
   - `hRA : actionCommutator φ = ⊤` → `N = ⊤` → `|R| = |R₁| = p³`。

## 4. 5.6 / 5.7 への注意

- **5.6 は Thm 4.18 を要する** (mmd L1734-: `r_p(G) ≤ 2` solvable structure)。
  Thm 4.18 自体が未実装 (S05 header も明記)。5.6 の前に **Thm 4.18 を S04 §4H として
  実装する必要がある**。その証明は Lem 4.17 ✅ + Lem 4.13 ✅ + Prop 1.3/1.5 系。
- 5.7 は Prop 1.2 (chief factor 還元) + 5.5 を chief factor ごとに適用。

## 5. ハマりどころ (今セッションの教訓)

- **letI-bound `zmodModule` は後続 instance 探索と単一化しない** (PRank
  `addAutEquivGL` の警告 docstring が正本)。Aut(E)→GL 橋は必ず
  `mulAutEquivGeneralLinearGroup` + `rw [← hfinrank]` で `GL (Fin 2) (ZMod p)` に
  寄せ、表現は具象加群 `Fin 2 → ZMod p` (`Matrix.GeneralLinearGroup.toLin`) 上で組む。
- `Function.Injective.comp` は dot-notation だと `Function.comp` に化ける
  (Injective が reducible)。明示適用で書く。
- `quotientMulAutHom` 群の実名は `OddOrder.Isaacs.Ch04.OddOrder.Isaacs.Ch03...` —
  `open OddOrder.Isaacs.Ch04.OddOrder.Isaacs.Ch03.IsAInvariant (quotientMulAutHom)`。
- rc2 mathlib: `ord_proj`/`ord_compl` 記法は消滅 →
  `Nat.exists_eq_pow_mul_and_not_dvd`。`Nat.even_iff_not_odd` 消滅 → `Nat.odd_iff` +
  omega (omega は `2 ∣ x` を扱える)。Cauchy = `exists_prime_orderOf_dvd_card'`
  (Nat.card 版)。`Monoid.exp_eq_one_of_subsingleton` (exponent_eq... ではない)。
- `set` は zeta-透過 (defeq OK) だが `simp` の normal form とはぶつかる。
  quotient 作用の coset 計算は `rw [hψV_def] at` で実名に戻してから
  `simp only [MulAut.one_apply]`。
