# 引き継ぎ: BG §7 Transitivity (2026-06-03 セッション末)

> このセッションで **BG §7 の Lemma 7.1 + Thm 7.2 + Thm 7.3 + Prop 1.16 + 共役ブリッジ2種**
> を完成 (全 axiom-clean, build-green 3560 jobs)。さらに **Gorenstein 依存監査** を実施し
> ROADMAP/notes の stale/誤りを訂正。本ノートは次セッションの起点。live 状況は memory
> `ft-master-roadmap` が正本。

## このセッションのコミット (新しい順、main, push 未)

```
09fcb9d Prove BG Theorem 7.2 (transitivity for m(Z(A)) ≥ 3)
d522728 Add BG Prop 1.16(2) cocyclic conjugation form for §7
31b0846 Prove BG Theorem 7.3 (transitivity for m(Z(A)) ≥ 2, q ∈ π(C_G(A)))
f1084b1 Add BG Prop 1.16(1) conjugation form for §7
75baa37 Audit Gorenstein-dependency status; fix stale ROADMAP/notes entries
d71cbe4 Prove BG Prop 1.16(2): coprime-action generation by cocyclic centralizers
48785d9 Prove BG Lemma 7.1 (Inductive Lemma) — §7 transitivity keystone
```
`lake build OddOrder` green (3560 jobs)。AxiomsCheck に全結果登録済 (axiom-clean)。

## ✅ Gorenstein 監査結論 (再監査するな, commit 75baa37)

「Isaacs で対応できず Gorenstein を形式化必須」かつ**未形式化の項目は残ゼロ**:
p-stability (App.A, G 3.8.1/§6.5) ✅ / ZJ・Z(J) (App.B Puig L(S)) ✅ / critical subgroup
(Thm 1.13, G 5.3.11/13) ✅ / §4 precursors (Gor Thm 3.7/3.8) ✅。BG mmd の "**G**" 引用は
12 件のみで全て Isaacs/mathlib 被覆 or BG 自己再証明。**Prop 1.16 第1式 = Gorenstein 6.2.4
= Isaacs FGT Thm 6.21** は repo 既存 (`Isaacs.Ch06.nontrivialActionFixedByClosure_eq_top_of_not_isCyclic`)。
詳細は memory `ft-master-roadmap` の「Gorenstein 監査」節 + `ft_master_roadmap_2026_05_29.md` の追補。

## §7 (`OddOrder/BG/Ch2_Uniqueness/S07_Transitivity.lean`) の状況

**✅ 完成 (sorry-free, axiom-clean, AxiomsCheck 登録)**:
- `inductiveLemma` (Lemma 7.1, 推移性 keystone)
- `transitive_of_three_le_rank_center` (Thm 7.2, m(Z(A))≥3)
- `transitive_of_two_le_rank_center_of_dvd` (Thm 7.3, m(Z(A))≥2 ∧ q∈π(C_G(A)))
- `mem_kSubgroup_of_piPrime_mem_centralizer` (§7 Note)

**残 sorry 3 個** (lines ~1042/1065/1081):
- `transitivity_propagates` (Thm 7.4, propagation)
- `hypothesis71_of_scn2_or_pLengthOne` (Prop 7.5, Hyp 7.1 十分条件)
- `thompsonTransitivity` (Thm 7.6, Thompson Transitivity Thm)

## 🔑 再利用可能インフラ (S07 内、§8–§16 でも使う)

- **`exists_mem_inf_centralizer_ne_bot_of_not_isCyclic`** (Prop 1.16(1) 共役形):
  noncyclic abelian `B≤G` が coprime `Q≠1` 正規化 ⇒ `∃x∈B^#, Q⊓C_G(x)≠1`。
- **`exists_cocyclic_inf_centralizer_ne_bot_of_not_isCyclic`** (Prop 1.16(2) 共役形):
  ⇒ `∃Y≤B, (∃b∈B, Y⊔⟨b⟩=B), Q⊓C_G(Y)≠1`。
- **`OddOrder.BG.Ch1.S01.cocyclicFixedByClosure_eq_top_of_not_isCyclic`** (Prop 1.16(2) 抽象形)
  + **`...nontrivialActionFixedByClosure_eq_top_of_not_isCyclic'`** (第1式 φ 形)
  (`S01b_Prop116.lean`)。第1式抽象形 = `Isaacs.Ch06.nontrivialActionFixedByClosure_eq_top_of_not_isCyclic`。
- private helper: `exists_elementaryAbelian_le_center_of_le_rank` (m(Z(A))≥n→noncyclic
  elem-ab B≤Z(A)), `not_isCyclic_of_cocyclic`, `centralizer_singleton_lt_top` (Z(G)=⊥),
  `conjAction`/`actionFixedBy_conjAction_restrict`, `isAInvariant_conjAction_iff`,
  `exists_le_pRank_of_le_rank`。

## 残り §7 の正確な依存 (⚠️ 全て §6/§1 の未形式化結果に blocked)

**重要**: `S06_Additional.lean` は **sorry 0 だが Thm 6.1–6.7 / Lem 6.5 を含まない**
(J(P)/Thompson subgroup の別補題のみ; ファイル L30 が「6.3-6.7, 6.5-6.6 = TODO」と明記)。
旧 `s07_lem71_implementation` note の「§6 sorry-free ✅」は**誤り**。

- **Thm 7.6** (`thompsonTransitivity`, mmd L2311) = **Prop 7.5(2) + Thm 7.2 (✅)**。
  追加: A∈SCN₃⇒A abelian∧m(A)≥3 (Z(A)=A), A は p-群ゆえ `primesOf A={p}` で
  `kSubgroup A = opiCoreInG {p}ᶜ (C_G(A))` 橋。**唯一の blocker = Prop 7.5**。
- **Prop 7.5** (`hypothesis71_of_scn2_or_pLengthOne`, mmd L2252):
  - case (1) (p-length one): **Thm 6.7** から従う (§6, **未形式化**)。
  - case (2) (SCN₂): 本体。依存 = **Thm 6.1** (=Hall-Higman, App.A `thmA4b` ✅) +
    **Prop 1.10** (`S01.coprime_nilpotent_acts_trivially_of_centralizer_self` ✅) +
    **Prop 1.15(b)** (`O_{p'}(C_X(b))⊆O_{p'}(X)`, **未形式化** — S01 には 1.15(a) のみ) +
    **Prop 1.16** (✅) + **G Thm 2.6.4** (p-群: `Ω₁(A)/Z ∩ Z(P/Z)≠1`, Isaacs 流に要形式化)。
    構成: B∈ℰ_p²(A)∧B⊴P 取得 → X=C_G(b) (b∈B^#) で Y⊆O_{p'}(X) を2段 (b∈Z / 一般) で示し
    Prop 1.16 (7.5) で Y=⟨C_Y(b)⟩ から結論。
- **Thm 7.4** (`transitivity_propagates`, mmd L2197): **|P:A| 帰納 (composition series)**。
  (a) C_K(P)=O_{π'}(C_G(P)) は易 (C_G(P)⊆C_G(A))。(b)(c)(d) は subnormal refine + 2 case
  (k≤n-2 で B=P_{n-1} 再帰 / k>n-2 で A⊴P, |P/A| prime)。(c) は Prop 1.5(✅)+normalizer 増大、
  (b) は KP solvable + Hall π/π' で共役、**(d) は Lem 6.5** (§6, **未形式化**) で
  `P∩N_G(P)'⊆N_G(Q)'`。**blocker = Lem 6.5 + composition series 機構**。

### ⇒ 次セッションの推奨
**残り §7 は §6 (Thm 6.7, Lem 6.5) + §1 Prop 1.15(b) を先に形式化しないと進めない**。選択肢:
1. **§6 Thm 6.7 / Lem 6.5 + Prop 1.15(b) を形式化** → Prop 7.5 → Thm 7.6 (Thompson Transitivity,
   §8–§16 最頻出) を開く。Thm 6.1 (App.A `thmA4b`) は既にある。
2. Thm 7.4 の (a)(c) のみ先行 ((d) は Lem 6.5 待ち) — 部分的、価値低め。
3. §7 を離れて別の高レバレッジ箇所 (実 sorry 2 個 = Peterfalvi (6.8) issue 0046 /
   (7.10) issue 0044) — ただし別 worktree `odd-order-repr-infra` が (6.8) を LIVE で進行中
   (memory 参照, 削除不可)。

## Lean 罠 (このセッションで遭遇・記録)

- **`Subgroup.normalizer` は Set G を取る**: `normalizer (H⊓Q)` は set-inf に化け `apply
  mem_normalizer_of_conj_smul_eq_self` が coe 逆算失敗 → `(Q := H⊓Q)` 明示で defeq 通過。
- **結合マクロン識別子 `ḡ`/`Ȳ`/`ȳ` は Lean で不正** ("expected token") → ASCII (`gbar`/`Yb`/`yb`)。
- `smul_inf` = `Subgroup.smul_inf`。`inf_comm` は明示引数 `inf_comm a b` (2-inf 曖昧回避)。
- `Subgroup.card_eq_one` (=⊥ iff), `Subgroup.bot_subgroupOf`, `Subgroup.nontrivial_iff_ne_bot`,
  `bot_ne_top` (要 Nontrivial), `Subgroup.card_quotient_dvd_card`, `Nat.le_log_iff_pow_le`
  (`.mp`), `Subgroup.map_eq_bot_iff` (H 明示引数), `Subgroup.map_top_of_surjective`,
  `exists_prime_orderOf_dvd_card'` (primed = Nat.card/Finite), `Subgroup.center` 正規性 =
  `inferInstance`, `isSolvable_of_comm`。
- **`set NQ` 後の新項は `have ... : ...NQ... := term` の型注釈で defeq 折りたたみ** (rw は
  syntactic で folding しない)。
- **rank 抽出**: `rank=⨆_p pRank` (`exists_le_pRank_of_le_rank` 自作)、`rank_le_of_injective`
  に MulEquiv を渡すとき `(f := e.toMonoidHom)` 明示 (coe 不一致)。**p は素数とは限らない**が
  coprimality は `B≤A ∧ q∈π'` から従い、noncyclic は exponent|p ∧ |B|≥p² から従う (primality 不要)。
- S01 の `isAInvariant_subgroupOf_restrict`/`isAInvariant_map_subtype_of_restrict` は private
  → S07 で局所再証明済。
- `congrArg Subtype.val` が ↥Y と推論される時は `(Subtype.val : ↥B → G)` 明示。

## 配置メモ
- Prop 1.16: `OddOrder/BG/Ch1_Preliminary/S01b_Prop116.lean` (設計 = `s01_prop116_implementation.md`)。
- §7: `OddOrder/BG/Ch2_Uniqueness/S07_Transitivity.lean` (Lem 7.1 設計 = `s07_lem71_implementation_2026_06_02.md`)。
- §7 は `import S01b_Prop116` + `open OddOrder.Isaacs.Ch06 (actionFixedBy ...)`。
