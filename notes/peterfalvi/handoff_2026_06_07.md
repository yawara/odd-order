# Peterfalvi (6.8) — handoff 2026-06-07 (central-Zc redesign: L2 complete, L3 in progress)

**Branch `peterfalvi` @ `fbf51ef`. Working tree clean. Leaf builds green. S08 has
exactly 1 sorry = the capstone `sibleySetup_is_coherent` (S08:~8536).**
Ralph loop is **cancelled**. Source of truth = `notes/peterfalvi/s08_6_8_blocker_central_Z.md`
(read it first — has findings #1–#6, the L2 field map, the L3 ν-construction analysis, and the
**2026-06-07 session-2** section at the bottom).

> **Session-2 update (2026-06-07):** two increments toward L3 (3b) `himg_ortho`.
> 1. **✅ reg-char ingredient `sumNonInflatedDegreeMulChar_of_mem`** (`InflationCharacter.lean`,
>    commit `abbe578`, sorry-free + axiom-clean): `∑_{N⊄ker χ} χ(1)χ(z) = -|G⧸N|` for `z∈N^#` —
>    the mmd L168 `∑dᵢχᵢ(z)` value. **Not yet consumed** (its consumer, the `Res η₁^{τ₁}`
>    decomposition = task #3, is still to be built).
> 2. **🟢 (6.7)-wiring feasibility RESOLVED** (notes commit `fbf51ef`): the handoff's open structural
>    risk — "is `H` Sylow-`p` of `G`?" — is YES in the Frobenius case (H ◁ L + coprime complement ⟹
>    unique normal Sylow-p of L ⟹ every p-elt of L in H; + N_G(Ĥ)=L from TI ⟹ Ĥ:=H.map L.subtype
>    ∈ Syl_p(G)). No missing hypothesis field. mathlib tools identified
>    (`normalizerCondition_of_isNilpotent`, `IsPGroup.exists_le_sylow`, `Sylow.unique_of_normal`,
>    `coprime_card_kernel_complement`); the `Ĥ∈Syl_p(G)` lemma (~80-120 lines, subgroupOf-normalizer
>    + L↔G bookkeeping) is the de-risked next build (task #2, step 2). Full 5-step plan in the
>    blocker-note session-2 section.

> ⚠️ Work is in the **worktree** `/home/ywr/odd-order-peterfalvi` (branch `peterfalvi`), NOT main.
> `.lake/packages` + `references` are symlinks from main. The branch carries interleaved
> `bg-s10-*`/`peterfalvi` `--no-ff` lane merges (the project's multi-lane integration) — normal;
> all the (6.8) work below is intact. Build leaf: `lake build OddOrder.Peterfalvi.S08_CoherenceTheorems`
> (~2s); full: `lake build OddOrder` (~75s).

## The big picture
Closing the (6.8) capstone `sibleySetup_is_coherent` (the sole S08 sorry; one of two sorries
blocking §9 (7.10)) via the **central-`Zc` redesign** (`Zc = Z(H)∩H′`, central — replaces the
unfillable `Z=⁅H,H⁆` route; see blocker note finding #2). Capstone target structure:
`by_cases hcoh : Nonempty CoherenceTarget` (`= IsCoherent … hyp.S …`); coherent → `hcoh.some`;
`¬coherent` → `exfalso`; `rcases hyp.cases` (Frobenius ∨ CertainType):
- **Frobenius / case (A):** L2 (`IsCoherent (Xset Zc)`) → L3 (`IsCoherent (Xset Zc ∪ Yset)`) →
  L4 (`false_of_coherentXunionYset_of_not_coherentS`, with `hHnonab` from X-nonempty).
- **CertainType / case (B):** the (6.8.2) analogue (`Z=W₂`) — **unplanned**.

## ✅ DONE this session (all sorry-free + axiom-clean unless noted)
| piece | def / commit | where |
|---|---|---|
| (6.7) IS formalized (corrected the note) | `peterfalvi_67_of_odd` | `SylowTICongruence.lean:140` |
| finding #6 diagnosis (the real blocker) | `d788674` | blocker note |
| L1 central-`Zc` facts | `b501838`/`b70b430` (pre-session) | S08 `centralCommutator*` |
| L4 (6.8.3) extension | `c49ab3f` (pre-session) | `false_of_coherentXunionYset_of_not_coherentS` (S08:~5840) |
| L2 hXne | `264966a` | `Xset_centralCommutator_nonempty`, `Xset_nonempty_of_subgroupOf_ne_bot` |
| L2 contract fix (additive `…withCover…`) | `2e46520` | `Xset_isCoherent_from_adjoinSteps_withCover_of_irreducible_X` + base-anchor consumer + Zc shell |
| L2 sub-lemma: anchor | `1f2148a` | `exists_xBaseBlock_anchor_index` |
| L2 sub-lemma: htail_le core | `c8ddb62` | `characterDegree_re_le_of_not_mem_pairUnion` (uses `hcover`) |
| L2 sub-lemma: hsum core | `ab01c92` | `natSum_partition_of_realSum` (abstract) |
| **L2 producer monolith** | **`d21d788`** | **`Xset_centralCommutator_isCoherent_of_frobenius`** → `IsCoherent (Xset Zc)` |
| L3 outer shell | `13836f7` | `coherentXunionYset_centralCommutator_of_glued_of_frobenius` |

**L2 is complete** (the X(Zc)-coherence — the hardest assembly; first-ever `…StepData` value built,
tail `Fin tailF.card`-indexed for the `κ:Type` constraint, ∃→data via `choose`). **L3 shell** takes
the (6.8.1) `τ₃` glue data (`ν`/`hagreeX`/`hagreeY`/`hmixed`/`hgen`) and produces
`IsCoherent (Xset Zc ∪ Yset)` (what L4 consumes); source-orthogonality discharged internally.

## 🔴 REMAINING (in dependency order)

### (1) L3 (3b): construct `ν` + prove `himg_ortho` — the deep (6.8.1) τ₃ argument
This is the gating piece (deep character theory; multi-session). Full analysis in the blocker note
(`### L3 ν-construction analysis`). Summary:
- `IntegralCharacterMap = ClassFunction ↥L →ₗ[ℤ] ClassFunction G` (S07:301). τ₁ =
  `coherentYset.extension`, τ₂ = `(Xset_centralCommutator_isCoherent_of_frobenius …).extension`.
- **(a)** construct `ν` agreeing with τ₂ on X-irr, τ₁ on Y-irr. Extensions are built via `retarget`
  (S07:2680), not `Basis.constr` — a **glue/combination constructor likely needs adding** to S07.
  Then `hagreeX`/`hagreeY` hold by ℤ-linearity on `ℤ[X]`/`ℤ[Y]`.
- **(b)** `hmixed ⟺ himg_ortho : ⟨τ₂ x, τ₁ y⟩ = 0` (`X^τ₂ ⊥ Y^τ₁`) — since `⟨x,y⟩=0` (source-ortho).
  This is the (6.8.1) `b≡c≡0 mod a` argument (mmd 04.8 L160-176): write
  `(χ₁−aη₁)^τ = X − a η₁^{τ₁} + b∑η_j^{τ₁}`; `Res η₁^{τ₁} = c∑dᵢχᵢ+χ′ = (c/a)(ρ_L−ρ_{L/Z})` ⟹
  `η₁^{τ₁}` const on `Z^#` ⟹ **(6.7) `peterfalvi_67_of_odd`** ⟹ `c≡0 mod a`; norm bound
  `(x−1)²+(m−1)x²≤1+1/a²`, `a>1` ⟹ `b=0` ⟹ `X=χ₁^{τ₁}` ⟹ `τ₃` isometry.
  Sub-lemmas to build: regular-character decomposition `∑dᵢχᵢ=(ρ_L−ρ_{L/Z})/(a|W₁|)`;
  the **(6.7) wiring** (match SibleyDade TI/Sylow/odd setup to `peterfalvi_67_of_odd`'s hyps: `P`,
  `L=N_G(P)`, `Z≤Z(P)`, `|C_L(·)|` const on `Z^#`); the norm/inner computations.
- Once (a)+(b): feed the **L3 shell** `coherentXunionYset_centralCommutator_of_glued_of_frobenius`
  → `IsCoherent (Xset Zc ∪ Yset)`, wrap `Nonempty`, hand to L4.

### (2) CertainType case (B): the (6.8.2) analogue
`Z=W₂`; mmd (6.8.2)/(6.8.2.1-3). Separate deep argument (the `τ₂`/`Y` independence via (6.7)).
Needs its own central-Z-style facts + a case-(B) L4. **Unplanned** — scope before starting.

### (3) capstone wiring
`sibleySetup_is_coherent` (S08:~8536 sorry): `by_cases Nonempty CoherenceTarget`; coherent →
`hcoh.some`; `¬` → `exfalso`; `rcases hyp.cases`; Frobenius → (need `hHnonab` from the X-nonempty
branch condition + `isPGroup_of_not_coherent` + `three_le_prime_of_isPGroup_of_odd` for L2's
`p`/`hp3`/`hHp`) → L2+L3+L4; CertainType → (2). Mechanical once (1),(2) exist.

## Recommended resume order
Read the blocker note → tackle **L3 (b) `himg_ortho`** first (the gating math; start with the (6.7)
wiring as a self-contained sub-lemma, and the regular-character decomposition). Then L3 (a) `ν`
constructor. Then capstone-wire the Frobenius branch (gives a *conditional* capstone, case-B still
sorry). Then case (B). The deep (6.8.1)/(6.8.2) character theory is best done attended in focused
sessions (not autonomous grinding — it is subtle, unlike the mechanical L2 StepData assembly).

## Notes/memory updated
`s08_6_8_blocker_central_Z.md` (source of truth, findings #1–#6 + L2 field map + L3 analysis);
memory `peterfalvi-s6-coherence-reduction.md` (L2 DONE). `loop_task_central_zc.md` has the loop spec
(Steps 1–2 done; if re-running the loop, point Step 3 at the L3 ν-construction — but see the
"attended, not autonomous" caveat above).
