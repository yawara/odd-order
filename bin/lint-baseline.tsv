# bin/lint-baseline.tsv — lint 警告 ratchet の基準 (issue 0138, closed)
# `count<TAB>relpath<TAB>category`。
# 2026-07-24: backlog 完済 (230→0)、CI gate は `bin/check-warnings --strict` (純ゼロ) に切替済み。
# baseline は空 (空 baseline では無印 `bin/check-warnings` も strict と等価)。
# 無断で baseline を復活させない — 正当な例外は per-decl `set_option linter.<name> false in`。
